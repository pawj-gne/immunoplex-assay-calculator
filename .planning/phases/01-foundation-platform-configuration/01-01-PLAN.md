---
phase: 01-foundation-platform-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/assets/index.css
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Application launches and displays main navigation shell"
    - "Electron main process starts without errors"
    - "React renderer loads in Electron window"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "electron-vite"
    - path: "src/main/index.ts"
      provides: "Electron main process entry point"
      contains: "BrowserWindow"
    - path: "src/preload/index.ts"
      provides: "Secure context bridge setup"
      contains: "contextBridge"
    - path: "src/renderer/src/App.tsx"
      provides: "Root React component with app shell"
      min_lines: 20
  key_links:
    - from: "src/main/index.ts"
      to: "src/preload/index.ts"
      via: "BrowserWindow preload config"
      pattern: "preload.*index"
    - from: "src/renderer/src/main.tsx"
      to: "src/renderer/src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
---

<objective>
Scaffold the Electron + React + TypeScript project using electron-vite with a basic application shell.

Purpose: Establish the project foundation that all subsequent phases build upon. This includes the Electron main process, preload scripts with context isolation, and a React renderer with Tailwind CSS styling.

Output: A launchable Electron application with a main navigation shell displaying the app name and placeholder content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-platform-configuration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Electron + React + TypeScript project</name>
  <files>
    package.json
    electron.vite.config.ts
    tsconfig.json
    tsconfig.node.json
    tsconfig.web.json
    src/main/index.ts
    src/preload/index.ts
    src/preload/index.d.ts
    src/renderer/index.html
    src/renderer/src/main.tsx
    src/renderer/src/env.d.ts
    .gitignore
  </files>
  <action>
    Create project using electron-vite React TypeScript template:
    ```bash
    npm create @quick-start/electron@latest . -- --template react-ts
    ```

    If the directory is not empty, either clear it first or create in a temp directory and move files.

    After scaffolding:
    1. Install additional dependencies:
       ```bash
       npm install decimal.js zod zustand
       npm install -D @types/node
       ```

    2. Update .gitignore to include:
       - node_modules/
       - dist/
       - out/
       - .env
       - *.log

    3. Verify electron-vite.config.ts exists and is configured for main/preload/renderer builds.

    4. Run electron-rebuild for native module compatibility (prep for Plan 02):
       ```bash
       npm install -D electron-rebuild
       ```

    Do NOT install better-sqlite3 or drizzle yet - that is Plan 02's scope.
  </action>
  <verify>
    Run `npm run dev` - Electron window should open with default template content.
    Run `npm run build` - Should complete without errors.
  </verify>
  <done>
    Project scaffolded with all electron-vite template files, additional dependencies installed (decimal.js, zod, zustand), and both dev and build commands work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 with app shell</name>
  <files>
    src/renderer/src/assets/index.css
    src/renderer/src/App.tsx
    package.json
  </files>
  <action>
    1. Install Tailwind CSS v4 and animation library:
       ```bash
       npm install tailwindcss @tailwindcss/vite tw-animate-css
       ```

    2. Update electron.vite.config.ts to include Tailwind plugin in renderer config:
       ```typescript
       import tailwindcss from '@tailwindcss/vite'
       // In renderer plugins array:
       plugins: [react(), tailwindcss()]
       ```

    3. Replace src/renderer/src/assets/index.css with Tailwind v4 CSS-first config:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";

       @theme {
         --font-sans: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
         --color-primary: #2563eb;
         --color-primary-foreground: #ffffff;
         --color-background: #f8fafc;
         --color-foreground: #0f172a;
         --color-muted: #64748b;
         --color-border: #e2e8f0;
       }

       body {
         font-family: var(--font-sans);
         background-color: var(--color-background);
         color: var(--color-foreground);
       }
       ```

    4. Create App.tsx with main navigation shell:
       ```tsx
       function App(): JSX.Element {
         return (
           <div className="min-h-screen flex flex-col">
             {/* Header */}
             <header className="bg-white border-b border-[var(--color-border)] px-6 py-4">
               <h1 className="text-xl font-semibold text-[var(--color-foreground)]">
                 Immunoplex Assay Calculator
               </h1>
             </header>

             {/* Main content area */}
             <main className="flex-1 p-6">
               <div className="max-w-4xl mx-auto">
                 <div className="bg-white rounded-lg border border-[var(--color-border)] p-6">
                   <h2 className="text-lg font-medium mb-4">Welcome</h2>
                   <p className="text-[var(--color-muted)]">
                     Select a platform to begin calculating reagent volumes.
                   </p>
                   {/* Platform selector will be added in Plan 03 */}
                   <div className="mt-6 p-4 bg-[var(--color-background)] rounded border border-dashed border-[var(--color-border)]">
                     <p className="text-sm text-[var(--color-muted)]">
                       Platform selection coming soon...
                     </p>
                   </div>
                 </div>
               </div>
             </main>

             {/* Footer */}
             <footer className="border-t border-[var(--color-border)] px-6 py-3">
               <p className="text-sm text-[var(--color-muted)]">
                 Phase 1: Foundation
               </p>
             </footer>
           </div>
         );
       }

       export default App;
       ```

    5. Ensure main.tsx imports the CSS file:
       ```tsx
       import './assets/index.css'
       ```
  </action>
  <verify>
    Run `npm run dev` - Application shows styled header with "Immunoplex Assay Calculator", main content area with welcome message and placeholder, and footer.
  </verify>
  <done>
    Tailwind CSS v4 configured with CSS-first approach, app shell displays with header, main content area, and footer. Styling uses CSS variables for theming.
  </done>
</task>

<task type="auto">
  <name>Task 3: Set up preload script with type declarations</name>
  <files>
    src/preload/index.ts
    src/preload/index.d.ts
    src/renderer/src/env.d.ts
  </files>
  <action>
    1. Update src/preload/index.ts to expose a minimal API structure for future IPC:
       ```typescript
       import { contextBridge, ipcRenderer } from 'electron'

       // Expose protected methods that allow the renderer process to use
       // ipcRenderer without exposing the entire object
       contextBridge.exposeInMainWorld('electronAPI', {
         // Platform operations (to be implemented in Plan 02)
         platform: {
           getAll: (): Promise<unknown[]> => ipcRenderer.invoke('platform:get-all'),
           getById: (id: string): Promise<unknown> => ipcRenderer.invoke('platform:get-by-id', id),
         },
         // Database health check (to be implemented in Plan 02)
         db: {
           health: (): Promise<{ ok: boolean }> => ipcRenderer.invoke('db:health'),
         },
       })
       ```

    2. Create/update src/preload/index.d.ts with type declarations:
       ```typescript
       import { ElectronAPI } from '@electron-toolkit/preload'

       // Placeholder types until Plan 02 defines proper Platform type
       interface Platform {
         id: string
         name: string
         description: string | null
         stockConcentration: number
         createdAt: string
         updatedAt: string
       }

       interface CustomElectronAPI {
         platform: {
           getAll: () => Promise<Platform[]>
           getById: (id: string) => Promise<Platform | null>
         }
         db: {
           health: () => Promise<{ ok: boolean }>
         }
       }

       declare global {
         interface Window {
           electronAPI: CustomElectronAPI
           electron: ElectronAPI
         }
       }
       ```

    3. Update src/renderer/src/env.d.ts to reference the preload types:
       ```typescript
       /// <reference types="vite/client" />
       /// <reference types="../../preload/index.d.ts" />
       ```

    Note: The IPC handlers in main process will be implemented in Plan 02. For now, calling these methods will result in "No handler registered" errors, which is expected.
  </action>
  <verify>
    Run `npm run dev` - Application launches without TypeScript errors.
    In DevTools console, `window.electronAPI` should be defined (though methods will fail until Plan 02).
  </verify>
  <done>
    Preload script exposes typed electronAPI on window object. Type declarations allow renderer to access API with full TypeScript support. IPC method stubs ready for Plan 02 implementation.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` launches Electron window with styled app shell
2. `npm run build` completes without errors
3. App displays header with "Immunoplex Assay Calculator"
4. App displays main content area with welcome message
5. `window.electronAPI` is accessible in DevTools console
6. No TypeScript errors in editor or build
</verification>

<success_criteria>
- Electron application launches successfully
- React renderer displays styled navigation shell
- Tailwind CSS v4 styling applied correctly
- Preload script exposes typed API structure
- Build process completes without errors
- Project ready for Plan 02 (database layer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform-configuration/01-01-SUMMARY.md`
</output>
