---
phase: 04-run-documentation-persistence-deployment
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/renderer/src/stores/runStore.ts
  - src/renderer/src/features/run/components/RunMetadataForm.tsx
  - src/renderer/src/features/run/components/RunList.tsx
  - src/renderer/src/features/run/index.ts
autonomous: true

must_haves:
  truths:
    - "Operator can enter all run metadata fields"
    - "Operator can save a run record"
    - "Operator can see list of saved runs"
    - "Operator can load a previously saved run"
    - "Saved runs persist across app restarts"
  artifacts:
    - path: "src/renderer/src/stores/runStore.ts"
      provides: "Zustand store for run state"
      exports: ["useRunStore"]
    - path: "src/renderer/src/features/run/components/RunMetadataForm.tsx"
      provides: "Form for all DOCM-01 metadata fields"
      min_lines: 80
    - path: "src/renderer/src/features/run/components/RunList.tsx"
      provides: "List of saved runs with load action"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/stores/runStore.ts"
      to: "window.electronAPI.run"
      via: "preload API calls"
      pattern: "electronAPI\\.run\\."
    - from: "RunMetadataForm.tsx"
      to: "runStore"
      via: "useRunStore hook"
      pattern: "useRunStore"
---

<objective>
Build run metadata UI: Zustand store, metadata form with all DOCM-01 fields, run list with load capability.

Purpose: Operators can capture, save, and retrieve run documentation records.
Output: Working run feature with form, list, and store integrated into the app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-run-documentation-persistence-deployment/04-RESEARCH.md
@.planning/phases/04-run-documentation-persistence-deployment/04-01-SUMMARY.md
@src/renderer/src/stores/calculatorStore.ts
@src/renderer/src/features/platform/
@src/renderer/src/features/selection/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand store and RunMetadataForm</name>
  <files>
    src/renderer/src/stores/runStore.ts
    src/renderer/src/features/run/components/RunMetadataForm.tsx
    src/renderer/src/features/run/index.ts
  </files>
  <action>
1. Create src/renderer/src/stores/runStore.ts following existing store patterns (see calculatorStore.ts):
   - State: `runs: RunRecord[]`, `currentRun: RunRecord | null`, `loading: boolean`, `error: string | null`
   - Actions: `fetchRuns()`, `saveRun(data: RunCreate)`, `loadRun(id: string)`, `deleteRun(id: string)`, `clearCurrentRun()`
   - All actions call window.electronAPI.run.* methods
   - saveRun updates the runs list optimistically (prepend new run)
   - loadRun sets currentRun
   - Handle errors: catch and set error state

2. Create src/renderer/src/features/run/components/RunMetadataForm.tsx:
   - Form with ALL DOCM-01 fields:
     - userName (text input)
     - operatorName (text input)
     - runDate (date input, default today)
     - numberOfPlates (number input)
     - numberOfSamples (number input)
     - sampleType (text input or select)
     - replicateMode (select: singlet/duplicate)
     - hamiltonAssignment (text input, optional)
     - tubeBlockPositions (text input, optional)
     - troughPositions (text input, optional)
   - Platform, species, panel, and analyte selections should read from existing stores (platformStore, selection stores) -- these are already selected in the app flow. Pre-populate from current calculator/selection state.
   - Save button calls useRunStore().saveRun() with form data + current platform/species/panel/analyte IDs from existing stores
   - Show success/error feedback after save
   - Use Tailwind CSS for styling, consistent with existing app style
   - When currentRun is set in store, populate form fields from it (load mode)

3. Create src/renderer/src/features/run/index.ts barrel export.
  </action>
  <verify>
    App builds. Form renders with all fields. Can type in fields without errors.
  </verify>
  <done>RunMetadataForm renders all DOCM-01 fields, connects to Zustand store, can save run records.</done>
</task>

<task type="auto">
  <name>Task 2: RunList and app integration</name>
  <files>
    src/renderer/src/features/run/components/RunList.tsx
  </files>
  <action>
1. Create src/renderer/src/features/run/components/RunList.tsx:
   - On mount, call useRunStore().fetchRuns()
   - Display list of saved runs showing: runDate, userName, platformId (or name if available), sampleType
   - Each row has "Load" button that calls loadRun(id) -- this populates the form
   - Each row has "Delete" button that calls deleteRun(id) with confirmation
   - Empty state: "No saved runs" message
   - Tailwind styling consistent with app

2. Integrate into the app layout:
   - Add Run Documentation section/tab/panel to the main app view
   - This could be a new section below existing content, or a tab -- follow the existing app navigation pattern
   - Include both RunMetadataForm and RunList in the run documentation area
   - Ensure the run feature is accessible from the main app flow
  </action>
  <verify>
    Launch app in dev mode. Navigate to run documentation. Save a run, see it in list. Close and reopen app, run still appears in list. Load a saved run, form populates.
  </verify>
  <done>Operator can save runs, see them in a list, load them back, and data persists across restarts.</done>
</task>

</tasks>

<verification>
- App launches and shows run documentation UI
- All DOCM-01 metadata fields are present and editable
- Save creates a record that appears in the list
- Load populates form from a saved record
- Close and reopen app -- saved runs still appear
- Delete removes a run from the list
</verification>

<success_criteria>
Complete run documentation workflow: operator enters metadata, saves, sees in list, loads back, persists across restarts. All DOCM-01 fields captured.
</success_criteria>

<output>
After completion, create `.planning/phases/04-run-documentation-persistence-deployment/04-02-SUMMARY.md`
</output>
