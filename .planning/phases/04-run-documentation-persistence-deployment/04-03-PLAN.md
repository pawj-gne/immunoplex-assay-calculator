---
phase: 04-run-documentation-persistence-deployment
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - electron-builder.yml
  - package.json
autonomous: false

must_haves:
  truths:
    - "Application packages into a Windows .exe installer"
    - "Installer runs on production PC without dev tools"
    - "better-sqlite3 native module works in packaged app"
  artifacts:
    - path: "dist/"
      provides: "Windows NSIS installer .exe"
  key_links:
    - from: "electron-builder.yml"
      to: "better-sqlite3"
      via: "asarUnpack includes native module"
      pattern: "asarUnpack"
---

<objective>
Package the application as a Windows .exe installer using electron-builder.

Purpose: Operators can install and run the app on the production PC without Node.js or dev tools.
Output: Working NSIS installer .exe in dist/ folder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-run-documentation-persistence-deployment/04-RESEARCH.md
@electron-builder.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure and build Windows installer</name>
  <files>
    electron-builder.yml
    package.json
  </files>
  <action>
1. Review electron-builder.yml. Ensure:
   - `asarUnpack` includes `node_modules/better-sqlite3/**` (native module must be outside ASAR)
   - `extraResources` includes `drizzle/migrations/**` (migrations folder for production)
   - NSIS settings are configured for single-user install (no admin required if possible)
   - Product name, app ID, and description are set correctly

2. Review package.json build scripts. Ensure `build:win` script exists and works.

3. Run `npm run build:win` to produce the installer.

4. If build fails due to native module issues:
   - Check that electron-rebuild runs correctly
   - Verify better-sqlite3 is in asarUnpack
   - Check that drizzle migrations are in extraResources

5. Verify the output: an .exe installer should exist in dist/ folder.
  </action>
  <verify>
    dist/ contains a .exe installer file. File size is reasonable (50-150MB for Electron app).
  </verify>
  <done>Windows .exe installer built successfully in dist/ folder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Windows .exe installer for the Immunoplex Assay Calculator</what-built>
  <how-to-verify>
    1. Run the installer .exe from the dist/ folder
    2. Complete installation (default location is fine)
    3. Launch the installed application from Start Menu or Desktop shortcut
    4. Verify: App opens and shows the main UI
    5. Verify: Can select a platform, enter calculator inputs
    6. Verify: Can save a run record and see it in the list
    7. Close the app, reopen it, verify saved run persists
    8. (Optional) Copy installer to production PC and repeat steps 2-7
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- .exe installer exists in dist/
- Installer runs and installs the app
- App launches from installed location without dev tools
- All features work: platform selection, calculations, plate viz, run save/load
- Data persists across app restarts
</verification>

<success_criteria>
Application packaged as Windows .exe installer. Installer works on target machine. All features functional in packaged build.
</success_criteria>

<output>
After completion, create `.planning/phases/04-run-documentation-persistence-deployment/04-03-SUMMARY.md`
</output>
