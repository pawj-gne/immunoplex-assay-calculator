---
phase: 04.1-smoke-test-fixes
plan: 05
type: execute
wave: 3
depends_on:
  - 04.1-01
  - 04.1-02
  - 04.1-03
  - 04.1-04
files_modified:
  - package.json
autonomous: false
requirements:
  - D-4.1-06
  - ROADMAP-4.1-Success-Criterion-5
user_setup:
  - service: windows-workstation
    why: "Phase 4.1 fixes must be verified on a physical Windows PC — CLAUDE.md §Testing Windows-only. macOS dev host cannot exercise the .exe."
    env_vars: []
    dashboard_config:
      - task: "Install dist/immunoplex-assay-calculator-{VERSION}-x64-setup.exe on the Windows lab workstation"
        location: "Windows lab PC used for Phase 4 smoke test"
      - task: "Run the full 14-step smoke test from .planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md"
        location: "Windows lab PC"
      - task: "Explicitly verify the 4 Phase 4.1 fix acceptance checks (BUG-01 column-major duplicates, BUG-02 multi-plate preservation, BUG-03 species visible in 3 places, UI-01 panel scoping + stale-singles clear)"
        location: "Windows lab PC"

tags:
  - release
  - windows-installer
  - retest
  - human-uat

must_haves:
  truths:
    - "package.json version bumped from 0.5.0 to 0.6.0 (Phase 4 bundle label previously used in Plan 04-03 text but never committed to package.json)"
    - "`npm run typecheck` passes on the dev/v1-01 branch with all four Wave 1+2 fixes merged (04.1-01, 02, 03, 04)"
    - "`npm run build:win` produces a fresh dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe"
    - "The 14-step smoke test from 04-SMOKE-TEST-GUIDE.md completes end-to-end on the Windows workstation with a fresh-installed 0.6.0 exe"
    - "Each of the 4 Phase 4.1 fix scenarios (BUG-01, BUG-02, BUG-03, UI-01) passes its explicit acceptance check during the retest"
    - "Retest outcome recorded in .planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md (pass/fail per step + per-fix acceptance)"
  artifacts:
    - path: "package.json"
      provides: "version 0.6.0 (Phase 4 bundle w/ 4.1 fixes)"
      contains: "\"version\": \"0.6.0\""
    - path: "dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe"
      provides: "fresh Windows installer with all 4 Wave 1+2 fixes"
      contains: "(binary)"
    - path: ".planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md"
      provides: "retest outcomes per step + per-fix"
      contains: "Phase 4.1 Smoke Test Retest"
  key_links:
    - from: "Wave 1+2 plans (04.1-01, 02, 03, 04) merged"
      to: "package.json version bump"
      via: "all four fix plans must be complete and green before bumping"
      pattern: "\"version\": \"0.6.0\""
    - from: "package.json 0.6.0"
      to: "npm run build:win"
      via: "electron-builder reads version from package.json for installer artifact name"
      pattern: "0.6.0-x64-setup.exe"
---

<objective>
Close Phase 4.1 by cutting a fresh Windows installer containing all four Wave 1+2 fixes and re-running the full 14-step smoke test from Plan 04-03 / 04-SMOKE-TEST-GUIDE.md.

Per D-4.1-06: "After 4.1 ships, rebuild the Windows .exe and re-run the full 14-step smoke test. Same test cycle, fresh installer. Do not shortcut to testing only the fixed bugs — we want to confirm no regressions elsewhere."

This plan is NOT autonomous. It contains:
- An autonomous pre-flight task (version bump + typecheck + build:win)
- A `checkpoint:human-action` for Windows physical-hardware installation + manual smoke test (CLAUDE.md §Testing Windows-only — macOS dev host cannot run a Windows .exe)
- An autonomous close-out task (record retest outcomes; update ROADMAP + STATE)

Wave 3 — runs after all four fix plans land (Wave 1: 04.1-01, 04.1-03, 04.1-04 on disjoint files; Wave 2: 04.1-02 after 04.1-01 because both edit plateStore.ts).

Output: `dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe` built; Windows retest completed by operator with results recorded in `04.1-SMOKE-TEST-RETEST.md`; if green, Phase 4.1 marked complete and v0.6.0 release path unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-smoke-test-fixes/04.1-CONTEXT.md
@.planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-RESULTS.md
@.planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md
@.planning/phases/04-run-documentation-persistence-deployment/04-03-SUMMARY.md
@package.json
@CLAUDE.md

<release_notes>
The previous Phase 4 build used version 0.5.0 (package.json). Plan 04-03 §SUMMARY labeled the intended release "v0.6.0 (Phase 4 bundle)" but the actual version bump was deferred. Phase 4.1 is the natural bundle point — the 4.1 fixes ride along with the Phase 4 feature set under 0.6.0. See STATE.md Releases table.

Per `.claude/skills/release/SKILL.md`, a full release also covers:
- Pre-flight (clean temp files, commit pending work)
- Build verification (typecheck, full build, build:win, confirm exe)
- Semver bump with user approval (0.5.0 → 0.6.0 is MINOR: new wizard steps + operator management + installer arrived in Phase 4; 4.1 fixes regressions before first external release of that feature set)
- PR creation to the current dev branch (skipped for 4.1 — we're ON dev/v1-01, release skill handles this when we finalize v1.0)
- Post-merge tagging (deferred to v1.0 release)

This plan handles the version bump + build + smoke retest. Tagging + PR + merge-to-main are explicitly OUT of scope and are handled by `/release` or `.claude/skills/release/SKILL.md` after Phase 4.1 passes retest.
</release_notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Version bump + full build verification</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file — version is on line 3)
    - .planning/STATE.md §Releases (confirms last released version is v0.5.0)
    - .planning/phases/04-run-documentation-persistence-deployment/04-03-SUMMARY.md (confirms the "v0.6.0 Phase 4 bundle" label was used in plan text but not committed)
    - CLAUDE.md §Testing Windows-only (confirms the build-on-macOS / test-on-Windows split)
  </read_first>
  <behavior>
    - package.json "version" field changes from "0.5.0" to "0.6.0".
    - All four fix plans (04.1-01 wave 1, 04.1-02 wave 2, 04.1-03 wave 1, 04.1-04 wave 1) are already merged into dev/v1-01 before this task runs (guaranteed by depends_on).
    - `npm run typecheck` passes.
    - `npm run build` succeeds (typecheck + electron-vite build).
    - `npm run build:win` produces dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe AND dist/immunoplex-assay-calculator-0.6.0-arm64-setup.exe (per Plan 04-03 decision: explicit win.target.arch [x64, arm64]).
    - No code changes beyond the version bump — all four fixes are already in place.
  </behavior>
  <action>
1. Edit `package.json`: change `"version": "0.5.0"` to `"version": "0.6.0"` on line 3. No other changes.

2. Run pre-flight from `.claude/skills/release/SKILL.md` Phase 1 / Phase 2:
   ```bash
   # Clean any temp files
   find . -name "tmpclaude-*" -type f -delete 2>/dev/null
   find . -name "temp_*" -type f -delete 2>/dev/null

   # Typecheck
   npm run typecheck

   # Full production build (renderer + main)
   npm run build

   # Windows installer
   npm run build:win

   # Verify both arch installers exist
   ls -la dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe
   ls -la dist/immunoplex-assay-calculator-0.6.0-arm64-setup.exe
   ```

3. Report the exe filenames and sizes in the task output.

4. If ANY step fails:
   - Report the specific failure
   - Do NOT proceed to the human-action task — stop here
   - The executor should escalate so the failure can be diagnosed (typical suspects: Wave 1/2 plan regression, missing merge, electron-rebuild issue from postinstall hook)

5. Do NOT:
   - Tag the release in git (tagging happens via `/release` after phase 4.1 is green)
   - Push to main (we're on dev/v1-01; merge happens later via the release workflow)
   - Edit electron-builder.yml (Plan 04-03 already set appId, arch, nsis.artifactName; they're correct for 0.6.0 because `${version}` interpolates from package.json)
   - Edit any source file outside package.json
  </action>
  <acceptance_criteria>
    - grep -c '"version": "0.6.0"' package.json returns 1 (exact string match on the bumped version)
    - grep -c '"version": "0.5.0"' package.json returns 0 (old version gone)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0 (produces out/main/, out/preload/, out/renderer/)
    - `npm run build:win` exits 0
    - File `dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe` exists and is non-zero size
    - File `dist/immunoplex-assay-calculator-0.6.0-arm64-setup.exe` exists and is non-zero size
    - No uncommitted changes outside package.json and dist/ (git status clean for src/)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "npm run typecheck && npm run build:win && ls dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe dist/immunoplex-assay-calculator-0.6.0-arm64-setup.exe"</automated>
  </verify>
  <done>
    - package.json version = 0.6.0
    - typecheck + build + build:win all green
    - Both x64 and arm64 Windows installers exist under dist/
    - No unexpected diffs outside package.json
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN-UAT — Windows physical-hardware smoke retest</name>
  <files>.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md</files>
  <what-built>
    Plan 04.1-01 (column-major duplicate fill), 04.1-02 (multi-plate preservation), 04.1-03 (Species in RunList + RunMetadataForm banner), 04.1-04 (scoped AnalyteGrid + stale-singles clear on panel switch) are all merged. `dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe` and `...-arm64-setup.exe` are built and ready.
  </what-built>
  <action>
    **This is a human-action checkpoint.** Claude cannot execute the steps below — they require a physical Windows workstation with USB or network transfer from the macOS dev host (per CLAUDE.md §Testing Windows-only). Claude's role here is to (a) remind the operator of the test protocol, (b) wait for the `<resume-signal>`, and (c) record the operator's verbatim results into `.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md` when the operator returns.

    Operator follows the steps documented in `<how-to-verify>` below. Claude remains idle until the operator types the resume signal.
  </action>
  <how-to-verify>
    **Pre-flight:**
    1. Transfer `dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe` (or arm64 as appropriate) to the Windows lab workstation used for the original Phase 4 smoke test.
    2. Uninstall the prior v0.5.0 build if present. Install v0.6.0. Launch the app.

    **Part A — Run the full 14-step smoke test from `04-SMOKE-TEST-GUIDE.md`:**
    (Reference: `.planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md`.)
    For each of steps 1-14, record PASS / FAIL / N/A with a brief note if FAIL. This is the regression net that ensures 4.1 didn't break any other Phase 4 behavior.

    **Part B — Explicit 4.1 fix acceptance (run in addition to / interleaved with Part A where the flow hits the relevant page):**

    **BUG-01 — duplicate pair orientation (column-major):**
    - On Wizard step 3 (Calculations), select replicate mode = Duplicates, sample count = 8.
    - PASS criterion: column 4 fills rows A-H with sample IDs 1-8; column 5 has the duplicates; columns 6-12 are empty. Visually trace: "does sample 1 occupy A4 and A5 (a single row-A pair), or does sample 1 fill A4+B4 with A5+B5 carrying the duplicate?"
    - Expected: samples 1-8 are in column 4 rows A→H (not row A columns 4,6,8,10); column 5 mirrors column 4.
    - Bump sample count to 9 → additional fill is A6+A7 (not a second row-A pair).
    - Bump to 16 → columns 4+5 full; columns 6+7 full; columns 8-12 empty.
    - Bump to 33 → columns 4-11 all full (32 samples); col 12 has A12+E12 for sample 33.
    - Bump to 36 → col 12 fully filled with samples 33-36 at vertical pairs (A/E, B/F, C/G, D/H).
    - Cross-reference Plan 04.1-01 SUMMARY §Verification traces.

    **BUG-02 — multi-plate preservation:**
    - Fresh run. Set sampleCount=16, replicate=singles → plate 1 auto-fills cols 4-5 rows A-H.
    - Click "Add Plate" → plate 2 exists, plate 1 still shows its 16 wells.
    - Navigate to plate 2; manually click well A4 to add it; navigate back to plate 1.
    - PASS criterion: plate 1's 16 wells are all still filled. Plate 2 still has A4.
    - Bump sampleCount to 73 (forcing minPlates=2 in singles). Plate 1's wells survive; plate 2 gains additional auto-fill for samples 17-73.
    - Set sampleCount back to 10. PASS criterion: plate 1's existing 16 wells are preserved (or, if preferred, the first 10 remain and 6 are cleared — the plan's "preserve non-empty" contract favors keeping all 16). Document which behavior you observe.
    - Set sampleCount to 0 → all plates cleared (this is the explicit wipe path — expected).

    **BUG-03 — Species visible:**
    - On Wizard step 1, select Species = Mouse. Complete wizard through step 4 (Document & Save).
    - PASS criterion A: the metadata form (step 4) shows a context banner at top reading `Platform: Milliplex · Species: Mouse · Panel: <name or Custom>`.
    - Save the run.
    - PASS criterion B: the Finalized Run View (step 5) metadata block shows "Species: Mouse" in the definition list. (Already expected from Plan 04-04; confirm still there.)
    - Navigate to the run list (wherever RunList is rendered — likely on step 4 or a saved-runs page).
    - PASS criterion C: the saved run row shows a Species column with "Mouse".

    **UI-01 — panel scoping + stale singles clear:**
    - On Wizard step 1, pick Platform=Milliplex, Species=Mouse. Step 2: two premix panels should appear (Panel I and Panel II).
    - Click "No Premix (Custom Assay)" → the analyte grid shows a single section titled "Analytes" with the full mouse milliplex master list. PASS criterion: no "Panel I" or "Panel II" section headers, no "Selected Panel" pill, no grayed-out analytes, no "All Analytes" heading.
    - Click Panel I → the grid shows a single section titled with Panel I's name, containing Panel I's 32 analytes only. Panel II analytes are NOT visible. Master list is NOT visible.
    - Add 2 singles (if Panel I allows singles on top — per existing max-5-singles rule).
    - Click Panel II → PASS criterion: (a) grid now shows Panel II's analytes only, (b) the 2 singles from Panel I are GONE from the sidebar (selectedSingleIds cleared per Task 1 of Plan 04.1-04), (c) Panel I analytes are not visible.
    - Click "No Premix" → PASS criterion: grid shows the full master list under "Analytes"; any remaining singles are cleared; no panel is selected.

    **Record results** in `.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md` using this structure:

    ```
    # Phase 4.1 Smoke Test Retest

    Tested: YYYY-MM-DD
    Artifact: dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe
    Tester: <your name>

    ## Part A — 14-Step Smoke Test
    | Step | Result | Notes |
    |------|--------|-------|
    | 1 | PASS | |
    | 2 | PASS | |
    ...
    | 14 | PASS | |

    ## Part B — Phase 4.1 Fix Acceptance
    ### BUG-01 (column-major duplicates): PASS / FAIL + observations
    ### BUG-02 (multi-plate preservation): PASS / FAIL + observations
    ### BUG-03 (species visible): PASS / FAIL (A, B, C)
    ### UI-01 (panel scoping + stale singles clear): PASS / FAIL + observations

    ## Overall: PASS / FAIL
    ```

    If ALL PASS → Phase 4.1 is complete. Proceed to Task 3.
    If ANY FAIL → stop. Do NOT run Task 3. Open a gap-closure phase (e.g., 4.2) via `/gsd-plan-phase 4.1 --gaps` or a fresh hotfix plan targeting the specific failures.
  </how-to-verify>
  <verify>
    <automated>MISSING — Windows physical-hardware testing is manual by CLAUDE.md §Testing Windows-only; no macOS-side automated verification is possible for this task.</automated>
  </verify>
  <done>
    - `.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md` exists with Part A (14-step) + Part B (4 fixes) results filled in
    - File contains an "## Overall: PASS" or "## Overall: FAIL" line
    - If PASS: Task 3 proceeds; if FAIL: Task 3 does NOT run and the orchestrator routes to gap closure
  </done>
  <resume-signal>
    Type "retest passed" (and attach or paste the 04.1-SMOKE-TEST-RETEST.md content)
    OR "retest failed: <summary>" (so the orchestrator can route to gap closure)
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record retest outcome + update ROADMAP/STATE</name>
  <files>
    .planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md (the tester's notes from Task 2)
    - .planning/ROADMAP.md §Phase 04.1 (lines ~176-189)
    - .planning/STATE.md (progress/milestone/pending todos)
  </read_first>
  <behavior>
    - If Task 2 passed: ROADMAP phase 4.1 status updates from "Not planned" / "In Progress" to "Complete"; all 5 plan checkboxes get ticked; overall-progress table row updates; STATE.md progress table updates.
    - If Task 2 failed: this task does NOT run (orchestrator routes to gap closure). Nothing to do here.
    - STATE.md `last_activity` + `stopped_at` updated to reflect 4.1 completion + 0.6.0 build.
    - STATE.md Accumulated Context → Decisions gains a 4.1 entry summarizing the 4 fixes.
    - HUMAN-UAT-04-03-01 pending todo in STATE.md is resolved (superseded by the 4.1 retest) — remove or mark complete.
  </behavior>
  <action>
1. Confirm `.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md` exists and contains a "## Overall: PASS" line. If not present or if FAIL: STOP and report; this task is only for the pass path.

2. Update `.planning/ROADMAP.md`:
   - Phase 04.1 section (around line 176): change the roadmap-summary line `| 4.1. Smoke Test Fixes (INSERTED) | 0/? | Not planned | - |` (line ~207) to `| 4.1. Smoke Test Fixes (INSERTED) | 5/5 | Complete | YYYY-MM-DD |` (today's date).
   - Phase 4.1 status icon: change `- [ ] **Phase 04.1...**` to `- [x] **Phase 04.1...**` if present above in the phase list, OR change the "Not planned" wording inside §Phase Details.
   - Update the Plans list in §Phase 04.1 (lines ~188-189) from `- [ ] TBD (run /gsd-plan-phase 04.1 to break down)` to the 5 real plan entries, each with a ticked `- [x]` and a one-line objective, e.g.:
     ```
     - [x] 04.1-01-PLAN.md -- BUG-01: column-major duplicate fill (Wave 1)
     - [x] 04.1-02-PLAN.md -- BUG-02: preserve plates across autoFill cascade (Wave 2)
     - [x] 04.1-03-PLAN.md -- BUG-03: Species in RunList + RunMetadataForm banner (Wave 1)
     - [x] 04.1-04-PLAN.md -- UI-01: scoped AnalyteGrid + clear stale singles on panel switch (Wave 1)
     - [x] 04.1-05-PLAN.md -- Version bump to 0.6.0 + build:win + Windows retest (Wave 3)
     ```
   - Update `**Plans**: TBD` to `**Plans**: 5 plans`.
   - Update the footer `*Last updated:*` line to today's date with a note "Phase 4.1 complete; v0.6.0 retested on Windows".

3. Update `.planning/STATE.md`:
   - Progress block: bump completed_plans from 24 to 29 (24 from Phase 4 + 5 from Phase 4.1), percent accordingly.
   - `last_updated`, `last_activity`, `stopped_at` set to today's date with "Completed Phase 4.1 smoke-test-fixes; v0.6.0 retested on Windows".
   - Velocity block: add "Phase 4.1 fully shipped; v0.6.0 Windows retest green".
   - By Phase table: add row `| 04.1-smoke-test-fixes | 5/5 | Complete | v0.6.0 (YYYY-MM-DD) |`.
   - Releases block: add `**v0.6.0** (YYYY-MM-DD): Phase 4 bundle + Phase 4.1 smoke-test fixes (BUG-01/02/03 + UI-01)`.
   - Pending Todos: REMOVE `HUMAN-UAT-04-03-01: Windows physical workstation smoke test` (superseded by 4.1 retest).
   - Decisions: APPEND a Phase 4.1 block (mirroring the style of the Phase 04 entries):
     - `04.1-01: Duplicate fill loop swapped to column-major (outer pair, inner row) — matches Hamilton pipetting order per D-4.1-01`
     - `04.1-02: autoFill preserves existing non-empty plates; overflow loop appends new plates; sampleCount=0 remains the explicit wipe path`
     - `04.1-03: Species added to RunList (7-column grid) and RunMetadataForm context banner; FinalizedRunHeader was already correct`
     - `04.1-04: selectPanel unconditionally clears selectedSingleIds; AnalyteGrid reduced from 7 props to 5 with scopedAnalytes + sectionLabel`
     - `04.1-05: Version bumped 0.5.0 → 0.6.0; electron-builder rebuilt both x64 and arm64 Windows installers; retest passed all 14 steps + 4 fix acceptances`

4. Do NOT tag the release or push to main. That's the job of `/release` once the full v1.0 is ready.

5. Write Plan 04.1-05 SUMMARY at `.planning/phases/04.1-smoke-test-fixes/04.1-05-SUMMARY.md` (standard template). Link to `04.1-SMOKE-TEST-RETEST.md`.
  </action>
  <acceptance_criteria>
    - grep -c "5/5 | Complete" .planning/ROADMAP.md returns at least 1 (phase 4.1 row in progress table)
    - grep -c "\\- \\[x\\] 04.1-01-PLAN.md" .planning/ROADMAP.md returns 1
    - grep -c "\\- \\[x\\] 04.1-05-PLAN.md" .planning/ROADMAP.md returns 1
    - grep -c "TBD (run /gsd-plan-phase 04.1 to break down)" .planning/ROADMAP.md returns 0 (placeholder removed)
    - grep -c "v0.6.0" .planning/STATE.md returns at least 2 (Releases table + By Phase row)
    - grep -c "HUMAN-UAT-04-03-01" .planning/STATE.md returns 0 (pending todo removed)
    - grep -c "04.1-01:" .planning/STATE.md returns 1 (new decision entry)
    - grep -c "04.1-05:" .planning/STATE.md returns 1
    - .planning/phases/04.1-smoke-test-fixes/04.1-05-SUMMARY.md exists and is non-empty
    - No git tag created for v0.6.0 (verify: `git tag -l "v0.6.0"` returns empty — release skill will tag later)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -c '5/5 | Complete' .planning/ROADMAP.md && grep -c 'v0.6.0' .planning/STATE.md"</automated>
  </verify>
  <done>
    - ROADMAP phase 4.1 marked Complete with all 5 plan checkboxes ticked
    - STATE.md progress/velocity/releases/decisions updated
    - HUMAN-UAT-04-03-01 pending todo resolved (superseded)
    - Plan 04.1-05 SUMMARY written
    - No git tag created (deferred to release workflow)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new code paths. This plan ships a rebuilt installer containing fixes from Wave 1+2. The installer signing / SmartScreen posture is unchanged from Plan 04-03 (deferred per D-26; Windows SmartScreen click-through accepted for v1 internal deployment).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-07 | Spoofing | Windows installer (unsigned) | accept | Per D-26 (Plan 04-03), code signing is deferred for v1 internal deployment. SmartScreen warning acknowledged. No change from v0.5.0 posture. |
| T-04.1-08 | Tampering | dist/*.exe artifact | accept | Local-build-only pipeline; no CI. Same trust model as v0.5.0. |

Zero HIGH findings. Phase 4.1 introduces no new attack surface; the rebuild uses the same electron-builder configuration as Plan 04-03.
</threat_model>

<verification>
- Task 1: package.json version = 0.6.0; typecheck green; build green; build:win produces both architectures; exe files exist under dist/
- Task 2: Windows workstation retest completes with PASS on all 14 steps + 4 fix acceptances; results recorded in 04.1-SMOKE-TEST-RETEST.md with "Overall: PASS"
- Task 3: ROADMAP phase 4.1 = Complete; STATE.md reflects v0.6.0 release + removal of HUMAN-UAT-04-03-01; Plan 04.1-05 SUMMARY exists
- No git tag v0.6.0 (tagging handled by `/release` when v1.0 ships)
</verification>

<success_criteria>
All four Phase 4.1 fixes verified on a fresh Windows .exe. 14-step smoke retest green. ROADMAP phase 4.1 Success Criterion #5 ("A fresh Windows .exe passes the original 14-step smoke test end-to-end") met. Phase 4.1 complete; v1.0 release unblocked.
</success_criteria>

<output>
After completion, `.planning/phases/04.1-smoke-test-fixes/04.1-05-SUMMARY.md` exists linking to 04.1-SMOKE-TEST-RETEST.md, and ROADMAP + STATE reflect Phase 4.1 complete with the 0.6.0 release entry.
</output>
