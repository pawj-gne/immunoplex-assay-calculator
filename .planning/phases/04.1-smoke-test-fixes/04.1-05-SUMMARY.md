---
phase: 04.1-smoke-test-fixes
plan: 05
subsystem: release
tags: [release, windows-installer, version-bump, human-uat-deferred]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: column-major duplicate fill in plateStore.autoFill
  - phase: 04.1-02
    provides: preserve existing plates + overflow in autoFill
  - phase: 04.1-03
    provides: Species in RunList row + RunMetadataForm banner
  - phase: 04.1-04
    provides: scoped AnalyteGrid + clear stale singles on panel switch
provides:
  - v0.6.0 Windows installers (x64 + arm64) built from macOS dev host
  - package.json version = 0.6.0 (was 0.5.0)
  - HUMAN-UAT-04.1-05-01 tracking entry for the deferred Windows smoke retest

# Tech tracking
task_count: 3
tasks_complete: 1
tasks_deferred: 2
status: partial
---

# Plan 04.1-05 — Windows Installer Rebuild + Smoke Retest (partial — Task 2/3 deferred)

## Status: PARTIAL

- **Task 1 (auto):** ✓ Complete — version bumped, typecheck + build + build:win all green, both architecture installers produced.
- **Task 2 (checkpoint:human-action):** DEFERRED — operator not available to test. Tracked as HUMAN-UAT-04.1-05-01.
- **Task 3 (auto):** BLOCKED — Task 3 is gated on Task 2 PASS (see plan action step 1: "If not present or if FAIL: STOP and report; this task is only for the pass path"). Will run after the retest returns PASS.

Phase 4.1 is **code-complete** on `dev/v1-01` but **not verified end-to-end** until the Windows smoke retest runs.

## Task 1 — Version bump + full build verification (complete)

**Commit:** `05e1989 chore(04.1-05): bump version to 0.6.0 for phase 4.1 bundle`

**Files modified:**
- `package.json`: `"version": "0.5.0"` → `"version": "0.6.0"` (line 3). No other edits.

**Build pipeline (run from macOS dev host):**
1. `npm run typecheck` → exit 0 (node + web tsconfigs)
2. `npm run build` → exit 0 (produced `out/main/index.js` 51.93 kB, `out/preload/index.js` 4.03 kB, `out/renderer/assets/index-*.js` 495.00 kB)
3. `npm run build:win` → exit 0 (electron-builder 25.1.8, electron 33.4.11, both x64 and arm64 via @electron/rebuild for better-sqlite3)

**Artifacts produced (`dist/`):**
| File | Size | Arch |
|------|------|------|
| immunoplex-assay-calculator-0.6.0-x64-setup.exe | 92,502,991 bytes | x64 |
| immunoplex-assay-calculator-0.6.0-arm64-setup.exe | 97,992,759 bytes | arm64 |
| immunoplex-assay-calculator-0.6.0-setup.exe | 189,991,703 bytes | universal (x64+arm64 bundle) |

Signing: none (per D-26; SmartScreen click-through accepted for v1 internal deployment).

**Acceptance criteria met:**
- [x] `grep -c '"version": "0.6.0"' package.json` = 1
- [x] `grep -c '"version": "0.5.0"' package.json` = 0
- [x] `npm run typecheck` exit 0
- [x] `npm run build` exit 0
- [x] `npm run build:win` exit 0
- [x] `dist/immunoplex-assay-calculator-0.6.0-x64-setup.exe` exists and is non-zero
- [x] `dist/immunoplex-assay-calculator-0.6.0-arm64-setup.exe` exists and is non-zero
- [x] No uncommitted source changes outside `package.json` and `dist/`

## Task 2 — HUMAN-UAT Windows smoke retest (DEFERRED)

**Tracking entry:** `HUMAN-UAT-04.1-05-01` (added to STATE.md §Pending Todos).

**Why deferred:** Operator is not available to run the physical-Windows retest at this time. The installer is built and ready; the retest will happen when the Windows lab workstation is accessible.

**What the retest must cover** (full procedure in `04.1-05-PLAN.md` lines 195-282):
1. **Part A — 14-step smoke test** from `.planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md` — regression net for Phase 4 behavior.
2. **Part B — 4.1 fix acceptance:**
   - BUG-01: column-major dup fill (A4 first; col 4 rows A–H get samples 1–8; verify counts 8/9/16/33/36).
   - BUG-02: plate-1 wells preserved when adding plate 2 / bumping sample count between 16 / 73 / 10.
   - BUG-03: Species=Mouse visible in RunMetadataForm banner, Finalized Run View, and RunList row.
   - UI-01: Panel I → Panel II switch clears singles and shows only target panel's analytes; "No Premix" shows single "Analytes" master list.
3. Results recorded in `.planning/phases/04.1-smoke-test-fixes/04.1-SMOKE-TEST-RETEST.md` with `## Overall: PASS` or `## Overall: FAIL`.

**Routing when results return:**
- PASS → resume Plan 04.1-05 Task 3 (update ROADMAP to mark 4.1 Complete, tick all 5 plan checkboxes, append Decisions entries to STATE.md, add v0.6.0 Releases row, remove HUMAN-UAT-04-03-01 which is superseded by this retest).
- FAIL → route to `/gsd-plan-phase 4.1 --gaps` to open a gap-closure plan targeting the specific failures; do NOT run Task 3.

## Task 3 — Record retest outcome + update ROADMAP/STATE (BLOCKED)

Cannot run until Task 2 returns `## Overall: PASS`. Plan step 1 explicitly gates on that string in `04.1-SMOKE-TEST-RETEST.md`.

## Phase 4.1 residual state

| Wave | Plan | Status | Evidence |
|------|------|--------|----------|
| 1 | 04.1-01 | ✓ Complete | commit `c13e5e1` + SUMMARY |
| 1 | 04.1-03 | ✓ Complete | commits `d84d741` + `7afffa9` + SUMMARY |
| 1 | 04.1-04 | ✓ Complete | commits `d44ff60` + `96edc24` + `6c4b126` + SUMMARY (merged via `0a586c9`) |
| 2 | 04.1-02 | ✓ Complete | commit `ec47f03` + SUMMARY |
| 3 | 04.1-05 | ◐ Partial — Task 1 done; Tasks 2+3 pending Windows retest | commit `05e1989` (version bump) + this SUMMARY |

**Typecheck across all 5 merged plans:** green (confirmed post-merge of Wave 1 and post-merge of Wave 2).

**Release posture:** v0.6.0 installers built but **not tagged** in git (per plan — tagging is `/release` workflow's job once v1.0 is ready). Do **not** promote these installers externally until the Windows retest completes.

## Next step (after operator returns)

Paste the contents of `04.1-SMOKE-TEST-RETEST.md` into the active session (or say "retest passed") and the orchestrator will execute Task 3 inline:
- tick remaining 4.1 checkboxes in `.planning/ROADMAP.md`
- update the Phase 04.1 progress row to `5/5 | Complete | YYYY-MM-DD`
- add a v0.6.0 row to STATE.md §Releases
- append the 5 Phase 4.1 Decisions entries to STATE.md
- remove HUMAN-UAT-04-03-01 from STATE.md §Pending Todos (superseded by this retest)
- backfill the "## Overall: PASS" auto-verification grep
