---
phase: 16
slug: windows-uat-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
  - .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md
  - .planning/phases/16-windows-uat-release/16-HAND-CALC.md
autonomous: true
requirements:
  - PHASE-16-SC-1
  - PHASE-16-SC-3
  - PHASE-16-SC-4
  - D-16-01
  - D-16-02
  - D-16-03
  - D-16-05
  - D-16-06
  - D-16-07
  - D-16-08
  - D-16-09
  - D-16-10
  - D-16-11
  - D-16-12
  - D-16-13
  - D-16-14
  - D-16-15a
  - D-16-15c
  - D-16-15d
  - D-16-19
  - D-16-20
  - D-16-21
user_setup: []

tags:
  - release
  - reconciliation
  - changelog
  - uat-script
  - hand-calc
  - documentation

must_haves:
  truths:
    - "STATE.md milestone field reconciled v2.0 -> v1.0 with milestone_name 'Release' per D-16-20"
    - "PROJECT.md §Current Milestone rewritten from 'v2.0 Panel XLSX Upload + Master-Panel Data Model' to v1.0 Release framing with §Next Milestone v1.1 (Phase 6 trigger: lab-confirmed v1.0 use) per D-16-20"
    - "ROADMAP.md Phase 16 entry: SC #1 says x64-only (drops arm64) per D-16-03; 'version TBD' line locked to v1.0.0 per D-16-01; Phase 6 reframed as 'v1.1: Network Layer & Central Server'; Phase 7 reframed as 'v1.2: Central Audit Trail' per D-16-19"
    - "REQUIREMENTS.md Active section reflects Smoke 3 SMK3-* requirements as v1.0 deliverables (not v2.0)"
    - "CHANGELOG.md exists at repo root in Keep-a-Changelog 1.1.0 format with §[Unreleased] empty placeholder + §[1.0.0] inaugural entry containing ### Added / Changed / Removed / Fixed sections in plain language (no SMK3-XX / D-XX codes in user-visible body per D-16-12); ### Removed section names 'v0.7.0 sub-panel CSV importer (superseded by Smoke 3 per-reagent schema)'"
    - "16-SMOKE-TEST-GUIDE.md exists with three sections: Section A (~15-step E2E flow using templates/panels/all-panels.xlsx fixture); Section B (per-requirement matrix with one row per SMK3-01..17 plus dedicated D-15-08 override-badge row plus optional D-15.1-keystroke row; SMK3-16 row pre-marked N/A per D-16-21); Section C (free-text observations)"
    - "16-HAND-CALC.md exists with PRD worked example (Thermofisher/Human/Panel 1, 2 plates x 100 samples -> 148 wells -> beads 7.4 mL, antibodies 3.7 mL) plus 2-3 additional panel hand-calcs covering the (a) premix-all-4-audit-blocks, (c) override-trigger, (d) SAPE-concentration-1 fixture-coverage items per D-16-15"
    - "Section B fixture-coverage audit identifies the actual all-panels.xlsx panel name satisfying each of D-16-15 (a), (c), (d) and pins those panels into Section A walkthrough"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "milestone label reconciled to v1.0; HUMAN-UAT-04.1-05-01 pending todo retained for Wave 3 closeout"
      contains: "milestone: v1.0"
    - path: ".planning/PROJECT.md"
      provides: "§Current Milestone updated to v1.0 Release frame"
      contains: "Current Milestone: v1.0 Release"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 SC#1 x64-only; v1.0.0 locked; Phase 6/7 reframed as v1.1/v1.2"
      contains: "v1.0.0"
    - path: ".planning/REQUIREMENTS.md"
      provides: "v2.1 Smoke 3 section retained but reframed under v1.0 deliverables"
      contains: "v1.0"
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog 1.1.0 inaugural entry for v1.0.0"
      contains: "## [1.0.0]"
    - path: ".planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md"
      provides: "Section A E2E + Section B matrix + Section C free-text UAT script"
      contains: "Section A"
    - path: ".planning/phases/16-windows-uat-release/16-HAND-CALC.md"
      provides: "Operator hand-calc reference for verifying Section A calc output without spreadsheet math"
      contains: "PRD Worked Example"
  key_links:
    - from: "ROADMAP Phase 16 SC list"
      to: "16-SMOKE-TEST-GUIDE.md Section A + B rows"
      via: "every Phase 16 SC must map to a Section A step or Section B row"
      pattern: "SMK3-"
    - from: "CHANGELOG §[1.0.0]"
      to: "REQUIREMENTS.md SMK3-01..17 user-visible features"
      via: "every SMK3-XX user-visible feature appears in plain-language form in CHANGELOG"
      pattern: "### Added"
    - from: "16-HAND-CALC.md PRD case"
      to: "16-SMOKE-TEST-GUIDE.md Section A Step 6 (calc verification)"
      via: "operator reads expected numbers from HAND-CALC while running Section A"
      pattern: "7.4 mL"
---

<objective>
Open Wave 1 of Phase 16. Reconcile every planning-doc reference to the stale "v2.0" milestone label to the v1.x release arc (v1.0 active, v1.1/v1.2 staged), then author the three operator-facing UAT artifacts (CHANGELOG.md, 16-SMOKE-TEST-GUIDE.md, 16-HAND-CALC.md) consumed by Wave 2 build + UAT. No source code edits in this plan — every task is a docs/config edit on the planning side or a new doc file under the phase directory or repo root.

Per D-16-20: STATE.md / PROJECT.md / ROADMAP.md / REQUIREMENTS.md all carry the stale "v2.0 Panel XLSX Upload + Master-Panel Data Model" milestone framing from pre-Smoke-3 planning. Phase 16 Wave 1 rewrites these to the current reality: v1.0.0 release is the Smoke 3 calculator on Windows lab PC; v2.0 milestone label is reframed (v1.x post-v1.0 release arc for Phase 6/7); v1.1 = Phase 6 (hosting, trigger: lab-confirmed real-world use); v1.2 = Phase 7 (central audit, depends_on v1.1).

Per D-16-10/11/12/13: CHANGELOG.md does not exist in repo. Wave 1 creates it at repo root in Keep-a-Changelog 1.1.0 format with v1.0.0 as inaugural entry. Audience = lab operator + future maintainer; plain language, no SMK3-XX / D-XX codes in user-visible body. Wave 3 will reuse the §[1.0.0] section verbatim as the GitHub Release body.

Per D-16-05/06/07/08/09/14/15/21: 16-SMOKE-TEST-GUIDE.md = hybrid format (Section A E2E ~15 steps using templates/panels/all-panels.xlsx as fixture; Section B per-requirement matrix one row per SMK3-01..17 plus dedicated D-15-08 override-badge row plus optional D-15.1-keystroke row; Section C free-text). SMK3-16 row pre-marked N/A per D-16-21 (skip seed-legacy-run.ts entirely — code-verified Phase 15 Group H, not user-observable without DB surgery). Pass criterion: every Section A step PASS AND every Section B row PASS or explicit N/A. A single Fail blocks v1.0.0 tag (triggers Phase 16.1).

Per D-16-15 (a/c/d only — b dropped per D-16-21): Fixture coverage audit — verify templates/panels/all-panels.xlsx contains (a) at least one premix panel exercising all 4 audit-trail blocks (Thermofisher-human-panel-i / Millipore-human-panel-1 / Bio-Rad-human-panel-1 per RESEARCH.md Finding #4); (c) at least one panel where Old Beads can credibly exceed 20% cap; (d) at least one panel whose SAPE row has concentration = 1. Section A walkthrough pins these specific panels.

Per D-16-07: 16-HAND-CALC.md = operator cheat sheet (PRD worked example + 2-3 additional panels' expected outputs) so the operator can verify Section A Step 6 calc output without spreadsheet math.

Wave 1 is fully parallel-safe (no source code conflicts with Wave 2/3) and runs autonomously. Each task ends with a Conventional Commit and `git push` per CLAUDE.md @.claude/commit.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-windows-uat-release/16-CONTEXT.md
@.planning/phases/16-windows-uat-release/16-RESEARCH.md
@.planning/phases/16-windows-uat-release/16-PATTERNS.md
@.planning/phases/16-windows-uat-release/16-VALIDATION.md
@.planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md
@.planning/SMOKE-3-PRD.md
@.planning/INGEST-RESOLUTIONS.md
@templates/panels/all-panels.xlsx
@CLAUDE.md
@.claude/commands/commit.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from in-repo state. -->

From .planning/STATE.md frontmatter (current state):
```yaml
milestone: v2.0
milestone_name: Release
status: ready_to_plan
```

Target after this plan's reconciliation:
```yaml
milestone: v1.0
milestone_name: Release
status: in_progress       # Phase 16 is active during execution
```

From .planning/PROJECT.md §Current Milestone (current text, lines ~41-53):
"## Current Milestone: v2.0 Panel XLSX Upload + Master-Panel Data Model
**Goal:** Replace the flat-CSV panel importer with a vendor-native multi-tab xlsx format ..."

Target after this plan: rewrite as "v1.0 Release — Smoke 3 calculator on Windows lab PC" with §Next Milestone "v1.1 (hosting/multi-machine), triggered by lab-confirmed v1.0 real-world use".

From .planning/ROADMAP.md Phase 16 entry (line 423-433):
- Line 428: SC #1 "`npm run build:win` produces x64 + arm64 installers" -> rewrite x64-only per D-16-03
- Line 432: SC #5 "Version tag chosen at gate (v0.8.0 for incremental, v2.0.0 for milestone — decision deferred from the 2026-05-11 ingest)" -> rewrite "v1.0.0 tagged after Windows UAT PASS per D-16-01"
- Line 37: Phase 16 summary "(version TBD: v0.8.0 vs v2.0.0 decided at gate)" -> rewrite "(v1.0.0)"

From .planning/REQUIREMENTS.md (current state):
- §v2.0 Requirements + §v2.1 Smoke 3 PRD Adoption — retain content but reframe headers under v1.0 deliverable umbrella.

From CHANGELOG keep-a-changelog 1.1.0 (https://keepachangelog.com/en/1.1.0/):
- Sections in order: Added / Changed / Deprecated / Removed / Fixed / Security
- Only categories with content are rendered (v1.0.0 entry: Added / Changed / Removed / Fixed — no Deprecated, no Security)
- Date format YYYY-MM-DD; today is 2026-05-12

From 04-SMOKE-TEST-GUIDE.md (Section A E2E precedent — adapt verbatim style):
- Header narrative + "## What you need" block + numbered top-level sections + ✅ Pass / ❌ Report if markers + tabular metadata + trailing FAQ

From templates/panels/all-panels.xlsx (Phase 13-02 fixture):
- 17 sheets total = 16 panels + 1 Table master sheet
- Per RESEARCH.md Finding #4: premix panels with full Beads/Antibodies/SAPE audit blocks = Thermofisher-human-panel-i, Millipore-human-panel-1, Bio-Rad-human-panel-1

From SMOKE-3-PRD.md §Calculation §Volume/well:
- Worked example: Thermofisher / Human / Panel 1, 2 plates × 100 samples → 148 wells × 0.05 mL beads = 7.4 mL; × 0.025 mL antibodies = 3.7 mL
- Round all calculated values to 0.1 mL (ceiling)
- Dead volume = setups × 2 mL
- PE volume = Total Volume of the Assay ÷ SAPE concentration
</interfaces>

<release_notes>
Phase 15.1 just shipped (2026-05-13). The four Phase 15.1 HUMAN-UAT carryover items (D-15-08 override badge on reopen, cap modal not re-triggering on first keystroke, 6 audit-trail rows render real mL values, IPC-failure recovery) get folded into 16-SMOKE-TEST-GUIDE.md Section B per the objective's Phase 15.1 integration block. Per D-16-21, the IPC-failure-recovery item (SMK3-16) is N/A — code-verified in Phase 15 Group H, deferred to a future maintenance phase. Section B SMK3-16 row carries that verbatim N/A justification.

Per D-16-23 (Wave 3 scope): pre-tag cleanup (`.claude/worktrees/` to `.gitignore`, `git worktree prune`, bulk-delete merged worktree-agent-* branches) is OUT of scope for Wave 1 — handled in Plan 16-03.
</release_notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconciliation sweep — STATE/ROADMAP/PROJECT/REQUIREMENTS milestone label v2.0 -> v1.0 + Phase 6/7 reframing</name>
  <files>
    .planning/STATE.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/STATE.md (full file — lines 1-15 frontmatter has milestone field; lines 16-50 Current Position block)
    - .planning/PROJECT.md §Current Milestone (lines 41-53)
    - .planning/ROADMAP.md lines 1-12 (Overview); line 37 (Phase 16 summary line "version TBD"); lines 243-262 (Phase 6 entry); lines 263-274 (Phase 7 entry); lines 423-433 (Phase 16 details + SC #1 + SC #5)
    - .planning/REQUIREMENTS.md §v2.0 Requirements + §v2.1 Smoke 3 PRD Adoption section headers
    - .planning/phases/16-windows-uat-release/16-CONTEXT.md D-16-01/02/03/19/20 (reconciliation scope)
  </read_first>
  <behavior>
    - STATE.md frontmatter: `milestone: v2.0` -> `milestone: v1.0`; `status: ready_to_plan` -> `status: in_progress`; `last_activity` and `stopped_at` updated to today's date + "Phase 16 Wave 1 reconciliation sweep".
    - STATE.md §Current Position: `Milestone: v2.0 (Panel XLSX Upload + Master-Panel Data Model)` -> `Milestone: v1.0 (Smoke 3 Release)`. `Status: Ready to plan` -> `Status: Wave 1 in progress (reconciliation + UAT script + CHANGELOG)`.
    - STATE.md §Performance Metrics By Phase: add rows for `12-smoke-3-calculator-rules` through `15.1-phase-15-code-review-gap-closure` if any are missing; mark Phases 12-15.1 complete.
    - PROJECT.md §Current Milestone block: full rewrite from "v2.0 Panel XLSX Upload + Master-Panel Data Model" (multi-paragraph) to "v1.0 Release — Smoke 3 calculator on Windows lab PC" with: Goal sentence, Status (in UAT), Pending UAT bullet, Next Milestone §"v1.1 — Network Layer & Central Server (Phase 6), trigger: lab-confirmed v1.0 real-world use".
    - ROADMAP.md Overview (line 7): the sentence "v2.0 extends the platform with a vendor-native multi-tab xlsx panel importer..." rewritten to anchor on v1.x: "v1.0 ships the Smoke 3 calculator on Windows lab PC (Phases 12-15.1 + Phase 16 release gate). v1.1 (Phase 6 — hosting/multi-machine) ships after lab-confirmed v1.0 real-world use; v1.2 (Phase 7 — central audit) depends on v1.1. v2.0 is reserved for a future major-version scope, not Phase 6/7 territory."
    - ROADMAP.md line 37 (Phase 16 summary): "PLACEHOLDER ... (version TBD: v0.8.0 vs v2.0.0 decided at gate)" -> "After Phases 12-15.1 ship, build .exe, install on Windows workstation, walk through Smoke 3 features end-to-end against real panel data, tag **v1.0.0**".
    - ROADMAP.md §Phase 6 header (line 243): "### Phase 6: Network Layer & Central Server" -> "### Phase 6: Network Layer & Central Server (v1.1)"; add "**Release target:** v1.1.0 (trigger: lab-confirmed v1.0 real-world use, suggestion ≥1-2 weeks; NOT immediate after v1.0)"; Plan 06-05 line "Windows build v0.7.0" -> "Windows build v1.1.0".
    - ROADMAP.md §Phase 7 header (line 263): "### Phase 7: Audit Trail" -> "### Phase 7: Audit Trail (v1.2)"; add "**Release target:** v1.2.0 (depends on v1.1.0 Phase 6)".
    - ROADMAP.md §Phase 16 entry (lines 423-433): SC #1 "produces x64 + arm64 installers" -> "produces an **x64** Windows installer (arm64 deferred to backlog 999.x — D-16-03)". SC #5 "Version tag chosen at gate (v0.8.0 for incremental, v2.0.0 for milestone — decision deferred from the 2026-05-11 ingest)" -> "**v1.0.0** is tagged and a GitHub Release is published after Windows UAT PASS, with CHANGELOG.md §[1.0.0] as the release body (D-16-01/02/10/13)".
    - REQUIREMENTS.md §v2.0 Requirements header retained (this is the historical bucket — don't touch traceability); add a paragraph at the very top of §v2.0 stating "**Re-scoped 2026-05-12 (Phase 16 reconciliation, per D-16-20):** The 'v2.0' label here is a historical planning artifact. The Smoke 3 PRD adoption (SMK3-* requirements in §v2.1) is the **v1.0.0 release** content. Phase 6 (Network Layer) and Phase 7 (Audit Trail) are v1.1 and v1.2 minor releases respectively, not v2.0. v2.0 is reserved for a future major version with scope TBD."
    - No changes to any source code, schema, IPC channel, or test file.
  </behavior>
  <action>
1. **Edit `.planning/STATE.md`:**
   - Frontmatter:
     - Line 3: `milestone: v2.0` -> `milestone: v1.0`
     - Line 5: `status: ready_to_plan` -> `status: in_progress`
     - Line 6: `stopped_at: Phase 15 complete ...` -> `stopped_at: Phase 16 Wave 1 reconciliation in progress`
     - Line 7: `last_updated: "2026-05-13T01:58:05.244Z"` -> `last_updated: "<today's ISO timestamp>"`
     - Line 8: `last_activity: 2026-05-13 -- Phase 15.1 execution started` -> `last_activity: <today's date> -- Phase 16 Wave 1 reconciliation sweep`
   - §Current Position (lines 27-34):
     - `Milestone: v2.0 (Panel XLSX Upload + Master-Panel Data Model) — **scope shifted 2026-05-11 by Smoke 3 PRD adoption**` -> `Milestone: v1.0 (Smoke 3 Release) — reconciled 2026-05-12 from stale v2.0 label per D-16-20`
     - `Phase: 16`
     - `Plan: Not started` -> `Plan: 16-01 (Wave 1)`
     - `Status: Ready to plan` -> `Status: Wave 1 in progress`
     - `Last activity: 2026-05-13` -> `Last activity: <today's date>`
   - §Performance Metrics By Phase: append rows for any Phase 12-15.1 entries missing (Phase 12-15.1 are all complete; reference ROADMAP.md Progress table). Example:
     ```
     | Phase 13 | 6/6 | Complete | 2026-05-12 |
     | Phase 14 | 8/8 | Complete | 2026-05-12 |
     | Phase 15 | 5/5 | Complete | 2026-05-13 |
     | Phase 15.1 | 3/3 | Complete | 2026-05-13 |
     ```
   - DO NOT remove `HUMAN-UAT-04.1-05-01` from Pending Todos (Wave 3 closeout handles this per D-16-21 / Plan 16-03).
   - DO NOT change progress.total_plans / completed_plans numbers — Wave 3 closeout handles final tally.

2. **Edit `.planning/PROJECT.md`:**
   - Replace §Current Milestone block (lines 41-53) with:
     ```markdown
     ## Current Milestone: v1.0 Release — Smoke 3 calculator on Windows lab PC

     **Goal:** Ship the Smoke 3 PRD-compliant desktop calculator on the operator's Windows workstation. Tag v1.0.0 after a clean Windows UAT pass.

     **Status:** In UAT — Phase 16 (Windows UAT & Release). Phases 12-15.1 are merged and code-complete on `dev/v1-01` (439/439 tests; typecheck exit 0; 0 critical/warning code-review findings).

     **Pending v1.0.0 deliverables:**
     - `npm run build:win` produces `dist/immunoplex-assay-calculator-1.0.0-x64-setup.exe`
     - Operator runs `16-SMOKE-TEST-GUIDE.md` Section A/B/C on the Windows lab PC against `templates/panels/all-panels.xlsx`; outcome `## Overall: PASS`
     - `git tag -a v1.0.0` + `gh release create v1.0.0` with CHANGELOG.md §[1.0.0] as the release body

     ## Next Milestone: v1.1 — Network Layer & Central Server (Phase 6)

     **Trigger:** Lab-confirmed v1.0 real-world use (suggestion ≥1-2 weeks of actual assay work). NOT immediate after v1.0.

     **Goal preview:** One designated lab PC runs an HTTP server owning the central SQLite DB; other PCs route DB ops via HTTP; offline fallback via local SQLite + sync-back queue.

     v1.2 (Phase 7 — central audit trail) follows v1.1 with a hard dependency on the v1.1 server layer. v2.0 is reserved for a future major version with scope TBD (not Phase 6/7 territory per D-16-20).
     ```

3. **Edit `.planning/ROADMAP.md`:**
   - Overview paragraph (line 7): rewrite the "v2.0 extends ..." sentence to anchor on v1.x as described in `<behavior>` above. Keep the 2026-05-11 Smoke 3 PRD adoption sentence intact.
   - Phase 16 summary line (around line 37): change to `- [ ] **Phase 16: Windows UAT & Release** - INSERTED 2026-05-11 - After Phases 12-15.1 ship, build .exe, install on Windows workstation, walk through Smoke 3 features end-to-end against real panel data, tag **v1.0.0**.`
   - §Phase 6 header (line 243): add ` (v1.1)` suffix and "**Release target:** v1.1.0" line at top of body. Plan 06-05 line: change "Windows build v0.7.0" to "Windows build v1.1.0".
   - §Phase 7 header (line 263): add ` (v1.2)` suffix and "**Release target:** v1.2.0 (depends on v1.1.0 / Phase 6)" line at top of body.
   - §Phase 16 entry (lines 423-433):
     - Replace `PLACEHOLDER` framing in the goal line.
     - SC #1: rewrite to "`npm run build:win` produces an **x64** Windows installer at `dist/immunoplex-assay-calculator-1.0.0-x64-setup.exe` (arm64 deferred to backlog 999.x per D-16-03)".
     - SC #5: rewrite to "**v1.0.0** is tagged and a GitHub Release is published after Windows UAT PASS, with CHANGELOG.md §[1.0.0] as the release body".
   - Footer `*Last updated:*` line (around line 465): update to today's date with note "Phase 16 Wave 1 reconciliation — v2.0 label retired, v1.x release arc locked (Phase 6 = v1.1; Phase 7 = v1.2)".

4. **Edit `.planning/REQUIREMENTS.md`:**
   - At the very top of `## v2.0 Requirements — Panel XLSX Upload + Master-Panel Data Model` (line 45), insert this paragraph:
     ```markdown
     > **Re-scoped 2026-05-12 (Phase 16 reconciliation, per D-16-20):** The "v2.0" label here is a historical planning artifact. The Smoke 3 PRD adoption (SMK3-* requirements in §v2.1 below) is the **v1.0.0 release** content. Phase 6 (Network Layer & Central Server) and Phase 7 (Audit Trail) are **v1.1.0** and **v1.2.0** minor releases respectively — NOT v2.0. v2.0 is reserved for a future major version with scope TBD.
     ```
   - Update footer `*Last updated:*` line to today's date.

5. **DO NOT:**
   - Touch any source code, schema, IPC channel, test, electron-builder.yml, or package.json (Wave 2 owns those).
   - Remove `HUMAN-UAT-04.1-05-01` from STATE.md Pending Todos (Wave 3 / Plan 16-03 owns this).
   - Edit the `## Releases` block in STATE.md (Wave 3 appends v1.0.0).
   - Change `progress.total_plans` / `completed_plans` numerics (Wave 3 handles final tally).

6. **Commit and push:**
   ```bash
   git add .planning/STATE.md .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
   git commit -m "docs(16-01): reconcile v2.0 milestone label to v1.0 + reframe Phase 6/7 as v1.1/v1.2

   - STATE.md milestone v2.0 -> v1.0; status in_progress; Phase 16 Wave 1 active
   - PROJECT.md Current Milestone rewritten as v1.0 Release (Smoke 3 on Windows lab PC); Next Milestone = v1.1 (Phase 6) triggered by lab-confirmed v1.0 use
   - ROADMAP.md Overview anchored on v1.x; Phase 16 SC#1 x64-only (drops arm64 per D-16-03); SC#5 locked to v1.0.0 (drops 'v0.8.0 vs v2.0.0' TBD per D-16-01); Phase 6 = v1.1.0; Phase 7 = v1.2.0
   - REQUIREMENTS.md v2.0 section header annotated with v1.x re-scope note per D-16-20

   Per D-16-19/20 (16-CONTEXT.md). No source code, schema, or test changes."
   git push origin HEAD
   ```
  </action>
  <acceptance_criteria>
    - `grep -c "milestone: v1.0" .planning/STATE.md` returns >= 1
    - `grep -c "milestone: v2.0" .planning/STATE.md` returns 0
    - `grep -c "Current Milestone: v1.0 Release" .planning/PROJECT.md` returns 1
    - `grep -c "Next Milestone: v1.1" .planning/PROJECT.md` returns 1
    - `grep -c "v1.1.0" .planning/ROADMAP.md` returns >= 2 (Phase 6 header + Plan 06-05 line)
    - `grep -c "v1.2.0" .planning/ROADMAP.md` returns >= 1 (Phase 7 header)
    - `grep -c "tag \*\*v1.0.0\*\*" .planning/ROADMAP.md` returns >= 1 (Phase 16 summary line OR SC#5)
    - `grep -c "v0.8.0 vs v2.0.0" .planning/ROADMAP.md` returns 0 (placeholder removed)
    - `grep -c "x64 + arm64" .planning/ROADMAP.md` returns 0 in the Phase 16 section (Pitfall: Phase 11 SC#1 still mentions x64+arm64 — leave that, it's a SUPERSEDED phase)
    - `grep -c "Re-scoped 2026-05-12 (Phase 16 reconciliation, per D-16-20)" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "HUMAN-UAT-04.1-05-01" .planning/STATE.md` returns >= 1 (NOT removed — Wave 3 handles it)
    - No diffs outside the 4 files above (verify with `git diff --name-only HEAD~1 HEAD`)
    - `git log --oneline -1` shows `docs(16-01): reconcile v2.0 milestone label ...`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -c 'milestone: v1.0' .planning/STATE.md && grep -c 'Current Milestone: v1.0 Release' .planning/PROJECT.md && grep -c 'tag \\*\\*v1.0.0\\*\\*' .planning/ROADMAP.md && [ $(grep -c 'v0.8.0 vs v2.0.0' .planning/ROADMAP.md) -eq 0 ] && grep -c 'Re-scoped 2026-05-12' .planning/REQUIREMENTS.md"</automated>
  </verify>
  <done>
    - All four planning docs reconciled to v1.x release arc
    - No source code / schema / test touched
    - HUMAN-UAT-04.1-05-01 retained in STATE.md Pending Todos (Wave 3 will clear it)
    - One commit on dev/v1-01, pushed to origin
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CHANGELOG.md at repo root — Keep-a-Changelog 1.1.0 inaugural entry for v1.0.0</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §v2.1 Smoke 3 PRD Adoption (SMK3-01..17 user-visible features)
    - .planning/SMOKE-3-PRD.md §Calculation + §Hierarchy of UI interaction (plain-language description of what changed in v1.0)
    - .planning/STATE.md §Releases (lines 230-238 — historical v0.x releases NOT back-filled per D-16's "Claude's Discretion" recommendation)
    - .planning/phases/16-windows-uat-release/16-CONTEXT.md D-16-10/11/12/13 (CHANGELOG scope + audience + v0.7.0 sub-panel removal mention)
    - .planning/phases/16-windows-uat-release/16-RESEARCH.md Pattern 3 (lines 268-319 — full Keep-a-Changelog skeleton)
    - electron-builder.yml line 9 (confirms CHANGELOG.md already excluded from packaged installer — no edit needed there)
  </read_first>
  <behavior>
    - CHANGELOG.md created at repo root with:
      - Title "# Changelog" + intro paragraph citing keepachangelog.com/en/1.1.0/ and semver.org
      - `## [Unreleased]` empty placeholder section
      - `## [1.0.0] - 2026-05-12` section (use today's date)
      - Sections in order: `### Added` / `### Changed` / `### Removed` / `### Fixed` (no Deprecated or Security per D-16-11)
      - Footer link references for `[unreleased]` (compare URL) and `[1.0.0]` (tag URL) using `https://github.com/pawj-gne/immunoplex-assay-calculator` base
    - Audience: lab operator + future maintainer. Plain language. NO SMK3-XX or D-XX codes in user-visible body (per D-16-12).
    - `### Added` enumerates Smoke 3 features that are user-visible:
      - Per-reagent diluent rule (concentration-keyed; any 1× premix wins)
      - 0.1-mL ceiling rounding
      - Setups-scaled dead volume (`setups × 2 mL`)
      - Plate-page inputs: Old Beads, Old Antibodies, Number of Setups
      - Run document audit trail (full inputs + intermediates + outputs + diluent decision)
      - SAPE Name display on the run document
      - Sectioned-xlsx panel importer (Criteria / Values / Category blocks per sheet)
      - Wholesale-replace re-upload (re-upload a (Platform, Species, Panel) triple replaces existing data)
      - Roman → Arabic panel-name normalization on import
      - Override badge when Old Beads or Old Antibodies exceed the 20% cap
      - Historical-run advisory banner on runs saved under prior calculation rules
      - PE volume formula (Total Volume of Assay ÷ SAPE concentration)
      - master_panel_reagents schema (per-reagent rows for Beads / Antibodies / SAPE)
    - `### Changed` enumerates semantics changes from v0.6.x:
      - Volume rounding precision changed from "nearest mL" to "ceiling at 0.1 mL"
      - Dead volume formula changed from a fixed 2 mL constant to `setups × 2 mL`
      - Diluent selection rule replaced (was request-type-keyed; now concentration-keyed)
      - Duplicate plate layout fills adjacent rows within a column (was horizontal pairs + vertical column-12 pairs)
      - Bead region display now shows a single flat list (was grouped by panel)
    - `### Removed` enumerates:
      - Legacy CSV panel importer + templates (`templates/panel-template.csv`, `templates/sample-panel-import.csv`)
      - "Stock concentration: Xx" labels on platform selection screens
      - v0.7.0 sub-panel CSV importer (superseded by Smoke 3 per-reagent schema) — per D-16's plan-time addendum so the v0.7.0 -> v1.0.0 version gap is explained
    - `### Fixed` enumerates carryover Phase 4.1 fixes that are part of the v1.0 baseline:
      - Duplicate pair orientation now matches Hamilton column-major pipetting (BUG-01)
      - Adding a second plate no longer wipes plate 1 (BUG-02)
      - Species visible in run metadata header and RunList (BUG-03)
      - Analyte picker scopes to selected premix panel (UI-01)
  </behavior>
  <action>
1. **Create `CHANGELOG.md` at repo root** with the following content (verbatim, using today's date `2026-05-12`):

```markdown
# Changelog

All notable changes to the Immunoplex Assay Calculator are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [1.0.0] - 2026-05-12

First release on Windows lab hardware. Implements the Smoke 3 PRD calculator (lab-authored
specification for reagent volume calculations against vendor-native panel data).

### Added

- Per-reagent diluent rule for Beads and Antibodies: if any selected premix is 1×, that
  premix is the diluent. Otherwise, the per-reagent diluent from the panel's Values block
  is used.
- Volume rounding at 0.1 mL precision (ceiling): final reagent volumes round up to the
  nearest tenth of a millilitre instead of the nearest whole millilitre.
- Setups-scaled dead volume: dead volume = `number of setups × 2 mL`. A new
  **Number of Setups** input on the Plate page drives this calculation.
- **Old Beads** and **Old Antibodies** inputs on the Plate page: reusable reagent volume
  is subtracted from the new-reagent calculation and added to the total reagent volume.
- Audit Trail section on the finalized run document showing the full calculation work:
  inputs (plates, samples, replicate mode, selections, old reagents, setups) →
  intermediates (total wells, volume/well, total bead/antibody volume, dead volume) →
  outputs (new reagents, total volumes, PE volume) → diluent decision (which premix won,
  or which Values-table diluent applied per reagent).
- **SAPE Name** field displayed in the run document metadata header (traceability;
  no calculation impact).
- Override badge on the run document when Old Beads or Old Antibodies exceed the
  20% reuse cap, so reviewers can see that the operator confirmed an override.
- Advisory banner on runs saved before the Smoke 3 calculator rules shipped:
  reopening shows the persisted values; the new rules are not retroactively applied.
- Vendor-native panel-xlsx importer accepting the lab's sectioned format (Criteria,
  Values, Category blocks per sheet) with per-reagent rows for Beads, Antibodies, and
  SAPE.
- Wholesale-replace re-upload semantics: re-uploading the same (Platform, Species, Panel)
  triple replaces the existing panel's analytes, premixes, and metadata in a single
  transaction.
- Roman-to-Arabic panel name normalization at import time: `Panel I` is stored and
  displayed as `Panel 1`. The lab can author either form.
- PE volume formula: PE volume = `total volume of the assay ÷ SAPE concentration`,
  read from the SAPE row of the panel's Values block (typically 1×, so PE volume
  equals the total assay volume).

### Changed

- Final volumes now round up to the nearest **0.1 mL** (ceiling), replacing the previous
  rule of rounding up to the nearest whole mL.
- Dead volume is now `number of setups × 2 mL`, replacing the fixed 2 mL constant.
- Diluent selection is now concentration-keyed (any 1× premix wins, otherwise per-reagent
  Values-table diluent applies), replacing the prior request-type-keyed rule with
  Assay Buffer fallback.
- Duplicate plate layout now fills adjacent rows within the same column
  (`(A4,B4) (C4,D4) (E4,F4) (G4,H4)`, then column 5, …, through column 12), replacing
  the prior horizontal-pair + column-12 vertical-pair layout.
- Bead region display is now a single flat list of every analyte in the current selection,
  sorted by bead region, replacing the prior grouping by panel membership.

### Removed

- Legacy CSV panel importer and its templates (`templates/panel-template.csv`,
  `templates/sample-panel-import.csv`). The vendor-native xlsx importer fully
  replaces this workflow.
- "Stock concentration: Xx" labels from platform-selection screens. The
  "Platform Selected: {name}" confirmation is retained.
- v0.7.0 sub-panel CSV importer (an interim importer shape that never reached lab use)
  is superseded by the Smoke 3 per-reagent schema and is not present in v1.0.0. This
  explains the version jump from v0.7.0 to v1.0.0 — v0.7.0 was a dev build only.

### Fixed

- Duplicate pair orientation now matches how a Hamilton robot pipettes (column-major).
- Adding a second plate no longer erases plate 1's layout.
- Species selection is now visible in the run metadata header and in the run list.
- Selecting a premix panel scopes the analyte picker to that panel's analytes only;
  switching between Panel I and Panel II clears stale single-analyte selections.

[unreleased]: https://github.com/pawj-gne/immunoplex-assay-calculator/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/pawj-gne/immunoplex-assay-calculator/releases/tag/v1.0.0
```

2. **Verify `electron-builder.yml` line 9 already excludes `CHANGELOG.md` from the packaged installer:**
   ```bash
   grep -c "CHANGELOG.md" electron-builder.yml
   ```
   Expected: 1 (it's in the `'!{...,CHANGELOG.md,README.md}'` exclusion pattern). NO edit needed to electron-builder.yml in this task.

3. **Commit and push:**
   ```bash
   git add CHANGELOG.md
   git commit -m "docs(16-01): create CHANGELOG.md inaugural v1.0.0 entry

   Keep-a-Changelog 1.1.0 format. Plain-language Added / Changed / Removed / Fixed sections
   for the Smoke 3 calculator release. Per D-16-10/11/12/13 (16-CONTEXT.md): no SMK3-XX
   or D-XX codes in user-visible body; audience is lab operator + future maintainer;
   v0.x releases not back-filled (v1.0.0 is inaugural entry). The Removed section
   explains the v0.7.0 -> v1.0.0 version jump (v0.7.0 sub-panel importer was superseded
   by Smoke 3 per-reagent schema before lab use).

   GitHub Release body in Wave 3 reuses the §[1.0.0] section verbatim per D-16-13.
   electron-builder.yml already excludes CHANGELOG.md from the packaged installer
   (line 9, no edit needed)."
   git push origin HEAD
   ```
  </action>
  <acceptance_criteria>
    - `test -f CHANGELOG.md` exits 0
    - `grep -c "## \[1.0.0\] - 2026-05-12" CHANGELOG.md` returns 1
    - `grep -c "## \[Unreleased\]" CHANGELOG.md` returns 1
    - `grep -c "### Added" CHANGELOG.md` returns 1
    - `grep -c "### Changed" CHANGELOG.md` returns 1
    - `grep -c "### Removed" CHANGELOG.md` returns 1
    - `grep -c "### Fixed" CHANGELOG.md` returns 1
    - `grep -c "SMK3-" CHANGELOG.md` returns 0 (D-16-12 — no internal codes in user-visible body)
    - `grep -c "D-16-" CHANGELOG.md` returns 0 (D-16-12)
    - `grep -c "v0.7.0 sub-panel" CHANGELOG.md` returns 1 (Removed section explains the version gap)
    - `grep -c "keepachangelog.com/en/1.1.0" CHANGELOG.md` returns 1
    - `grep -c "Semantic Versioning" CHANGELOG.md` returns 1
    - `grep -c "0.1 mL" CHANGELOG.md` returns >= 1 (Added or Changed section mentions the rounding rule)
    - `grep -c "Hamilton" CHANGELOG.md` returns 1 (Fixed section)
    - `grep -c "CHANGELOG.md" electron-builder.yml` returns 1 (already excluded — confirm no edit needed)
    - `git log --oneline -1` shows `docs(16-01): create CHANGELOG.md inaugural v1.0.0 entry`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test -f CHANGELOG.md && grep -c '## \\[1.0.0\\] - 2026-05-12' CHANGELOG.md && [ $(grep -c 'SMK3-' CHANGELOG.md) -eq 0 ] && [ $(grep -c 'D-16-' CHANGELOG.md) -eq 0 ] && grep -c 'v0.7.0 sub-panel' CHANGELOG.md && grep -c 'CHANGELOG.md' electron-builder.yml"</automated>
  </verify>
  <done>
    - CHANGELOG.md exists at repo root in Keep-a-Changelog 1.1.0 format
    - v1.0.0 entry uses plain language (no SMK3 / D-XX codes)
    - v0.7.0 sub-panel removal called out in Removed section so version gap is explained
    - electron-builder.yml exclusion confirmed (no installer-side edit needed)
    - One commit, pushed
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 16-SMOKE-TEST-GUIDE.md (Section A E2E + Section B per-requirement matrix + Section C free-text) + 16-HAND-CALC.md (operator cheat sheet)</name>
  <files>
    .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md,
    .planning/phases/16-windows-uat-release/16-HAND-CALC.md
  </files>
  <read_first>
    - .planning/phases/04-run-documentation-persistence-deployment/04-SMOKE-TEST-GUIDE.md (full file — Section A E2E style: narrative header + "What you need" + numbered top-level sections + ✅ Pass / ❌ Report if + tabular metadata + FAQ)
    - .planning/REQUIREMENTS.md §v2.1 Smoke 3 (SMK3-01..17 — Section B row source)
    - .planning/SMOKE-3-PRD.md §Calculation §Volume/well (PRD worked example — 16-HAND-CALC source)
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-CONTEXT.md (full audit-trail rendering for the PRD case — operator visual reference)
    - .planning/phases/16-windows-uat-release/16-CONTEXT.md D-16-05/06/07/08/09/14/15/21 (UAT format + fixture coverage)
    - templates/panels/all-panels.xlsx (the 17-sheet fixture — operator imports as Section A Step 2)
    - Phase 13-02 SUMMARY for which panels are in the fixture (Thermofisher-human-panel-i, Millipore-human-panel-1, Bio-Rad-human-panel-1 per RESEARCH.md Finding #4)
  </read_first>
  <behavior>
    **16-SMOKE-TEST-GUIDE.md structure:**
    - Header: "# Phase 16 — Windows UAT (Smoke 3) — Smoke Test Guide" + a ~2-paragraph operator-friendly intro (mirrors 04-SMOKE-TEST-GUIDE.md style)
    - "## What you need" block: installer filename `immunoplex-assay-calculator-1.0.0-x64-setup.exe`, x64 Windows lab PC (any recent Intel/AMD Windows workstation — not arm), templates/panels/all-panels.xlsx (transferred alongside the installer), 16-HAND-CALC.md printout or on-screen, ~30 minutes
    - "## Section A — End-to-end run flow (~15 steps)" using SMK3-CONTEXT.md D-16-05 (a):
      1. Install fresh .exe (uninstall prior version first; SmartScreen click-through messaging copied from 04-SMOKE-TEST-GUIDE.md Step 1)
      2. First launch (confirm app footer / title bar shows `v1.0.0`)
      3. Open Manage page → Import Panel XLSX → select `templates/panels/all-panels.xlsx` → confirm summary banner reports 16 panels imported (Table sheet ignored per SMK3-10)
      4. Walk to Calculator: Platform = Thermofisher, Species = Human, Panel = Panel 1 (the PRD worked-example panel — covers D-16-15(a) all-4-audit-blocks)
      5. Analyte selection: pick the premix listed in the panel; record selected analyte count (TA)
      6. Calculations: enter sample count = 100 (per PRD case), plate count auto-fills to 2; replicate mode = Singles; Old Beads = 0; Old Antibodies = 0; Number of Setups = 1
      7. Verify calculator output against `16-HAND-CALC.md` row "PRD case 1": total wells 148, total bead volume **7.4 mL**, total antibody volume **3.7 mL**, dead volume 2.0 mL, total assay volume + PE volume per the hand-calc row
      8. Bump Number of Setups to 3 → verify dead volume becomes 6.0 mL and total assay volume increases per hand-calc row "PRD case 1 setups=3"
      9. **D-16-15(c) override-trigger panel** — switch to (Platform / Species / Panel TBD by planner from the fixture — see Task 3 sub-step a below) and enter Old Beads at a value > 20% of new-bead volume → confirm cap modal appears → click "Override" → confirm amber OVERRIDE badge appears in the calculator output
      10. **D-16-15(d) SAPE-concentration-1 panel** — switch to (Platform / Species / Panel TBD by planner from the fixture — see Task 3 sub-step a below); verify the calculator's PE Volume line equals the Total Volume of Assay (because SAPE concentration = 1)
      11. Document & Save: fill metadata form per a fixed table (request 16001, ad-hoc unchecked, today's date, Joven operator, Hamilton/positions all = 1, comments "smoke 3 UAT") → click Save → confirm navigation to Finalized Run View
      12. Finalized Run View: confirm Audit Trail section renders all 4 blocks (Inputs / Intermediates / Outputs / Diluent decision) with real "X.X mL" values (no em-dashes for the 6 derived rows — Phase 15.1 WR-06 fix)
      13. Confirm SAPE Name appears in the metadata header (per panel — e.g., "SAPE-10" or whatever the imported panel carries)
      14. Reopen the saved run from Past Runs → verify (a) override badge still visible (D-15-08 — Phase 15.1 WR-01 fix), (b) cap modal does NOT re-trigger on first keystroke in the Old Beads field (Phase 15.1 UAT-item #2), (c) all snapshot values match what was saved
      15. Print → cancel print dialog (just verify button works); Start New Run → confirm wizard clears but Past Runs list survives
    - "## Section B — Per-requirement acceptance matrix" — 18 rows minimum (SMK3-01..17 = 17 rows, plus dedicated `D-15-08 override badge` row, plus optional `D-15.1-keystroke` row). Each row: `| Requirement | Pass / Fail / N/A | Evidence (one-line) |`. SMK3-16 row pre-marked **N/A** per D-16-21 with verbatim justification: "N/A — historical-run banner is code-verified (Phase 15 Group H); not user-observable on the lab PC without DB surgery, deferred to a future maintenance phase". SMK3-11 row explicitly instructs operator to re-upload all-panels.xlsx twice and confirm no duplicate rows. SMK3-14 row explicitly instructs operator to verify duplicate plate layout col-pair fill (the Phase 14 BUG-01 carryover).
    - "## Section C — Free-text observations + bugs found" — empty section with a prompt: "Anything the script didn't anticipate? Note here. Include screenshots if a UI element is broken. Sections that took longer than expected, ambiguous wording in the script, etc."
    - Trailing "## Overall: PASS / FAIL" line — replaced by operator at retest time (Wave 2 produces 16-SMOKE-TEST-RETEST.md from this template; this file is the script, the RETEST.md is the operator's answers)
    - FAQ block at end (mirrors 04-SMOKE-TEST-GUIDE.md): SmartScreen explanation, where data lives (`%APPDATA%\immunoplex-assay-calculator\`), can data be deleted, how to uninstall

    **16-HAND-CALC.md structure:**
    - Header: "# Phase 16 — Operator Hand-Calc Reference (Smoke 3)" + 1-paragraph intro: "Use this sheet to verify calculator output during Section A of `16-SMOKE-TEST-GUIDE.md`. If the on-screen number matches the expected value here within 0.1 mL, the calculator is working. If not, mark Section A Step 6 (or the affected step) as FAIL in `16-SMOKE-TEST-RETEST.md`."
    - 3-4 worked example rows, each a self-contained markdown subsection:
      - **PRD case 1 — Thermofisher / Human / Panel 1, 100 samples × 2 plates, Singles, Setups=1, Old=0/0**
        - Total wells = (100 + 24×2) = 148
        - Bead volume = 148 × 0.05 mL = 7.40 mL → ceiling-rounded to 7.4 mL
        - Antibody volume = 148 × 0.025 mL = 3.70 mL → 3.7 mL
        - Dead volume = 1 × 2 mL = 2.0 mL
        - Total assay volume = 7.4 + 3.7 + (PE TBD) + dead = (see PE below)
        - PE volume = Total assay volume ÷ 1 (SAPE concentration assumed 1×) = identical to total assay volume → 13.5 mL (or per actual panel data — planner verifies via the fixture)
      - **PRD case 1 setups=3** — same inputs as case 1 except Number of Setups = 3
        - Dead volume = 3 × 2 mL = 6.0 mL
        - Total assay volume bumps by 4 mL → 17.5 mL (verify on screen)
      - **D-16-15(c) override-trigger panel** — planner-selected from fixture (premix panel with smaller per-well volume so 20% cap is easier to trip): record expected new-bead volume + 20% cap threshold so operator knows what value of Old Beads will trigger the override modal
      - **D-16-15(d) SAPE-concentration-1 panel** — planner-selected from fixture: confirm SAPE concentration = 1 in the panel's Values block; expected PE volume = total assay volume (no division step visible to operator)
    - Each row is a 5-7 line markdown block with a fixed-width table or bullet list. Operator scans the table during Section A.
  </behavior>
  <action>
1. **Sub-step a: Fixture coverage audit — pin actual panels for D-16-15 (a), (c), (d):**
   
   Inspect `templates/panels/all-panels.xlsx` using a one-shot tsx script or by opening in any spreadsheet viewer to identify the actual sheet names. Per RESEARCH.md Finding #4, candidates are `Thermofisher-human-panel-i`, `Millipore-human-panel-1`, `Bio-Rad-human-panel-1`. The executor may run:
   ```bash
   # Quick sheet-name enumeration via Node/xlsx (xlsx is already a project dep)
   node -e "const xlsx = require('xlsx'); const wb = xlsx.readFile('templates/panels/all-panels.xlsx'); console.log(wb.SheetNames);" 2>&1 | head -30
   ```
   For each fixture-coverage item:
   - **(a) all-4-audit-blocks panel:** Choose `Thermofisher-human-panel-i` (the PRD worked-example panel — guaranteed to have Beads + Antibodies + SAPE per-reagent rows + premix selection). If the actual sheet name differs, note the actual name in Section A Step 4.
   - **(c) override-trigger panel:** Choose any premix panel where the per-well volume × wells is small enough that Old Beads ≥ 20% is plausible (e.g., a 33-plex Milliplex Mouse Panel where total bead volume ~5 mL → Old Beads = 1.5 mL trips the cap at 30% reuse). Read the actual Volume/well from the Values block of the chosen sheet and document the threshold in 16-HAND-CALC.md.
   - **(d) SAPE-concentration-1 panel:** Read the Values block SAPE row of each fixture sheet; choose the first panel where SAPE concentration = 1 (most panels — typical case per PRD). If multiple, use the same panel as (a) to keep the operator on a familiar panel name.

   Record the chosen panel names + their key values in a small "Fixture Coverage" table inserted at the top of 16-HAND-CALC.md.

2. **Sub-step b: Write `.planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md`:**

   Structure (full content — keep operator-friendly tone from 04-SMOKE-TEST-GUIDE.md):

   ```markdown
   # Phase 16 — Windows UAT (Smoke 3) — Smoke Test Guide

   Thanks for helping verify v1.0! This is the first Windows install of the Smoke 3
   calculator on a real lab PC against real panel data. You'll walk through a full
   run end-to-end, then tick a per-requirement matrix.

   **Estimated time:** ~30 minutes.

   **If anything fails:** stop, note exactly what you saw in Section C (and screenshot
   any errors). A single FAIL blocks the v1.0.0 tag — but it also gives us a clean
   bug report for a quick fix loop. Don't try to work around issues.

   ---

   ## What you need

   - Installer: `immunoplex-assay-calculator-1.0.0-x64-setup.exe`
   - Panel fixture: `templates/panels/all-panels.xlsx` (transfer alongside the installer)
   - This guide on-screen + `16-HAND-CALC.md` on-screen (or printed)
   - An x64 Windows lab PC (any recent Intel/AMD Windows workstation — not ARM)
   - ~30 min

   ---

   ## Section A — End-to-end run flow

   ### Step 1. Install the app

   1. Uninstall any prior Immunoplex Assay Calculator version via Windows Settings → Apps.
   2. Double-click `immunoplex-assay-calculator-1.0.0-x64-setup.exe`.
   3. Windows SmartScreen will probably complain ("Windows protected your PC" /
      "unrecognized app"). Click **More info** → **Run anyway**. Expected — the installer
      isn't code-signed yet (deferred polish item).
   4. Click through with defaults. Desktop + Start Menu shortcuts appear.

   **✅ Pass:** Installer finishes, shortcuts appear.
   **❌ Report if:** Installer errors out, hangs, or shortcuts don't appear.

   ### Step 2. First launch — confirm v1.0.0

   1. Double-click the desktop shortcut.
   2. The app window appears within ~5 seconds.
   3. Confirm the title bar or app footer shows **v1.0.0** (NOT v0.7.0).

   **✅ Pass:** App opens, v1.0.0 visible.
   **❌ Report if:** Title shows wrong version, app crashes, UI is blank.

   ### Step 3. Import panel data

   1. Navigate to **Manage** → find the **Import Panel XLSX** button.
   2. Select `templates/panels/all-panels.xlsx`.
   3. After import, a banner reports how many panels were imported.

   **✅ Pass:** Banner reports **16 panels imported** (Table sheet is decorative and ignored).
   No error.
   **❌ Report if:** Banner reports a different count, any panel fails, or an error toast appears.

   ### Step 4. Pick platform / species / panel (PRD worked example)

   1. Navigate to the Calculator wizard, Step 1.
   2. **Platform:** Thermofisher
   3. **Species:** Human
   4. **Panel:** Panel 1 *(planner: confirm actual sheet name from the fixture; substitute if needed)*

   **✅ Pass:** Next button enables; SAPE Name field visible somewhere on the metadata
   header for this panel.

   ### Step 5. Pick analytes

   1. The premix for Panel 1 is shown — select it.
   2. Note the total analyte count (TA) in the sidebar.

   **✅ Pass:** Selection registers; bead region list shows a flat list (not grouped by panel).

   ### Step 6. Calculations — PRD case 1 (Singles, Setups=1)

   1. Enter **Sample count = 100**. Plate count should auto-fill to **2**.
   2. **Replicate mode = Singles**
   3. **Old Beads = 0**, **Old Antibodies = 0**, **Number of Setups = 1**
   4. Compare on-screen output to `16-HAND-CALC.md` § PRD case 1:
      - Total wells = **148**
      - Total bead volume = **7.4 mL**
      - Total antibody volume = **3.7 mL**
      - Dead volume = **2.0 mL**
      - PE volume = (per hand-calc — typically equals total assay volume)

   **✅ Pass:** Every value matches the hand-calc within 0.1 mL.
   **❌ Report if:** Any value drifts; note the on-screen value vs. expected.

   ### Step 7. Bump Setups to 3 — verify dead volume scales

   1. Change **Number of Setups** to **3**.
   2. Dead volume should become **6.0 mL** (3 × 2 mL).
   3. Total assay volume increases by 4 mL vs Step 6.

   **✅ Pass:** Dead volume and total scale linearly with setups.

   ### Step 8. Override badge (D-16-15(c) — override-trigger panel)

   1. *Planner: substitute the override-trigger panel name from `16-HAND-CALC.md` § (c) row.*
   2. Switch the wizard to the override-trigger panel; enter the Old Beads value from the
      hand-calc row (≥ 20% of the new-bead volume).
   3. A cap modal should appear — click **Override**.
   4. The calculator output should show an **amber OVERRIDE badge**.

   **✅ Pass:** Cap modal triggers; override badge visible after confirm.
   **❌ Report if:** Modal doesn't appear, or badge isn't visible.

   ### Step 9. SAPE concentration = 1 (D-16-15(d))

   1. *Planner: substitute the SAPE-conc-1 panel name from `16-HAND-CALC.md` § (d) row.*
   2. Switch the wizard to the SAPE-conc-1 panel; re-enter the inputs from Step 6.
   3. Confirm **PE Volume** on screen equals the **Total Volume of Assay** value (because
      SAPE concentration = 1, the division step is a no-op).

   **✅ Pass:** PE Volume = Total Assay Volume.

   ### Step 10. Document & Save — metadata form

   Fill the run metadata with these exact values:

   | Field | Value |
   |---|---|
   | Request Number | `16001` |
   | "Ad-hoc run (no request number)" | leave **unchecked** |
   | User | your name |
   | Operator | **Joven** |
   | Date | today |
   | Sample Type | **Supernatant** |
   | Dilution Factor | `5` |
   | Hamilton | `1` |
   | Run Plate Position | `1` |
   | Standard Position | `1` |
   | Trough Position | `1` |
   | Comments | `smoke 3 UAT` |

   Click **Save**.

   **✅ Pass:** No error; auto-navigates to Finalized Run View.

   ### Step 11. Audit Trail rendering — 4 blocks, real mL values

   On the Finalized Run View, scroll to the **Audit Trail** section. It should render
   **four labeled blocks**:

   1. **Inputs:** plates, samples, replicate mode, selections, old beads, old antibodies, setups
   2. **Intermediates:** total wells, volume/well per reagent, total bead/antibody volume, dead volume
   3. **Outputs:** new beads, new antibodies, total bead volume, total antibody volume, PE volume — each with a real "X.X mL" value (NOT an em-dash)
   4. **Diluent decision:** which premix won (1× diluent rule), or which Values-table diluent applied per reagent

   **✅ Pass:** All 4 blocks render; the 6 rows that used to be em-dashes now show real
   "X.X mL" values (Phase 15.1 WR-06 fix).
   **❌ Report if:** Any row shows `—` (em-dash) where a number is expected; any block missing.

   ### Step 12. SAPE Name visible

   On the same Finalized Run View, find the metadata header (top of the run document).
   The **SAPE Name** field should be visible — for the PRD panel this should read
   the SAPE name from the panel's Values block (e.g., `SAPE-10`).

   **✅ Pass:** SAPE Name field rendered with a non-empty value.

   ### Step 13. Reopen run — override badge persists + cap modal does not re-trigger

   1. Click **Back** → confirm "edit anyway" through the dirty-state modal → return to
      Past Runs.
   2. Click **Load** on Request 16001.
   3. Walk to the Calculator step:
      - **✅ Pass (D-15-08):** The amber OVERRIDE badge is still visible (Phase 15.1
        WR-01 fix restored the override flags on reopen).
      - **✅ Pass (D-15.1-keystroke):** Click into the Old Beads field — the cap modal
        does NOT re-trigger on the first keystroke (Phase 15.1 UAT-item #2 fix).
   4. Verify the snapshot values (sample count, total bead volume, PE volume) match what
      you saw before saving.

   **❌ Report if:** Override badge missing on reopen; cap modal re-triggers immediately;
   any field reverts to default.

   ### Step 14. Print + Start New Run

   1. Click **Print** → Windows print dialog opens → cancel it. (Just verify the button works.)
   2. Click **Start New Run**:
      - Wizard returns to Step 1, all selections cleared.
      - **Operators** dropdown still populated; **Past Runs** list still shows Request 16001.

   **✅ Pass:** Wizard clears; app-wide data survives.

   ### Step 15. Close + reopen (persistence check)

   1. Close the app (Alt+F4 or X).
   2. Reopen from the desktop shortcut.
   3. Navigate to Past Runs → Request 16001 is still there → click Load → all values
      rehydrate.

   **✅ Pass:** Run survives app restart.

   ---

   ## Section B — Per-requirement acceptance matrix

   For each row, tick **Pass / Fail / N/A** in `16-SMOKE-TEST-RETEST.md` with a one-line
   evidence note. Any **Fail** blocks the v1.0.0 tag.

   | Requirement | Pass / Fail / N/A | Evidence (one-line) |
   |---|---|---|
   | SMK3-01 — Stock-concentration labels removed from platform-selection screens | | |
   | SMK3-02 — Plate page exposes **Old Beads** input | | |
   | SMK3-03 — Plate page exposes **Old Antibodies** input | | |
   | SMK3-04 — Plate page exposes **Number of Setups** input (default 1, min 1) | | |
   | SMK3-05 — Dead volume = setups × 2 mL (verified at Section A Step 7) | | |
   | SMK3-06 — Final volumes round UP to nearest 0.1 mL (verified at Section A Step 6) | | |
   | SMK3-07 — Concentration-keyed diluent rule (1× premix wins; else per-reagent Values-table) | | |
   | SMK3-08 — Sectioned-xlsx panel importer (Criteria / Values / Category) accepts `all-panels.xlsx` | | |
   | SMK3-09 — Roman → Arabic panel-name normalization on import | | |
   | SMK3-10 — Master `Table` tab ignored on import (16 panels imported, not 17) | | |
   | SMK3-11 — Wholesale-replace re-upload — re-import `all-panels.xlsx` twice → zero duplicate rows | | |
   | SMK3-12 — SAPE Name visible in run document metadata header (verified at Section A Step 12) | | |
   | SMK3-13 — Premix deselection re-enables members as singles (not auto-added) | | |
   | SMK3-14 — Bead region display = single flat list sorted by bead region + duplicate plate col-pair fill correct | | |
   | SMK3-15 — Audit Trail 4-block layout with per-reagent breakdown (verified at Section A Step 11) | | |
   | SMK3-16 — Historical-run snapshot freeze | **N/A** | N/A — historical-run banner is code-verified (Phase 15 Group H); not user-observable on the lab PC without DB surgery, deferred to a future maintenance phase |
   | SMK3-17 — PE volume = Total Volume of Assay ÷ SAPE concentration (verified at Section A Step 9) | | |
   | D-15-08 — Override badge visible on reopen + cap modal does NOT re-trigger on first keystroke (verified at Section A Step 13) | | |

   ---

   ## Section C — Free-text observations + bugs found

   *Anything the script didn't anticipate. Include screenshots if a UI element is broken.
   Sections that took longer than expected, ambiguous wording in the script, etc.*

   (operator fills in)

   ---

   ## Overall: PASS / FAIL

   *(operator picks one when done)*

   ---

   ## FAQ

   **Q: SmartScreen warned me — is this safe?**
   Yes. The installer isn't code-signed yet (we'll sign before public release). Same build
   pipeline as v0.5–v0.7.

   **Q: Where does my data live?**
   `%APPDATA%\immunoplex-assay-calculator\immunoplex.db` (SQLite). Survives uninstall.

   **Q: Can I delete the test run afterward?**
   Yes — use the Delete button on Request 16001 in Past Runs. Honestly I'd rather leave
   it so I can inspect on the next release if needed.

   **Q: Can I uninstall afterward?**
   Yes, Windows Settings → Apps. The DB in `%APPDATA%` stays behind; delete that folder
   manually for a clean removal.

   Thanks!
   ```

   *(Use the actual fixture sheet names from sub-step a where the template above says
   "planner: substitute".)*

3. **Sub-step c: Write `.planning/phases/16-windows-uat-release/16-HAND-CALC.md`:**

   Structure:

   ```markdown
   # Phase 16 — Operator Hand-Calc Reference (Smoke 3)

   Use this sheet to verify calculator output during Section A of `16-SMOKE-TEST-GUIDE.md`.
   If the on-screen number matches the expected value here within 0.1 mL, the calculator
   is working. If not, mark the affected Section A step as FAIL in
   `16-SMOKE-TEST-RETEST.md` and note the on-screen value vs. expected.

   All values are ceiling-rounded to 0.1 mL (the Smoke 3 rule).

   ## Fixture Coverage Map

   | D-16-15 item | Panel name (in `all-panels.xlsx`) | Why this panel |
   |---|---|---|
   | (a) all-4-audit-blocks premix panel | *<planner: actual sheet name from fixture audit>* | PRD worked-example panel; covers Beads + Antibodies + SAPE per-reagent rows + premix selection |
   | (c) override-trigger panel | *<planner: actual sheet name>* | Per-well volume is small enough that Old Beads ≥ 20% trips the cap |
   | (d) SAPE-concentration-1 panel | *<planner: actual sheet name>* | SAPE row in Values block has concentration = 1 → PE volume equals total assay volume |

   ---

   ## PRD case 1 — Thermofisher / Human / Panel 1, 100 samples × 2 plates, Singles, Setups=1, Old=0/0

   **Inputs:**
   - Samples = 100, plates = 2, replicate = Singles
   - Old Beads = 0, Old Antibodies = 0, Setups = 1
   - Volume/well (Beads) = 0.05 mL (from panel Values block)
   - Volume/well (Antibodies) = 0.025 mL

   **Calculation:**
   - Total wells = 100 + (24 × 2) = **148**
   - Bead volume = 148 × 0.05 = 7.40 mL → **7.4 mL** (ceiling at 0.1)
   - Antibody volume = 148 × 0.025 = 3.70 mL → **3.7 mL**
   - Dead volume = 1 × 2 = **2.0 mL**
   - Total assay volume (per PRD §Calculation) = sum of new reagent volumes + dead volume
   - PE volume = total assay volume ÷ SAPE conc (assume 1×) = **equals total assay volume**

   *(planner: if the fixture's actual Volume/well values differ from 0.05 / 0.025,
   substitute the actual values and recompute the expected mL totals before committing.)*

   ## PRD case 1 — Setups = 3

   Same inputs as PRD case 1 except Number of Setups = 3.

   - Dead volume = 3 × 2 = **6.0 mL** (was 2.0 mL with setups=1)
   - Total assay volume bumps by 4.0 mL vs setups=1
   - Bead and Antibody volumes are unchanged (volume/well × wells is independent of setups)
   - PE volume rescales because total assay volume changed

   ## D-16-15(c) — override-trigger panel

   *<planner: substitute actual values from the chosen panel>*

   **Inputs:**
   - Samples = 100, plates = 2, replicate = Singles, Setups = 1
   - Volume/well (Beads) = *<from panel>* mL

   **Threshold:**
   - New bead volume (before old-bead subtraction) = 148 × *<volume/well>* mL
   - 20% reuse cap = 20% × new bead volume = *<computed>* mL
   - Enter Old Beads = *<value ≥ 20% cap>* mL → cap modal triggers → click Override →
     amber badge appears in calculator output.

   ## D-16-15(d) — SAPE concentration = 1 panel

   *<planner: substitute actual values from the chosen panel>*

   **Inputs:**
   - Samples = 100, plates = 2, replicate = Singles, Setups = 1

   **PE volume check:**
   - SAPE concentration in panel Values block = **1**
   - PE Volume on screen = Total Volume of Assay (no division step visible).
   - Operator confirms PE Volume row equals Total Volume of Assay row.
   ```

4. **Commit and push** (single commit for both files since they're co-authored):
   ```bash
   git add .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md \
           .planning/phases/16-windows-uat-release/16-HAND-CALC.md
   git commit -m "docs(16-01): author 16-SMOKE-TEST-GUIDE.md (Section A/B/C) + 16-HAND-CALC.md

   Hybrid UAT script per D-16-05:
   - Section A: 15-step E2E flow using templates/panels/all-panels.xlsx as the canonical
     fixture (D-16-06). PRD worked example (Thermofisher/Human/Panel 1, 100 samples × 2 plates,
     setups=1 -> 7.4 mL beads / 3.7 mL antibodies) drives Step 6 calc verification.
   - Section B: 18-row per-requirement matrix (SMK3-01..17 + dedicated D-15-08 override-badge
     row). SMK3-16 row pre-marked N/A per D-16-21 with verbatim justification (historical-run
     banner code-verified Phase 15 Group H; not user-observable without DB surgery).
   - Section C: free-text observations block.
   - Trailing 'Overall: PASS / FAIL' line is the Wave 2 retest gate per D-16-08/09.

   16-HAND-CALC.md provides operator cheat sheet (PRD case + setups=3 variant + D-16-15(c)
   override-trigger panel + D-16-15(d) SAPE-conc-1 panel) per D-16-07/15. Fixture coverage
   audit pins the actual panel names from all-panels.xlsx.

   Phase 15.1 HUMAN-UAT carryover items folded into Section A: Step 11 (audit-trail 6 real
   mL values — WR-06), Step 13 (override badge persists on reopen — WR-01 + cap-modal-no-re-
   trigger — Phase 15.1 UAT item #2). Phase 15.1 item #4 (IPC-failure recovery) routed to
   Section B SMK3-16 N/A per D-16-21."
   git push origin HEAD
   ```
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` exits 0
    - `test -f .planning/phases/16-windows-uat-release/16-HAND-CALC.md` exits 0
    - `grep -c "Section A — End-to-end run flow" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1
    - `grep -c "Section B — Per-requirement acceptance matrix" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1
    - `grep -c "Section C — Free-text observations" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1
    - `grep -c "Overall: PASS / FAIL" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1
    - `grep -cE "SMK3-(0[1-9]|1[0-7])" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns >= 17 (each SMK3-01..17 row appears at least once)
    - `grep -c "D-15-08" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns >= 1 (dedicated row per D-16-14)
    - `grep -c "N/A — historical-run banner is code-verified" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1 (SMK3-16 verbatim N/A per D-16-21)
    - `grep -c "immunoplex-assay-calculator-1.0.0-x64-setup.exe" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns >= 1
    - `grep -c "templates/panels/all-panels.xlsx" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns >= 1
    - `grep -c "16 panels imported" .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md` returns 1
    - `grep -c "PRD case 1" .planning/phases/16-windows-uat-release/16-HAND-CALC.md` returns >= 1
    - `grep -c "Fixture Coverage Map" .planning/phases/16-windows-uat-release/16-HAND-CALC.md` returns 1
    - `grep -c "7.4 mL" .planning/phases/16-windows-uat-release/16-HAND-CALC.md` returns >= 1 (PRD bead volume locked)
    - `grep -c "3.7 mL" .planning/phases/16-windows-uat-release/16-HAND-CALC.md` returns >= 1 (PRD antibody volume locked)
    - `git log --oneline -1` shows `docs(16-01): author 16-SMOKE-TEST-GUIDE.md ...`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test -f .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md && test -f .planning/phases/16-windows-uat-release/16-HAND-CALC.md && grep -c 'Section A — End-to-end run flow' .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md && grep -cE 'SMK3-(0[1-9]|1[0-7])' .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md | awk '{ if (\$1 >= 17) exit 0; else exit 1 }' && grep -c 'N/A — historical-run banner is code-verified' .planning/phases/16-windows-uat-release/16-SMOKE-TEST-GUIDE.md && grep -c '7.4 mL' .planning/phases/16-windows-uat-release/16-HAND-CALC.md"</automated>
  </verify>
  <done>
    - 16-SMOKE-TEST-GUIDE.md exists with Section A (15 steps) + Section B (17 SMK3 rows + 1 D-15-08 row, SMK3-16 pre-marked N/A) + Section C
    - 16-HAND-CALC.md exists with PRD case + setups=3 + D-16-15(c) + D-16-15(d)
    - Fixture coverage audit complete — actual panel names recorded
    - Phase 15.1 HUMAN-UAT carryover items folded into Section A steps 11 / 13
    - One commit, pushed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 1 produces planning docs + a repo-root CHANGELOG.md + two phase-directory markdown files. No new code paths, no new IPC, no new schema. No untrusted input crosses any boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information disclosure | CHANGELOG.md release notes | accept | Operator-facing language only (D-16-12); no DB paths, no API keys, no env values pasted in. Manual review at task acceptance time. |
| T-16-01-02 | Tampering | 16-SMOKE-TEST-GUIDE.md (consumed by Wave 2 operator) | accept | Markdown file; transferred to the lab PC alongside the installer; no integrity check needed (the operator reads it on-screen, not executes it). |
| T-16-01-03 | Repudiation | RETEST.md (produced in Wave 2, signed off by operator) | accept | Wave 2's operator-authored RETEST.md is the audit trail. RETEST.md is the source of truth for "did v1.0.0 pass UAT?"; the tag and release in Wave 3 cite it directly. |

Zero HIGH findings. Wave 1 introduces zero new attack surface — it's all documentation.
</threat_model>

<verification>
- Task 1 reconciles 4 planning docs; greps verify v2.0 -> v1.0 + Phase 6/7 reframing
- Task 2 creates CHANGELOG.md; greps verify Keep-a-Changelog 1.1.0 structure + plain-language audience (no SMK3 / D-XX codes in user-visible body) + v0.7.0 sub-panel removal note
- Task 3 creates 16-SMOKE-TEST-GUIDE.md + 16-HAND-CALC.md; greps verify Section A/B/C structure, 18-row matrix coverage, SMK3-16 N/A verbatim per D-16-21, PRD worked-example numbers (7.4 mL / 3.7 mL) locked in hand-calc

After this plan ships, Wave 2 (Plan 16-02) consumes all three artifacts:
- CHANGELOG.md is read at version-bump time + reused verbatim by Wave 3 (`gh release create --notes-file`)
- 16-SMOKE-TEST-GUIDE.md is handed off to the operator at the Windows UAT checkpoint
- 16-HAND-CALC.md is the operator's calc-verification reference during Section A Step 6
</verification>

<success_criteria>
All four planning docs reconciled to the v1.x release arc. CHANGELOG.md exists at repo root in Keep-a-Changelog 1.1.0 format with v1.0.0 as the inaugural entry. 16-SMOKE-TEST-GUIDE.md + 16-HAND-CALC.md ready for the Wave 2 build + HUMAN-UAT checkpoint. No source code, no schema, no test, no electron-builder.yml, no package.json touched in this plan.
</success_criteria>

<output>
After completion, write `.planning/phases/16-windows-uat-release/16-01-SUMMARY.md` using the standard summary template. Frontmatter `requirements-completed` lists `[D-16-01, D-16-02, D-16-03, D-16-05, D-16-06, D-16-07, D-16-08, D-16-09, D-16-10, D-16-11, D-16-12, D-16-13, D-16-14, D-16-15a, D-16-15c, D-16-15d, D-16-19, D-16-20, D-16-21]` (the locked decisions this plan implements at the doc-authoring level). `key-files` lists the four planning docs + CHANGELOG.md + 16-SMOKE-TEST-GUIDE.md + 16-HAND-CALC.md.
</output>
