---
phase: 03.1-panel-data-import
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/main/ipc/import.ts
  - src/shared/constants/channels.ts
  - src/preload/index.ts
  - src/renderer/src/features/import/ImportButton.tsx
  - src/renderer/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "Operator can click Import button and select a CSV/Excel file"
    - "Successful import shows count of created analytes and panels"
    - "Failed import shows validation errors with row numbers"
    - "Imported data appears in existing analyte/panel selection UI"
    - "App builds and packages without errors"
  artifacts:
    - path: "src/main/ipc/import.ts"
      provides: "IPC handler for import channel"
      exports: ["registerImportHandlers"]
    - path: "src/renderer/src/features/import/ImportButton.tsx"
      provides: "Import UI trigger and result display"
      exports: ["ImportButton"]
  key_links:
    - from: "src/renderer/src/features/import/ImportButton.tsx"
      to: "src/main/ipc/import.ts"
      via: "IPC bridge"
      pattern: "window\\.api\\.importPanelData|invoke.*import"
    - from: "src/main/ipc/import.ts"
      to: "src/main/import/importer.ts"
      via: "importPanelData call"
      pattern: "importPanelData"
---

<objective>
Wire the import pipeline to the UI: IPC handler, preload bridge, import button with file dialog, result/error display, and build verification.

Purpose: Operators can trigger panel data import from the app and see clear feedback on success or failure.
Output: Complete import feature accessible from the UI, verified via build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-panel-data-import/03.1-RESEARCH.md
@.planning/phases/03.1-panel-data-import/03.1-01-SUMMARY.md
@src/shared/constants/channels.ts
@src/preload/index.ts
@src/main/ipc/import.ts
@src/renderer/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC handler, preload bridge, and Import UI</name>
  <files>
    src/main/ipc/import.ts
    src/shared/constants/channels.ts
    src/preload/index.ts
    src/renderer/src/features/import/ImportButton.tsx
    src/renderer/src/App.tsx
  </files>
  <action>
    1. Add to `src/shared/constants/channels.ts`:
       - `IMPORT_PANEL_DATA: 'import:panel-data'`

    2. Create `src/main/ipc/import.ts`:
       - Export `registerImportHandlers()` function
       - Handle IMPORT_PANEL_DATA channel:
         a. Show file dialog using `dialog.showOpenDialog` with filters for csv, xlsx, xls
         b. If canceled, return `{ success: false, canceled: true }`
         c. Call `importPanelData(filePath)` from importer module
         d. Return the ImportResult
       - Register in main process (find where other handlers are registered and add this one)

    3. Update `src/preload/index.ts`:
       - Add `importPanelData: () => ipcRenderer.invoke(IPC_CHANNELS.IMPORT_PANEL_DATA)` to the API object exposed to renderer

    4. Create `src/renderer/src/features/import/ImportButton.tsx`:
       - Button labeled "Import Panel Data" with upload icon (use a simple SVG or text icon)
       - On click: call `window.api.importPanelData()`
       - Show loading state while importing
       - On success: show green banner with counts ("Imported X analytes, Y panels, Z links. Skipped N existing analytes.")
       - On error (validation errors): show red banner with error list, each showing row number and issues
       - On cancel: do nothing
       - Style with Tailwind, consistent with existing app styling
       - Result banner dismissible (X button or auto-dismiss after 10s)

    5. Add ImportButton to the app - find the appropriate location in the navigation or settings area. Look at App.tsx and the existing layout to determine where an import action fits (likely in a header/toolbar area or alongside platform selection).

    6. Register the import IPC handler in main process startup (find where `registerPlatformHandlers`, `registerAnalyteHandlers`, etc. are called and add `registerImportHandlers` there).
  </action>
  <verify>
    - `npx tsc --noEmit` compiles
    - `npm run build` succeeds
    - IPC channel constant exists
    - Preload exposes importPanelData
    - ImportButton component renders
  </verify>
  <done>Import button visible in UI. Clicking it opens file dialog. Selected file is parsed, validated, and imported. Results shown to operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete panel data import feature: file dialog, CSV/Excel parsing, validation, transactional DB insert, UI feedback</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the app
    2. Locate the Import button in the UI
    3. Create a test CSV file with this content:
       ```
       platform,species,panel_name,analyte_name,bead_region,bead_stock_conc,antibody_stock_conc
       Milliplex,Mouse,Test Panel,IL-1a,32,20,25
       Milliplex,Mouse,Test Panel,IL-1b,47,20,25
       ```
    4. Click Import, select the CSV file
    5. Verify success message shows "2 analytes, 1 panel, 2 links"
    6. Navigate to Milliplex > Mouse in the analyte selection - verify IL-1a and IL-1b appear
    7. Import the same file again - verify it reports skipped analytes (no duplicates)
    8. Test with an invalid CSV (missing columns) - verify error messages show row numbers
    9. Run `npm run build` to verify production build succeeds
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full import flow works end-to-end: button -> dialog -> parse -> validate -> insert -> feedback
- Duplicate imports are handled gracefully (skip, not error)
- Invalid files produce clear, actionable error messages
- Production build succeeds with xlsx dependency included
</verification>

<success_criteria>
- Operator can import a CSV/Excel file via the UI
- Success shows created/skipped counts
- Errors show row numbers and issue descriptions
- Existing analyte/panel selection reflects imported data
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-panel-data-import/03.1-02-SUMMARY.md`
</output>
