---
phase: 03.3-analyte-selection-redesign
plan: 06
type: ad-hoc
autonomous: false
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/features/calculator/components/CalculatorPanel.tsx
  - src/renderer/src/features/plate/components/PlateGrid.tsx
  - src/renderer/src/features/plate/components/PlatePanel.tsx
  - src/renderer/src/features/plate/components/WellCell.tsx
  - src/renderer/src/stores/plateStore.ts
must_haves:
  truths:
    - "Column headers 4-12 are click/drag interactive to toggle all wells in a column, matching existing well drag semantics"
    - "CalculatorPanel renders PlatePanel inline; Page 2 exposes a single consolidated card instead of two"
---

# Plan 06: Column Header Selection and Layout Consolidation

Backfilled 2026-04-22 from git history for an ad-hoc commit that shipped without a plan at the time.

<objective>
Add click-and-drag selection on column headers 4-12 so users can toggle entire sample columns without dragging across every well, and collapse the Page 2 two-card layout (CalculatorPanel + separate Plate Preview card) into a single CalculatorPanel that embeds PlatePanel directly. Also auto-navigate plateStore to newly added plates so "Add Plate" leaves the user on the plate they just created.
</objective>

<task id="1">
<action>Wire column-header interaction props through PlateGrid/WellCell, implement click/drag state and mouseup-anywhere teardown in PlatePanel, move PlatePanel import from App.tsx into CalculatorPanel, and set activePlate to the new plate number in plateStore.addPlate.</action>
<done>Clicking a header in cols 4-12 toggles that column, dragging across headers toggles multiple columns, Page 2 renders one consolidated card, and Add Plate lands on the new plate.</done>
</task>
