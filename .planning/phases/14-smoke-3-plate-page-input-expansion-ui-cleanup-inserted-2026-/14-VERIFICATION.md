---
phase: 14-smoke-3-plate-page-input-expansion-ui-cleanup
verified: 2026-05-12T15:00:00Z
status: human_needed
score: 6/6 must-haves verified (all programmatic gates pass; visual/runtime UX requires Windows UAT)
overrides_applied: 0
human_verification:
  - test: "Plate-page wizard renders 7 inputs in D-01 order (Number of Plates → Replicate Mode → Number of Samples → Old Beads → Old Antibodies → Number of Setups → Request Type) with operator-correct labels, step values, and defaults (0 / 0 / 1)"
    expected: "Inputs appear in stated order; defaults match (Plates=1, Old Beads=0.0, Old Antibodies=0.0, Number of Setups=1); step=0.1 for Old Beads/Old Antibodies; step=1 for Number of Plates/Number of Setups; min enforced on type/blur"
    why_human: "vitest config is Node-only (no jsdom + @testing-library/react), so true component-render coverage of CalculatorForm is not wired. Source-order grep verifies the labels exist in correct lexical order but does not exercise the real DOM. Phase-16 Windows UAT is the documented verification path."
  - test: "20%-cap soft-block + confirm-once override flow"
    expected: "Typing Old Beads (or Old Antibodies) > 20% of total reaction volume opens OldReagentCapModal once. Yes-override sets amber 'overridden' badge and resumes ItemizedVolumeDisplay; Cancel snaps input to floor-rounded cap value and re-resumes; CalculatorPanel renders amber 'Cap exceeded — override or lower the value to resume calculation' placeholder whenever capPaused is true and no override accepted"
    why_human: "The capPaused store + getOutputs() gating is unit-tested (calculatorStore.capPaused.test.ts — 6 tests green) and the WR-01 stale-closure regression is locked by oldReagentCommit.test.ts (12 tests green), but the React useEffect → setCapPaused → CalculatorPanel render path runs only in a browser/Electron environment, not the Node-only vitest harness."
  - test: "Duplicate plate layout visual confirmation"
    expected: "With Replicate Mode = Duplicates and Sample Count = 36, plate 1 shows samples 1-4 in col 4 as adjacent-row vertical pairs (A4,B4)(C4,D4)(E4,F4)(G4,H4), then 5-8 in col 5, …, 33-36 in col 12. Col-12 special-case A↔E / B↔F / C↔G / D↔H is retired."
    why_human: "Store-level geometry is exhaustively unit-tested (76 getDuplicatePair cases + 10 plateStore.autoFill / usePlateLayout integration cases), but operator-facing rendering of the PlateGrid component runs only in Electron."
  - test: "Premix deselection UX flow"
    expected: "Pick a premix → singles pool excludes members. Add a single from the pool. Deselect premix → previously-picked single survives in 'Selected Analytes' (D-18 preservation). Premix members become re-selectable in the pool but are NOT auto-added (D-19). Switching to a new premix prunes only the new premix's members from selectedSingleIds; non-overlap singles survive (D-20)."
    why_human: "selectionStore tests (5 cases) cover the state-machine; SelectedAnalytesList flat-list rendering covered only by sortByBeadRegion unit tests (4 cases). The full user-visible flow needs runtime in Electron."
  - test: "Bead-region display: flat list, sorted ascending, member badges with no per-row remove on premix members"
    expected: "Selected Analytes panel shows one flat list of (panel members + singles), sorted ascending by numeric bead region. Each row has analyte name + bead-region badge. Singles rows show an X-remove on hover; premix-member rows do NOT show the X-remove."
    why_human: "Sort key is unit-tested (sortAnalytes.test.ts — 4 cases). Visual ordering and conditional remove-button rendering require Electron / Windows UAT."
  - test: "Stock-concentration UI cleanup verified visually on platform selector"
    expected: "Platform-selection step (wizard step 0) shows: each PlatformCard renders title + optional 'Selected' badge + optional description ONLY (no 'Stock Concentration: Xx' footer). The green-bordered 'Platform Selected: {name}' box renders only the heading (no 'Stock concentration: Xx' line, no 'Ready to proceed with reagent calculations.' line)."
    why_human: "Grep confirms the strings are removed from source, but visual confirmation of layout + spacing after the deletions requires Windows UAT."
  - test: "SMK3-16 snapshot fidelity end-to-end through SQLite"
    expected: "Save a run with oldBeads=1.5 + oldAntibodies=2.0 + numberOfSetups=3 → close app → re-open → load the saved run → calculator inputs restore to exactly 1.5 / 2.0 / 3. A run saved BEFORE Phase 14 (no oldBeads/oldAntibodies columns populated) reloads with oldBeads=0 + oldAntibodies=0 + numberOfSetups=1."
    why_human: "Drizzle migration 0008 + repository round-trip is exhaustively unit-tested (run.test.ts T-1..T-6 + Group J T-J1/T-J2/T-J3), but verifying the IPC ↔ SQLite ↔ IPC ↔ renderer cycle on a real installed Windows build is the documented SMK3-16 acceptance path (PERS-01/02 cf.)."
process_notes:
  - "Plan-executor commit attribution leaked: 7 of 8 plan executors landed commits directly on dev/v1-01 rather than worktree branches despite explicit guardrails. The final code is on dev/v1-01 with 381/381 tests passing — this is a process / audit-trail anomaly, not a deliverable regression. Logged here for milestone-level awareness; does not block phase acceptance."
  - "vitest is Node-only (environment: 'node', include: src/**/*.test.ts). No jsdom / @testing-library/react devDep. True render tests for CalculatorForm / CalculatorPanel / SelectedAnalytesList / PlatformCard / PlatformSelector are not wired — logic is exhaustively covered via store + pure-helper unit tests, and visual confirmation is deferred to Phase 16 Windows UAT. This is an accepted phase-level constraint, not a Phase 14 gap."
  - "Drizzle migration is named 0008_runs_setups_and_old_reagents.sql (not 0004 as the original Plan 14-08 frontmatter suggested) — the file-name discrepancy was caused by additional intermediate migrations landing during Phase 13 (0004..0007). The content (ALTER TABLE runs ADD COLUMN number_of_setups / old_beads / old_antibodies REAL NOT NULL DEFAULT ...) matches the plan; only the filename ordinal differs."
  - "BeadRegionList.tsx (used by PrepSheet + FinalizedRunView) is still a placeholder targeting RECP-03 v1 prep-sheet scope, NOT SMK3-14. SMK3-14 (flat list of selected analytes sorted by bead region) is implemented in SelectedAnalytesList.tsx per CONTEXT D-14/D-17. This matches the plan's intent."
---

# Phase 14: Smoke 3 — Plate Page Input Expansion + UI Cleanup — Verification Report

**Phase Goal:** The Plate page exposes `Old Beads` / `Old Antibodies` / `Number of Setups` inputs feeding the new calculator rules. Premix deselection re-enables members as singles without auto-adding. Bead region display becomes a flat list. Stock-concentration labels on platform-selection screens are removed.

**Verified:** 2026-05-12T15:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| #   | Truth                                                                                                                                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC-1 | Plate page renders numeric inputs `Old Beads (mL)`, `Old Antibodies (mL)`, `Number of Setups` with defaults 0 / 0 / 1 and mins 0 / 0 / 1, all driving calculator volumes in real time                                              | ✓ VERIFIED (programmatic) | `CalculatorForm.tsx:225 / 278 / 315 / 352` renders the four numeric inputs (`Number of Plates`, `Old Beads (mL)`, `Old Antibodies (mL)`, `Number of Setups`) with `step=0.1` / `step=1` and `min=0` / `min=1` per D-04. Store-level defaults `oldBeads: 0`, `oldAntibodies: 0`, `numberOfSetups: 1` at `calculatorStore.ts:104-108`. Validating setters at lines 204-225 reject negatives + NaN. SC text says "above the existing sample-count field" — D-01 reordered to plates-first per PRD note; Old Beads / Old Antibodies / Number of Setups now sit BELOW Sample Count. Accepted as a plan-level interpretation of "drive the calculator volumes in real time."           |
| SC-2 | Calculator subtracts Old Beads from new-bead volume calc and adds it to total bead volume reported on the run document; same for antibodies. Dead volume multiplies by setups.                                                       | ✓ VERIFIED | `applyOldReagentSubtraction` at `calculator.ts:124` returns `{ newReagentUL: max(0, raw-old), totalReagentUL: old + new }`. `calculatorStore.getOutputs()` at `calculatorStore.ts:319-323` floor-rounds old-reagent inputs (D-08) and produces `newBeadsUL` / `totalBeadsUL` / `newAntibodiesUL` / `totalAntibodiesUL`. Dead volume = `numberOfSetups × DEAD_VOLUME_PER_SETUP_UL` (Phase 12 SMK3-05, preserved). 5 vitest cases in Group I + 5 cases in Group K lock the PRD-fixture math. |
| SC-3 | Premix deselection: members return to the singles pool as selectable but are NOT auto-added; CALC-05 cap (max 5 singles when premix selected) still applies                                                                       | ✓ VERIFIED | `selectionStore.selectPanel` at `selectionStore.ts:171-219` is refactored per D-18/D-20: `panelId===null` branch no longer clears `selectedSingleIds` (D-18 preservation comment at line 185); `panelId!==null` branch prunes by `newPanelMemberIds` set membership (D-20 at line 200-208). `getAvailableSingles` filters by selection; CALC-05 cap enforced at `selectionStore.ts:239`. 5 vitest cases in selectionStore.test.ts + 2 E2E cases in Group L (T-L1, T-L2) lock both contracts. |
| SC-4 | Bead region display = single flat list of every analyte in the current selection, sorted by bead region. Premix members and singles appear together; no grouping or hierarchy.                                                  | ✓ VERIFIED | `SelectedAnalytesList.tsx:22-25` builds a single `flatList = [...panelAnalytes, ...singleAnalytes].sort(sortByBeadRegion)`. No grouping/hierarchy in render (lines 60-99). `sortByBeadRegion` at `sortAnalytes.ts:21` ascends numerically with alpha tiebreaker + lexical fallback. 4 vitest cases in sortAnalytes.test.ts + Plan 05 SUMMARY confirms `panelName` prop dropped (D-17). Per-row remove button conditioned on `!isPanelMember` per D-16 (line 78). |
| SC-5 | UI cleanup: `Stock Concentration: Xx` line in PlatformCard removed; `Stock concentration: Xx` and `Ready to proceed with reagent calculations.` lines in PlatformSelector removed; `Platform Selected: {name}` heading retained | ✓ VERIFIED | `grep "Stock Concentration\|stockConcentration"` in `PlatformCard.tsx` → ZERO matches (footer fully removed per D-12). `grep "Stock concentration\|Ready to proceed"` in `PlatformSelector.tsx` → ZERO matches. `grep "Platform Selected:"` in `PlatformSelector.tsx` → 1 match at line 54 (heading retained per D-13). |
| SC-6 | Duplicate plate layout fills adjacent rows in same column: col 4 = (A4,B4)(C4,D4)(E4,F4)(G4,H4), then col 5..12. usePlateLayout updated; tests added.                                                                            | ✓ VERIFIED | `getDuplicatePair` rewritten at `calculator.ts:60-72` with `row % 2 === 0 ? row + 1 : row - 1` partner. `DUPLICATE_HORIZONTAL_PAIRS` + `DUPLICATE_VERTICAL_COL` deleted from source tree (full `grep -rn` returns ZERO matches). `usePlateLayout.ts:82` and `plateStore.ts:135` both use `PAIR_TOP_ROWS = [0,2,4,6]` column-first iteration. 76 vitest cases in getDuplicatePair.test.ts + 10 cases in usePlateLayout.test.ts (Plans 02 SUMMARY confirms). |

**Score:** 6/6 truths VERIFIED programmatically. Each truth has an additional human-verification item for visual / runtime confirmation in Phase 16 Windows UAT.

### Required Artifacts (Plan-frontmatter must_haves)

| Artifact (path)                                                                              | Expected                                                                | Status     | Details                                                                                                                                                |
| -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/renderer/src/lib/decimal.ts`                                                            | `floorToTenthML` exported                                               | ✓ VERIFIED | Line 70 — Decimal.ROUND_FLOOR, mL-domain, accepts Decimal \| number. 17 vitest cases pass.                                                            |
| `src/renderer/src/lib/calculator.ts`                                                         | `applyOldReagentSubtraction` exported                                   | ✓ VERIFIED | Line 124 — `max(0, raw - old)` + `old + new`. 5 vitest cases pass.                                                                                    |
| `src/shared/types/calculator.ts`                                                             | `CalculatorOutputs` extended with 4 optional fields                     | ✓ VERIFIED | Lines 39-45 — `newBeadsUL?`, `totalBeadsUL?`, `newAntibodiesUL?`, `totalAntibodiesUL?` all `Decimal`.                                                  |
| `src/shared/constants/calculator.ts`                                                         | `getDuplicatePair` rewritten + retired constants deleted                | ✓ VERIFIED | Line 60-72 new geometry. Retired constants ZERO matches in `src/`.                                                                                    |
| `src/renderer/src/features/plate/hooks/usePlateLayout.ts`                                    | Column-first vertical-pair fill via PAIR_TOP_ROWS                       | ✓ VERIFIED | Line 82 — `PAIR_TOP_ROWS = [0,2,4,6]`, column-major iteration.                                                                                         |
| `src/renderer/src/stores/plateStore.ts`                                                      | `autoFill` matches usePlateLayout + `setPlateCount` action added        | ✓ VERIFIED | Line 135 PAIR_TOP_ROWS; line 36 setPlateCount interface; line 299 impl. 9 vitest cases pass.                                                          |
| `src/renderer/src/features/platform/components/PlatformCard.tsx`                             | Stock Concentration footer deleted; title + Selected + description kept | ✓ VERIFIED | `grep "Stock Concentration\|stockConcentration"` → 0 matches; `grep "platform.name\|platform.description\|Selected"` → ≥1 each.                       |
| `src/renderer/src/features/platform/components/PlatformSelector.tsx`                         | Stock conc + Ready-to-proceed lines deleted; Platform Selected heading kept | ✓ VERIFIED | `grep "Stock concentration\|Ready to proceed"` → 0; `Platform Selected:` 1 match at line 54.                                                       |
| `src/renderer/src/stores/calculatorStore.ts`                                                 | oldBeads + oldAntibodies + capPaused + setters + getOutputs gating      | ✓ VERIFIED | Lines 52/58 fields; 74 capPaused; 82-85 setters; 290 capPaused guard in getOutputs; 319-323 floor-round + subtraction wiring.                          |
| `src/shared/types/run.ts`                                                                    | RunRecord + RunCreate extended with optional oldBeads + oldAntibodies   | ✓ VERIFIED | Lines 42, 47, 93, 94 — all optional `number` per D-08 author-intent rule.                                                                              |
| `src/renderer/src/features/run/hooks/useRunSnapshot.ts`                                      | buildRunSnapshot writes oldBeads + oldAntibodies                        | ✓ VERIFIED | Lines 116, 117 — `calculator.oldBeads`, `calculator.oldAntibodies` written verbatim (no pre-round) per D-08. Also imports `DEAD_VOLUME_PER_SETUP_UL` post WR-03 fix.   |
| `src/renderer/src/stores/runStore.ts`                                                        | loadRun cascade restores oldBeads + oldAntibodies AFTER setNumberOfSetups | ✓ VERIFIED | Lines 141 setNumberOfSetups, 148 setOldBeads, 149 setOldAntibodies (`?? 0` default).                                                                  |
| `src/main/db/schema.ts`                                                                      | runs table has number_of_setups + old_beads + old_antibodies columns   | ✓ VERIFIED | Lines 205-207 — all `real(...).notNull().default(...)`.                                                                                                |
| `drizzle/migrations/0008_runs_setups_and_old_reagents.sql`                                   | ALTER TABLE runs ADD COLUMN x3                                          | ✓ VERIFIED | 3 ALTER TABLE statements; filename ordinal differs from plan (0008 vs 0004) due to intervening Phase 13 migrations — see process_notes.                |
| `src/main/db/repositories/run.ts`                                                            | create + update + getById round-trip the 3 new fields                   | ✓ VERIFIED | Lines 85-87 insertValues; 162-164 update; 6 vitest cases (T-1..T-6) lock round-trip.                                                                  |
| `src/renderer/src/features/selection/lib/sortAnalytes.ts`                                    | `sortByBeadRegion` exported with numeric/lexical/alpha fallback         | ✓ VERIFIED | Line 21 — 4 vitest cases pass.                                                                                                                         |
| `src/renderer/src/stores/selectionStore.ts`                                                  | selectPanel(null) preserves; selectPanel(newId) prunes by membership    | ✓ VERIFIED | Lines 171-219; 5 vitest cases + 2 E2E cases (Group L T-L1/T-L2).                                                                                       |
| `src/renderer/src/features/selection/components/SelectedAnalytesList.tsx`                    | Flat sorted list; panelName prop dropped; conditional remove button     | ✓ VERIFIED | `grep "panelName\|useState\|Individual Additions\|panelExpanded"` → all 0. Flat list at line 22-25; conditional remove at line 78.                    |
| `src/renderer/src/features/calculator/components/CalculatorForm.tsx`                         | 7 inputs in D-01 order + 20%-cap UX + modal wiring + setCapPaused effect | ✓ VERIFIED | Lines 222, 241, 273, 275, 312, 349, 371 — labels in correct source order; line 102 setCapPaused useEffect; line 392 OldReagentCapModal wired.         |
| `src/renderer/src/features/plate/components/OldReagentCapModal.tsx`                          | Thin ConfirmModal wrapper with destructive Yes-override                 | ✓ VERIFIED | File exists; grep confirms title, primary/secondary labels, primaryStyle="destructive".                                                                |
| `src/renderer/src/features/calculator/components/CalculatorPanel.tsx`                        | Cap-exceeded amber placeholder when capPaused                           | ✓ VERIFIED | Line 35-41 — `{capPaused ? <amber-placeholder /> : ...}` ternary; defense-in-depth alongside getOutputs() null-return.                                |
| `src/renderer/src/features/calculator/hooks/useCalculator.ts`                                | Extended to expose 5 new inputs + 5 new setters incl. capPaused          | ✓ VERIFIED | Lines 30-39 destructure; 91-95 return inputs; 122-126 return setters.                                                                                  |
| `src/renderer/src/features/calculator/lib/oldReagentCommit.ts`                               | (Added post WR-01 fix) Pure helper for old-reagent commit decision      | ✓ VERIFIED | File exists; 12 vitest cases lock the override-state-machine decision logic (regression test for the WR-01 stale-closure bug).                         |

### Key Link Verification

| From                                              | To                                                          | Via                                                           | Status      | Details                                                                                                                |
| ------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| `calculatorStore.ts`                              | `lib/decimal.ts` floorToTenthML                             | `import { floorToTenthML }`                                   | ✓ WIRED     | Line 12 import; line 319-320 callsites convert mL → µL inside getOutputs.                                              |
| `calculatorStore.ts`                              | `lib/calculator.ts` applyOldReagentSubtraction              | `import { applyOldReagentSubtraction }`                       | ✓ WIRED     | Line 10 import; lines 322-323 callsites compute new/total per reagent.                                                  |
| `runStore.loadRun`                                | `calculatorStore.setOldBeads / setOldAntibodies`            | Phase 12-pattern cascade                                      | ✓ WIRED     | Lines 141 → 148-149 — setOldBeads/setOldAntibodies fire AFTER setNumberOfSetups in source order.                       |
| `useRunSnapshot.buildRunSnapshot`                 | `calculatorStore` oldBeads + oldAntibodies fields           | Direct read at snapshot-build time                            | ✓ WIRED     | Lines 116-117 read raw author-intent values (D-08); also imports DEAD_VOLUME_PER_SETUP_UL post-WR-03.                  |
| `CalculatorForm`                                  | `useCalculator` hook returning 5 new inputs + 5 new setters | Destructure                                                   | ✓ WIRED     | Lines 31-45 destructure includes oldBeads, oldAntibodies, plateCount, numberOfSetups, capPaused + all 5 setters.        |
| `CalculatorForm` cap-pause effect                 | `calculatorStore.setCapPaused`                              | useEffect on derived `beadsExceedsCap \|\| antibodiesExceedsCap` | ✓ WIRED   | Line 102-103 useEffect; pushes derived flag into the store; getOutputs() returns null when paused.                     |
| `CalculatorPanel`                                 | `useCalculator.capPaused` for output gating                 | Ternary in JSX                                                | ✓ WIRED     | Line 26 destructure + line 35 ternary; renders amber placeholder when paused.                                          |
| `SelectedAnalytesList`                            | `lib/sortAnalytes.sortByBeadRegion`                         | Named import + sort callsite                                  | ✓ WIRED     | Line 3 import; line 23 invocation.                                                                                      |
| `selectionStore.selectPanel`                      | `selectedSingleIds` preservation logic                      | D-18 — branch skips clearing                                  | ✓ WIRED     | Line 185 comment explicit; no `selectedSingleIds: []` inside the null branch.                                          |
| `runRepository.create / update / getById`         | runs table number_of_setups / old_beads / old_antibodies    | Drizzle column mapping                                        | ✓ WIRED     | Repository lines 85-87 / 162-164 / list mapping; migration 0008 adds columns; 6 round-trip vitest cases pass.          |
| `plateStore.setPlateCount`                        | UI input "Number of Plates"                                 | useCalculator pulls setPlateCount → CalculatorForm onBlur     | ✓ WIRED     | useCalculator.ts:51 pulls from plateStore; CalculatorForm commitPlates at line 110-114 calls setPlateCount.            |
| `OldReagentCapModal`                              | `ConfirmModal` primitive                                    | Composition                                                   | ✓ WIRED     | Imports ConfirmModal; renders with destructive "Yes, override" primary + Cancel secondary.                              |

### Data-Flow Trace (Level 4)

| Artifact                        | Data Variable                              | Source                                                   | Produces Real Data | Status       |
| ------------------------------- | ------------------------------------------ | -------------------------------------------------------- | ------------------ | ------------ |
| `CalculatorForm`                | oldBeads / oldAntibodies / numberOfSetups  | calculatorStore via useCalculator                        | Yes — store values | ✓ FLOWING    |
| `CalculatorForm`                | plateCount                                 | plateStore via useCalculator                              | Yes — store value  | ✓ FLOWING    |
| `CalculatorForm`                | capML / totalReactionVolumeML              | Derived from sampleCount × volumePerWell + setups × DEAD | Yes — live arith   | ✓ FLOWING    |
| `CalculatorPanel`               | capPaused / outputs / itemizedVolumes      | useCalculator → calculatorStore.getOutputs               | Yes — gated null/CalculatorOutputs | ✓ FLOWING |
| `SelectedAnalytesList`          | panelAnalytes / singleAnalytes (props)     | AnalyteSelectionPanel pulls from selectionStore           | Yes — selection-driven | ✓ FLOWING |
| `useRunSnapshot.buildRunSnapshot` | oldBeads / oldAntibodies / numberOfSetups | calculatorStore direct read at save time                  | Yes — raw author intent (D-08) | ✓ FLOWING |
| `runStore.loadRun` cascade      | oldBeads / oldAntibodies / numberOfSetups  | run record from SQLite via IPC                            | Yes — `?? 0/1` defaults absorb legacy runs | ✓ FLOWING |
| `runRepository.create`          | numberOfSetups / oldBeads / oldAntibodies   | RunCreate payload → Drizzle insert                       | Yes — write path verified by T-1..T-3 | ✓ FLOWING |
| `runRepository.getById / list`  | numberOfSetups / oldBeads / oldAntibodies   | Drizzle select → RunRecord                                | Yes — read path verified by T-4 + T-6 | ✓ FLOWING |

No HOLLOW or DISCONNECTED artifacts found. All data sources produce real values; defaults at the store and IPC boundaries handle legacy/empty inputs deterministically.

### Behavioral Spot-Checks

| Behavior                                                    | Command                                                              | Result               | Status |
| ----------------------------------------------------------- | -------------------------------------------------------------------- | -------------------- | ------ |
| Full vitest suite passes                                    | `npm test`                                                           | 381/381 in 25 files  | ✓ PASS |
| TypeScript typecheck clean (both node + web tsconfigs)      | `npm run typecheck`                                                  | Exit 0, no errors    | ✓ PASS |
| Plate-geometry retired-constant grep returns zero matches   | `grep -rn "DUPLICATE_HORIZONTAL_PAIRS\|DUPLICATE_VERTICAL_COL" src/` | 0 matches            | ✓ PASS |
| Stock-conc grep cleanup verified in PlatformCard            | `grep "Stock Concentration\|stockConcentration" PlatformCard.tsx`     | 0 matches            | ✓ PASS |
| Stock-conc grep cleanup verified in PlatformSelector        | `grep "Stock concentration\|Ready to proceed" PlatformSelector.tsx`   | 0 matches            | ✓ PASS |
| Migration 0008 adds 3 ALTER TABLE columns                   | `grep "ADD" drizzle/migrations/0008_*.sql`                            | 3 statements         | ✓ PASS |

### Requirements Coverage

| Requirement   | Source Plan(s)                       | Description                                                                                         | Status      | Evidence                                                                                                                                                       |
| ------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SMK3-01**   | 14-03                                | Remove Stock Concentration labels from platform-selection screens                                   | ✓ SATISFIED | PlatformCard footer + PlatformSelector stock-conc + ready-to-proceed deletions verified by grep. Platform Selected: {name} heading retained.                  |
| **SMK3-02**   | 14-01, 14-04, 14-06, 14-08           | Plate page exposes Old Beads input (mL, ≥0, default 0); subtracted from new-bead vol, added to total | ✓ SATISFIED | CalculatorForm renders Old Beads with min=0/step=0.1; calculatorStore.oldBeads field + validating setter; getOutputs() produces newBeadsUL + totalBeadsUL.    |
| **SMK3-03**   | 14-01, 14-04, 14-06, 14-08           | Mirror of SMK3-02 for antibodies                                                                    | ✓ SATISFIED | Same as SMK3-02 — mirrored fields/setters/outputs for antibodies. Group I T-I5 specifically locks the antibodies math path.                                  |
| **SMK3-04**   | 14-04, 14-06, 14-08                  | Number of Setups input (default 1, min 1)                                                           | ✓ SATISFIED | CalculatorForm renders Number of Setups with min=1/step=1; numberOfSetups field on calculatorStore (Phase 12, persisted in DB via 0008 migration in Phase 14). |
| **SMK3-13**   | 14-05, 14-07                         | Premix deselection: members return to singles pool as selectable, NOT auto-added; CALC-05 cap holds | ✓ SATISFIED | selectionStore.selectPanel D-18 preservation + D-20 prune-by-membership; CALC-05 cap intact (MAX_SINGLES_PREMIX). 5 unit + 2 E2E vitest cases.               |
| **SMK3-14**   | 14-05                                | Bead region display = flat list of every analyte (TA) sorted by bead region                          | ✓ SATISFIED | SelectedAnalytesList builds flat sorted list via sortByBeadRegion; panelName prop dropped; conditional remove per D-16.                                       |
| **SMK3-RPL-01** | 14-02                              | Replicate Mode set = Single + Duplicate only (no triplicate)                                         | ✓ SATISFIED | CalculatorForm radios offer only `singles` + `duplicates` (line 251-263); ReplicateMode union type has only those two values.                                |
| **SMK3-RPL-02** | 14-02                              | Duplicate layout = adjacent rows in same column for cols 4-12                                        | ✓ SATISFIED | getDuplicatePair rewritten; usePlateLayout + plateStore.autoFill use PAIR_TOP_ROWS column-major; retired horizontal-pair + col-12 special case. 86+ vitest cases. |

**All 8 declared phase requirements (SMK3-01, 02, 03, 04, 13, 14, RPL-01, RPL-02) are satisfied. No orphaned requirements in REQUIREMENTS.md Phase 14 traceability.**

### Anti-Patterns Found

| File                                                                  | Line  | Pattern                                                                                        | Severity | Impact                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/renderer/src/features/plate/components/BeadRegionList.tsx`        | 1-75  | Placeholder UI (says "Bead region data not yet configured for this platform")                  | ℹ️ Info  | This is **NOT** the SMK3-14 surface — SMK3-14 targets SelectedAnalytesList per CONTEXT D-14/D-17. BeadRegionList belongs to RECP-03 v1 prep-sheet scope and remains a documented placeholder. Out of Phase-14 scope. |
| Cross-cutting: review IN-01 through IN-06                              | n/a   | 6 minor info-level findings (numberOfSetups REAL vs INTEGER, JSX.Element return-type, etc.) | ℹ️ Info  | Explicitly out of `fix_scope: critical_warning` per 14-REVIEW-FIX.md. Tracked for Phase 15+ follow-up. No deliverable regression.                                                                                                                                                                                     |
| `src/renderer/src/features/calculator/components/CalculatorForm.tsx`   | 127, 142 | `parseFloat` accepts trailing garbage like `"1.5abc"` → 1.5                                  | ℹ️ Info  | Review IN-04: cosmetic input-strictness concern; existing `!Number.isFinite(n)` guard catches NaN but parseFloat truncates. Defer to follow-up; no immediate operator-visible regression.                                                                                                                            |
| n/a — no blocker or warning-level anti-patterns introduced by Phase 14 | n/a   | n/a                                                                                            | n/a      | All 3 review Warnings (WR-01 stale-closure, WR-02 cap-snap flicker, WR-03 dead-volume magic number) were fixed in 14-REVIEW-FIX.md iteration 1 prior to verification.                                                                                                                                                |

### Human Verification Required

See `human_verification:` frontmatter for the full list. Summary:

1. **Plate-page input rendering + defaults / mins / steps** — visual confirmation of CalculatorForm in Electron.
2. **20%-cap soft-block + override modal flow** — runtime confirmation of the React useEffect → setCapPaused → CalculatorPanel render path.
3. **Duplicate plate-layout visual** — PlateGrid render of col-4 (A4,B4)(C4,D4)(E4,F4)(G4,H4) through col-12.
4. **Premix deselection UX flow** — end-to-end selection of premix → single → deselect premix → previously-picked single survives, members re-selectable but not auto-added.
5. **Flat bead-region sorted list with conditional remove button** — visual confirmation of SelectedAnalytesList rendering.
6. **Stock-concentration cleanup visual** — confirm PlatformCard + PlatformSelector layout after deletions.
7. **SMK3-16 snapshot fidelity end-to-end** — save+reload through actual SQLite on Windows; legacy run reload defaults to 0/0.

All 7 items are routed to Phase 16 Windows UAT (per CLAUDE.md "dev on macOS, test on Windows" workflow).

### Gaps Summary

**No gaps found.** All 6 ROADMAP Success Criteria, all 8 declared requirements (SMK3-01..04, 13, 14, RPL-01, RPL-02), and all artifacts pass programmatic verification. 381/381 vitest suite green; typecheck clean; all key links wired; data flows verified at all 4 levels.

The phase has two categories of items that prevent a `passed` status:

1. **Programmatic-vs-visual asymmetry (expected):** vitest is Node-only by project convention (CLAUDE.md). Component-render confirmation is deferred to Phase 16 Windows UAT — an accepted phase-level workflow constraint, not a Phase 14 gap. This results in 7 human-verification items being routed forward.

2. **Process-level note (informational):** 7 of 8 plan executors leaked commits onto `dev/v1-01` directly rather than worktree branches despite explicit guardrails. The final code is correct and tested; only commit attribution is anomalous. Logged under `process_notes:` for milestone-level awareness; does not block phase acceptance.

---

_Verified: 2026-05-12T15:00:00Z_
_Verifier: Claude (gsd-verifier)_
