---
phase: 14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/features/platform/components/PlatformCard.tsx
  - src/renderer/src/features/platform/components/PlatformSelector.tsx
autonomous: true
requirements: [SMK3-01]
requirements_addressed: [SMK3-01]

must_haves:
  truths:
    - "PlatformCard no longer renders `Stock Concentration: Xx` text or the surrounding `border-t pt-2` separator (D-12)"
    - "PlatformSelector's green-bordered selected-platform box renders ONLY `Platform Selected: {selectedPlatform.name}` — no Stock concentration line, no `Ready to proceed with reagent calculations.` line (D-13)"
    - "All other content in PlatformCard (Title + Selected badge + Description) and PlatformSelector (selector grid, loading/error states, green box heading) is preserved byte-for-byte except the deletions"
  artifacts:
    - path: "src/renderer/src/features/platform/components/PlatformCard.tsx"
      provides: "Card with Title + Selected badge + Description only (no footer)"
      contains: "platform.name"
    - path: "src/renderer/src/features/platform/components/PlatformSelector.tsx"
      provides: "Selector with green box containing only `Platform Selected: {name}` heading"
      contains: "Platform Selected:"
  key_links:
    - from: "src/renderer/src/features/platform/components/PlatformCard.tsx"
      to: "platform.stockConcentration field"
      via: "REMOVED — no longer read by this component"
      pattern: "NOT platform\\.stockConcentration"
---

<objective>
Strip stock-concentration labels from the Platform selection UI per Smoke 3 SMK3-01 + CONTEXT D-12/D-13. PlatformCard loses its `Stock Concentration: Xx` footer block (including the `border-t` separator). PlatformSelector loses both the `Stock concentration: Xx` line and the `Ready to proceed with reagent calculations.` line inside the green selected-platform box.

Purpose: Smoke 3 PRD §Hierarchy First Step explicitly says stock-concentration labels are removed from operator-facing surfaces (the operator no longer needs to think about platform stock factor — it's an implementation detail). The Platform Selected: {name} heading stays for orientation.

Output: Two surgical deletions. No new tests required — this is pure subtractive UI cleanup; the file's surrounding render-tree is preserved.

This plan is independent of Plans 01, 02, 04, 05, 06, 07 — parallel in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-CONTEXT.md
@.planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Stock Concentration footer from PlatformCard.tsx (D-12)</name>
  <files>src/renderer/src/features/platform/components/PlatformCard.tsx</files>
  <read_first>
    - src/renderer/src/features/platform/components/PlatformCard.tsx (existing 52 lines — see lines 42-49 for the deletion target)
    - .planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-PATTERNS.md §7 (file 7 — exact deletion block + post-state shape)
    - .planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-CONTEXT.md §decisions D-12
  </read_first>
  <action>
    Delete lines 42-49 of `src/renderer/src/features/platform/components/PlatformCard.tsx` entirely (the `<div className="flex items-center gap-2 pt-2 border-t border-[var(--color-border)]">` block through its closing `</div>`):

    ```typescript
    <div className="flex items-center gap-2 pt-2 border-t border-[var(--color-border)]">
      <span className="text-xs font-medium text-[var(--color-muted)] uppercase tracking-wide">
        Stock Concentration:
      </span>
      <span className="text-sm font-semibold text-[var(--color-foreground)]">
        {platform.stockConcentration}x
      </span>
    </div>
    ```

    PRESERVE the surrounding sibling JSX byte-for-byte:
    - Lines 25-34: the flex-justify-between header with `<h3>` title + Selected badge
    - Lines 36-40: the optional description paragraph (`{platform.description && ...}`)
    - Lines 9-23: the outer `<button>` with click handler + className
    - Closing `</button>` after the deletion

    Post-state: PlatformCard renders Title (with optional Selected badge) + optional Description. Nothing else. Per D-12: no replacement micro-fact, no preserved spacer (the `border-t` separator goes WITH the stock-conc block).

    DO NOT remove `platform.stockConcentration` from the `Platform` interface (other consumers — e.g. useCalculator.ts line 75 — still read it). DO NOT modify the import line at line 1 (the `Platform` type is still used).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    PlatformCard.tsx renders without the stock-concentration footer. TypeScript compiles. `npm run test` continues to pass.
  </done>
  <acceptance_criteria>
    - grep `Stock Concentration` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns ZERO matches
    - grep `platform.stockConcentration` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns ZERO matches
    - grep `border-t.*border-\[var\(--color-border\)\]` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns ZERO matches (the only border-t in this file was the deleted footer)
    - grep `platform.name` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns at least 1 match (title preserved)
    - grep `platform.description` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns at least 1 match (description preserved)
    - grep `Selected` in `src/renderer/src/features/platform/components/PlatformCard.tsx` returns at least 1 match (badge preserved)
    - `npm run typecheck` exits 0
    - `npm run test` exits 0 (no regression)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Strip Stock concentration + Ready-to-proceed lines from PlatformSelector.tsx green box (D-13)</name>
  <files>src/renderer/src/features/platform/components/PlatformSelector.tsx</files>
  <read_first>
    - src/renderer/src/features/platform/components/PlatformSelector.tsx (existing 66 lines — see lines 50-63 for the green-box block, with lines 56-61 being the deletion targets)
    - .planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-PATTERNS.md §8 (file 8 — exact deletion lines + post-state shape)
    - .planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-CONTEXT.md §decisions D-13
  </read_first>
  <action>
    In `src/renderer/src/features/platform/components/PlatformSelector.tsx`, delete lines 56-61 entirely (the two `<p>` blocks):

    ```typescript
    <p className="text-sm text-green-700">
      Stock concentration: <strong>{selectedPlatform.stockConcentration}x</strong>
    </p>
    <p className="text-sm text-green-600 mt-2">
      Ready to proceed with reagent calculations.
    </p>
    ```

    Resulting selected-platform block (the green-bordered box) shrinks to:
    ```typescript
    {selectedPlatform && (
      <div className="p-4 bg-green-50 border border-green-200 rounded-lg">
        <h3 className="font-medium text-green-800 mb-2">
          Platform Selected: {selectedPlatform.name}
        </h3>
      </div>
    )}
    ```

    Note: the `mb-2` on the `<h3>` becomes visually redundant when there's no following sibling — leave it as-is per D-13 ("Keep the green-bordered box + Platform Selected: {name} heading"); no JSX restyling.

    PRESERVE byte-for-byte:
    - Lines 1-2: imports
    - Lines 4-31: loading / error / empty-state branches
    - Lines 33-48: the platform grid (`<h2>Select Platform</h2>` + `<PlatformCard>` map)
    - Lines 51-55: the `{selectedPlatform && (` opening + the green container `<div>` + the `<h3>Platform Selected: ...</h3>`
    - Line 63 (now): the closing `</div>` of the green container
    - Lines 64-65: the closing `)}` and outer fragment `</div>`

    DO NOT touch the import of `usePlatforms` or `PlatformCard` (lines 1-2). DO NOT modify the platform grid map. DO NOT remove `selectedPlatform` destructuring at line 5 — the heading still reads `selectedPlatform.name`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    PlatformSelector.tsx green box renders only the heading. TypeScript compiles. Full test suite green.
  </done>
  <acceptance_criteria>
    - grep `Stock concentration` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns ZERO matches (note: lowercase 's', matches the actual line text)
    - grep `Ready to proceed` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns ZERO matches
    - grep `selectedPlatform.stockConcentration` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns ZERO matches
    - grep `Platform Selected:` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns exactly 1 match (heading preserved per D-13)
    - grep `bg-green-50` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns exactly 1 match (green container preserved)
    - grep `selectedPlatform.name` in `src/renderer/src/features/platform/components/PlatformSelector.tsx` returns at least 1 match
    - `npm run typecheck` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer-internal | UI deletion only; no data flow change, no input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information disclosure | Stock concentration is not sensitive | accept | Stock concentration is a fixed platform attribute (visible in DB seed, log files, build artifacts); removing it from the UI is a UX simplification, not a confidentiality control |
| n/a — local Electron, no network surface in this phase | — | — | — | — |
</threat_model>

<verification>
- `grep -rn "Stock Concentration\|Stock concentration\|Ready to proceed" src/renderer/src/features/platform/` returns ZERO matches across both component files
- `npm run typecheck` exits 0
- `npm run test` exits 0 (no regression — these components have no dedicated tests; downstream consumers don't read the removed strings)
</verification>

<success_criteria>
1. `PlatformCard` renders Title + optional Selected badge + optional Description — no footer block, no `border-t` separator (D-12)
2. `PlatformSelector` green box renders only `Platform Selected: {name}` — no stock-conc line, no ready-to-proceed line (D-13)
3. `platform.stockConcentration` field remains in the `Platform` type and continues to be read by non-UI consumers (useCalculator.ts line 75)
4. Zero new tests required — deletion is verifiable via grep
</success_criteria>

<output>
After completion, create `.planning/phases/14-smoke-3-plate-page-input-expansion-ui-cleanup-inserted-2026-/14-03-SUMMARY.md` summarizing:
- Lines deleted from each component
- Grep verification results
- Any deviations from action with rationale
</output>
