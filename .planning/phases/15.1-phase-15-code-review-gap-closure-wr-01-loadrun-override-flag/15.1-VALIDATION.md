---
phase: 15.1
slug: phase-15-code-review-gap-closure-wr-01-loadrun-override-flag
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-12
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

Scope: tactical fix sweep for three Phase 15 code-review warnings (WR-01 / WR-02 / WR-06). No new requirements. All test infrastructure exists from Phase 15; only new test groups are added.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 1.x (Node-only, no jsdom — per VALIDATION precedent in Phase 15) |
| **Config file** | `vitest.config.ts` (existing) |
| **Quick run command** | `npm test -- --run <specific test file>` |
| **Full suite command** | `npm test -- --run` |
| **Estimated runtime** | ~3s full suite (421 tests as of Phase 15 close) |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --run <touched test file>` (per-task quick run)
- **After every plan wave:** Run `npm test -- --run` (full suite — catches cross-file regressions)
- **Before `/gsd-verify-work`:** Full suite must be green AND `npm run typecheck` must be green
- **Max feedback latency:** 10 seconds (the project's full suite is fast enough that per-task quick runs are unnecessary; planner may collapse to full-suite-after-every-task)

---

## Per-Task Verification Map

> Placeholder rows — planner fills in actual task IDs after the plan is written. Each WR fix has at least one RED-test commit + one GREEN-implementation commit per the TDD convention from Phase 15.

| Task ID | Plan | Wave | Requirement / Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|------------------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.1-01-01 | 01 | 1 | D-15.1-03/04 (WR-01) | — | Reopened run restores both override flags into calculatorStore + CalculatorForm local state; cap modal does NOT re-trigger on first input after reopen | unit + integration | `npm test -- --run src/renderer/src/stores/__tests__/runStore.loadRun.test.ts` (NEW file) | ❌ W0 | ⬜ pending |
| 15.1-01-02 | 01 | 1 | D-15.1-01/02 (WR-02) | — | When `getWithReagents` returns null OR throws, `buildRunSnapshot` returns a RunCreate WITHOUT `calculationRulesVersion` and WITHOUT the 10 snapshot fields; existing custom-assay path (no masterPanelId) STILL writes `calculationRulesVersion='smoke3'` with empty snapshot fields | unit | `npm test -- --run src/renderer/src/features/run/hooks/__tests__/useRunSnapshot.test.ts` | ✅ | ⬜ pending |
| 15.1-01-03 | 01 | 1 | D-15.1-05/06/07/08 (WR-06) | — | 3 new pure-helper functions in `auditTrail.ts` derive Raw bead/antibody volume, New beads/antibodies, Total bead/antibody volume from snapshot fields + calculator helpers; explicit `ceilToTenthML` application; mL→µL conversion at helper boundary; null inputs → null return (em-dash render preserved); over-subtraction clamps to 0 | unit | `npm test -- --run src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` | ✅ | ⬜ pending |
| 15.1-01-04 | 01 | 1 | Group P regression | — | All 6 derived rows render real values for the canonical PRD fixture (matches existing Group A 9.4mL fixture used by Phases 12/14/15); legacy pre-Phase-15 run (calculationRulesVersion=NULL) still renders em-dashes | integration | `npm test -- --run src/renderer/src/lib/__tests__/calculator.integration.test.ts` | ✅ | ⬜ pending |
| 15.1-01-05 | 01 | 1 | Final regression | — | Full suite green; typecheck green; no regression to Phase 12/14/15 test files | full | `npm test -- --run && npm run typecheck` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/renderer/src/stores/__tests__/runStore.loadRun.test.ts` — NEW file for WR-01 round-trip tests (covers store-side flag restoration). Existing `runStore.ts` has minimal test coverage in the current codebase; create per Pitfall 4 in 15.1-RESEARCH.md.

Wave-0 is exactly **one new test file**. Everything else extends existing files:
- `useRunSnapshot.test.ts` adds 2 new cases for WR-02 (null IPC + throw IPC)
- `auditTrail.test.ts` adds Group P (~5 cases for WR-06 derivations)
- `calculator.integration.test.ts` may add one integration row for WR-06 vs canonical fixture (optional — pure-helper tests in Group P should be sufficient)

*Test fixtures: `makeMockRunForJ` at `calculator.integration.test.ts:687-723` is directly reusable for WR-01 fixture per 15.1-RESEARCH.md "Test Fixture Inventory" section.*

---

## Manual-Only Verifications

| Behavior | Requirement / Decision | Why Manual | Test Instructions |
|----------|------------------------|------------|-------------------|
| Override badge visible on the form after reopen (UX correctness) | D-15.1-03/04 (WR-01) | `.tsx` render correctness is not vitest-verifiable on macOS (no jsdom) — same constraint as Phase 15's HUMAN-UAT items | Phase 16 UAT picks this up automatically: Section A reopen-and-verify-snapshot step now includes "form shows OVERRIDE chip after reopen" sub-bullet (per 15.1-CONTEXT.md `<code_context>` Integration Points). No new HUMAN-UAT row needed; Phase 16 covers it. |
| 6 audit-trail rows render real values (visual correctness) | D-15.1-05 (WR-06) | Same — `.tsx` render correctness | Phase 16 Section B SMK3-15 row already covers this; per 15.1-CONTEXT.md `<code_context>` Integration Points, Phase 16 plan updates SMK3-15 row to remove the "ignore the 6 em-dash rows" caveat once 15.1 ships. |
| WR-02 silent-skip warning appears in dev console | D-15.1-01 | console.warn output is observable only at runtime; vitest stubs `window.electronAPI` so the warn path is exercised but not visually inspected | Phase 16 UAT covers indirectly: if WR-02 fires during the operator's UAT save, the run will lack a snapshot but the operator wouldn't see the console. Document in CHANGELOG.md Known Issues. Manual dev verification is optional. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (Wave 0 = 1 new test file for WR-01)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (15.1 has 5 tasks, all with automated verify)
- [ ] Wave 0 covers all MISSING references (only `runStore.loadRun.test.ts` is missing)
- [ ] No watch-mode flags (`npm test -- --run` is one-shot, vitest default is also one-shot in CI)
- [ ] Feedback latency < 10s (full suite is ~3s)
- [ ] `nyquist_compliant: true` set in frontmatter — flip after planner approves the per-task verify map above

**Approval:** pending
