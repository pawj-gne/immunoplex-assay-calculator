---
status: partial
phase: 15.1-phase-15-code-review-gap-closure-wr-01-loadrun-override-flag
source: [15.1-VERIFICATION.md]
started: 2026-05-12T19:14:00Z
updated: 2026-05-12T19:14:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Reopen save-with-override run on Windows — OVERRIDE badge visible
expected: Amber 'overridden' chip renders next to the Old Beads / Old Antibodies numeric input immediately after the run is loaded; does NOT wait for user interaction.
result: [pending]

### 2. Reopen save-with-override run on Windows — cap modal does NOT re-trigger on first keystroke
expected: Cap modal does NOT re-trigger on the first keystroke into the Old Beads field; `beadsOverrideAccepted` local state already reflects `storeOldBeadsOverride=true` from the loadRun cascade.
result: [pending]

### 3. Reopen smoke3 run on Windows — 6 audit-trail rows render real mL values
expected: Raw bead volume / Raw antibody volume / New beads / New antibodies / Total bead volume / Total antibody volume each render as 'X.X mL' (0.1-mL ceiling); no '—' placeholders for these 6 rows when `beadsVolumePerWell` / `antibodiesVolumePerWell` are populated.
result: [pending]

### 4. End-to-end IPC-failure recovery — getWithReagents failure path
expected: Trigger IPC failure for `getWithReagents` (corrupt master-panel DB row or mock IPC). Save run with premix selected. Run saves successfully (no error toast / no save-blocked banner). Persisted row has `calculationRulesVersion=NULL`. Reopen: `HistoricalRunBanner` appears AND audit trail renders em-dashes for the master-panel snapshot fields.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
