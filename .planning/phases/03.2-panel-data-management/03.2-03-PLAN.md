---
phase: 03.2-panel-data-management
plan: 03
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - src/renderer/src/features/manage/PanelList.tsx
  - src/renderer/src/features/manage/PanelEditModal.tsx
  - src/renderer/src/features/manage/ManagePage.tsx
  - src/renderer/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "Operator can view panels and their analyte membership"
    - "Operator can edit panel names"
    - "Operator can add/remove analytes from panels"
    - "Operator can delete panels"
    - "ManagePage is accessible from app navigation"
  artifacts:
    - path: "src/renderer/src/features/manage/PanelList.tsx"
      provides: "Panel list with expandable analyte membership"
      min_lines: 60
    - path: "src/renderer/src/features/manage/PanelEditModal.tsx"
      provides: "Modal for editing panel name and managing analyte membership"
      min_lines: 50
  key_links:
    - from: "src/renderer/src/features/manage/PanelList.tsx"
      to: "electronAPI.panel"
      via: "window.electronAPI.panel.update/delete/addAnalyte/removeAnalyte"
      pattern: "electronAPI\\.panel\\.(update|delete|addAnalyte|removeAnalyte)"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/features/manage/ManagePage.tsx"
      via: "import and route/tab"
      pattern: "ManagePage"
---

<objective>
Build panel management UI and integrate ManagePage into app navigation.

Purpose: Operators need to manage panels (view membership, edit names, add/remove analytes, delete) and access management from the main app.
Output: PanelList, PanelEditModal components, and app navigation integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-panel-data-management/03.2-01-SUMMARY.md
@.planning/phases/03.2-panel-data-management/03.2-02-SUMMARY.md
@src/shared/types/panel.ts
@src/shared/types/analyte.ts
@src/renderer/src/App.tsx
@src/renderer/src/features/manage/ManagePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PanelList and PanelEditModal</name>
  <files>src/renderer/src/features/manage/PanelList.tsx, src/renderer/src/features/manage/PanelEditModal.tsx</files>
  <action>
PanelList.tsx:
- Props: { platformId: string, speciesId: string }
- Fetch panels via window.electronAPI.panel.getByPlatformAndSpecies(platformId, speciesId)
- For each panel, show name and a toggle to expand/collapse analyte list
- Expanded view: fetch analytes via window.electronAPI.panel.getWithAnalytes(panelId), show analyte names in a sub-list with "Remove" button next to each
- Remove calls window.electronAPI.panel.removeAnalyte(panelId, analyteId), then refetch
- Panel-level actions: Edit (opens PanelEditModal), Delete (window.confirm, then window.electronAPI.panel.delete(id))
- Show "No panels found" when empty
- Tailwind: card-style panels (border rounded-lg p-4 shadow-sm), bg-white

PanelEditModal.tsx:
- Props: { panel: PremixPanel, platformId: string, speciesId: string, onClose: () => void, onSaved: () => void }
- Modal overlay (same style as AnalyteEditModal)
- Form fields: name (text), description (text, optional)
- Section "Add Analyte to Panel": dropdown of all analytes for this platform/species (fetched via electronAPI.analyte.getByPlatformAndSpecies), filtered to exclude already-in-panel analytes. "Add" button calls window.electronAPI.panel.addAnalyte(panelId, analyteId).
- Save button calls window.electronAPI.panel.update({ id, name, description }), then onSaved()/onClose()
- Cancel button calls onClose()

Wire PanelList into ManagePage's "Panels" tab.
  </action>
  <verify>`npm run build` succeeds</verify>
  <done>Panel list displays with expandable analyte membership, edit modal allows name change and analyte add/remove, delete works with confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ManagePage into app navigation</name>
  <files>src/renderer/src/App.tsx</files>
  <action>
Read App.tsx to understand current navigation structure. Add a "Manage Data" navigation item/tab that renders ManagePage. This could be:
- If app uses tab-based navigation: add a new tab
- If app uses sidebar: add a nav link
- If app uses a simple layout: add a button/link in the header

Import ManagePage from features/manage/ManagePage. Use conditional rendering (useState for active page) or existing routing pattern.

Ensure the ManagePage is accessible alongside existing calculator and import features without disrupting them.
  </action>
  <verify>`npm run build` succeeds. App launches and "Manage Data" nav item is visible.</verify>
  <done>ManagePage accessible from main app navigation. All existing features still work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete panel data management: analyte table with edit/delete, panel list with membership management, integrated into app navigation</what-built>
  <how-to-verify>
    1. Launch app with `npm run dev`
    2. Navigate to "Manage Data"
    3. Select a platform and species that has imported data
    4. Analytes tab: verify table shows analytes. Edit one (change name), confirm it persists. Delete one, confirm it disappears.
    5. Panels tab: verify panels show. Expand one to see analytes. Remove an analyte from panel. Edit panel name. Delete a panel.
    6. Switch back to calculator -- verify it still works correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- App has "Manage Data" navigation
- Analyte CRUD works end-to-end
- Panel CRUD with membership management works end-to-end
- All 7 success criteria from phase goal met
</verification>

<success_criteria>
Full panel data management feature accessible from app navigation. Analytes: view, edit, delete. Panels: view, edit name, add/remove analytes, delete. All changes persist immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-panel-data-management/03.2-03-SUMMARY.md`
</output>
