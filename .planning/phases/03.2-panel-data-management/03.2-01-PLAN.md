---
phase: 03.2-panel-data-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/repositories/analyte.ts
  - src/main/db/repositories/panel.ts
  - src/shared/constants/channels.ts
  - src/main/ipc/analyte.ts
  - src/main/ipc/panel.ts
  - src/preload/index.ts
autonomous: true

must_haves:
  truths:
    - "Renderer can call update/delete on analytes via IPC"
    - "Renderer can call update/delete/addAnalyte/removeAnalyte on panels via IPC"
    - "Repository methods correctly persist changes to SQLite"
  artifacts:
    - path: "src/main/db/repositories/analyte.ts"
      provides: "update and delete methods"
      contains: "update|delete"
    - path: "src/main/db/repositories/panel.ts"
      provides: "update, delete, removeAnalyteFromPanel methods"
      contains: "update|delete|removeAnalyteFromPanel"
    - path: "src/shared/constants/channels.ts"
      provides: "ANALYTE_UPDATE, ANALYTE_DELETE, PANEL_UPDATE, PANEL_DELETE, PANEL_ADD_ANALYTE, PANEL_REMOVE_ANALYTE"
    - path: "src/preload/index.ts"
      provides: "analyte.update, analyte.delete, panel.update, panel.delete, panel.addAnalyte, panel.removeAnalyte"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/analyte.ts"
      via: "IPC channels"
      pattern: "ANALYTE_UPDATE|ANALYTE_DELETE"
    - from: "src/preload/index.ts"
      to: "src/main/ipc/panel.ts"
      via: "IPC channels"
      pattern: "PANEL_UPDATE|PANEL_DELETE|PANEL_ADD_ANALYTE|PANEL_REMOVE_ANALYTE"
---

<objective>
Add CRUD backend for analyte and panel management -- repository methods, IPC channels, IPC handlers, and preload bridge.

Purpose: Frontend needs update/delete operations exposed through the preload bridge to manage panel data.
Output: Complete backend layer for all management operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/db/repositories/analyte.ts
@src/main/db/repositories/panel.ts
@src/shared/constants/channels.ts
@src/main/ipc/analyte.ts
@src/main/ipc/panel.ts
@src/preload/index.ts
@src/shared/types/analyte.ts
@src/shared/types/panel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update/delete repository methods</name>
  <files>src/main/db/repositories/analyte.ts, src/main/db/repositories/panel.ts</files>
  <action>
Add to analyteRepository:
- `update(id: string, data: Partial<Pick<Analyte, 'name' | 'beadRegion' | 'premixConc' | 'singleConc'>>): Analyte` -- uses drizzle `set()` with `where(eq(analytes.id, id))`, sets updatedAt to now, returns updated record via getById
- `delete(id: string): void` -- first delete from panelAnalytes where analyteId matches (cascade), then delete from analytes where id matches

Add to panelRepository:
- `update(id: string, data: Partial<Pick<PremixPanel, 'name' | 'description'>>): PremixPanel` -- uses drizzle `set()` with `where(eq(premixPanels.id, id))`, sets updatedAt to now, returns updated record via getById
- `delete(id: string): void` -- first delete from panelAnalytes where panelId matches (cascade), then delete from premixPanels where id matches
- `removeAnalyteFromPanel(panelId: string, analyteId: string): void` -- delete from panelAnalytes where both panelId and analyteId match

Import AnalyteUpdate from shared types. Follow existing patterns (getDatabase(), .run() for mutations).
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>All five new repository methods exist and type-check</done>
</task>

<task type="auto">
  <name>Task 2: Add IPC channels, handlers, and preload bridge</name>
  <files>src/shared/constants/channels.ts, src/main/ipc/analyte.ts, src/main/ipc/panel.ts, src/preload/index.ts</files>
  <action>
1. In channels.ts, add to IPC_CHANNELS:
   - ANALYTE_UPDATE: 'analyte:update'
   - ANALYTE_DELETE: 'analyte:delete'
   - PANEL_UPDATE: 'panel:update'
   - PANEL_DELETE: 'panel:delete'
   - PANEL_ADD_ANALYTE: 'panel:add-analyte'
   - PANEL_REMOVE_ANALYTE: 'panel:remove-analyte'

2. In src/main/ipc/analyte.ts, add handlers following existing pattern (validate unknown args with typeof checks):
   - ANALYTE_UPDATE: expects (data: unknown) where data is AnalyteUpdate. Validate data is object with string id. Call analyteRepository.update(). Use Zod schema: z.object({ id: z.string(), name: z.string().optional(), beadRegion: z.number().optional(), premixConc: z.number().optional(), singleConc: z.number().optional() }).
   - ANALYTE_DELETE: expects (id: unknown). Validate string. Call analyteRepository.delete().

3. In src/main/ipc/panel.ts, add handlers:
   - PANEL_UPDATE: expects (data: unknown) where data is PremixPanelUpdate. Zod: z.object({ id: z.string(), name: z.string().optional(), description: z.string().nullable().optional() }). Call panelRepository.update().
   - PANEL_DELETE: expects (id: unknown). Validate string. Call panelRepository.delete().
   - PANEL_ADD_ANALYTE: expects (panelId: unknown, analyteId: unknown). Validate both strings. Call panelRepository.addAnalyteToPanel().
   - PANEL_REMOVE_ANALYTE: expects (panelId: unknown, analyteId: unknown). Validate both strings. Call panelRepository.removeAnalyteFromPanel().

4. In src/preload/index.ts, extend the electronAPI:
   - analyte: add update(data), delete(id)
   - panel: add update(data), delete(id), addAnalyte(panelId, analyteId), removeAnalyte(panelId, analyteId)
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit` and `npm run build` succeeds</verify>
  <done>All 6 new IPC channels registered, handlers validate input with Zod, preload bridge exposes all management operations</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- All 6 new channels appear in channels.ts
- Preload bridge has analyte.update, analyte.delete, panel.update, panel.delete, panel.addAnalyte, panel.removeAnalyte
</verification>

<success_criteria>
Complete backend layer for panel data management. All CRUD operations available through preload bridge. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-panel-data-management/03.2-01-SUMMARY.md`
</output>
