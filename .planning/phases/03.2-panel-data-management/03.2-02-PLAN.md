---
phase: 03.2-panel-data-management
plan: 02
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - src/renderer/src/features/manage/AnalyteTable.tsx
  - src/renderer/src/features/manage/AnalyteEditModal.tsx
  - src/renderer/src/features/manage/ManagePage.tsx
autonomous: true

must_haves:
  truths:
    - "Operator can view all analytes for selected platform/species in a table"
    - "Operator can edit analyte properties via modal"
    - "Operator can delete analytes with confirmation"
    - "Changes persist immediately in database"
  artifacts:
    - path: "src/renderer/src/features/manage/AnalyteTable.tsx"
      provides: "Table displaying analytes with edit/delete actions"
      min_lines: 50
    - path: "src/renderer/src/features/manage/AnalyteEditModal.tsx"
      provides: "Modal form for editing analyte fields"
      min_lines: 40
    - path: "src/renderer/src/features/manage/ManagePage.tsx"
      provides: "Container page with platform/species selection and tab navigation"
      min_lines: 40
  key_links:
    - from: "src/renderer/src/features/manage/AnalyteTable.tsx"
      to: "electronAPI.analyte"
      via: "window.electronAPI.analyte.update/delete"
      pattern: "electronAPI\\.analyte\\.(update|delete)"
---

<objective>
Build analyte management UI -- table view with inline edit (via modal) and delete, filtered by platform/species.

Purpose: Operators need to view and modify analyte data directly in the app.
Output: ManagePage with AnalyteTable and AnalyteEditModal components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-panel-data-management/03.2-01-SUMMARY.md
@src/shared/types/analyte.ts
@src/renderer/src/features/selection/
@src/renderer/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ManagePage container with platform/species filter</name>
  <files>src/renderer/src/features/manage/ManagePage.tsx</files>
  <action>
Create ManagePage.tsx as the top-level management container:
- Reuse the existing platform/species selection pattern from the selection feature (look at how selectionStore or existing components handle platform/species dropdowns)
- Two tabs: "Analytes" and "Panels" (use simple button-based tab switcher, Tailwind styling: border-b-2 border-blue-500 for active)
- When platform AND species are selected, load and display the active tab content
- Pass platformId and speciesId as props to child components
- Show "Select platform and species to manage data" placeholder when not selected
- Export as default for routing/import

Use Tailwind classes consistent with existing app. Standard React state (useState) for tab selection and platform/species -- no new store needed since this is local UI state. Fetch platforms via window.electronAPI.platform.getAll() and species via window.electronAPI.species.getByPlatformId(). Use useEffect for loading.
  </action>
  <verify>Component renders without errors when imported</verify>
  <done>ManagePage renders with platform/species dropdowns and tab switcher</done>
</task>

<task type="auto">
  <name>Task 2: AnalyteTable and AnalyteEditModal</name>
  <files>src/renderer/src/features/manage/AnalyteTable.tsx, src/renderer/src/features/manage/AnalyteEditModal.tsx</files>
  <action>
AnalyteTable.tsx:
- Props: { platformId: string, speciesId: string }
- Fetch analytes via window.electronAPI.analyte.getByPlatformAndSpecies(platformId, speciesId) in useEffect (refetch when props change)
- Render HTML table with columns: Name, Bead Region, Premix Conc, Single Conc, Actions
- Actions column: Edit button (pencil icon or "Edit" text) and Delete button (red text "Delete")
- Edit opens AnalyteEditModal with selected analyte
- Delete shows window.confirm("Delete analyte {name}?"), on confirm calls window.electronAPI.analyte.delete(id), then refetch list
- Show "No analytes found" when empty
- Tailwind: table-auto w-full, alternating row colors (even:bg-gray-50), hover:bg-gray-100

AnalyteEditModal.tsx:
- Props: { analyte: Analyte, onClose: () => void, onSaved: () => void }
- Modal overlay (fixed inset-0, bg-black/50, centered card)
- Form with fields: name (text), beadRegion (number), premixConc (number), singleConc (number)
- Pre-populated with current analyte values
- Save button calls window.electronAPI.analyte.update({ id, ...formData }), then calls onSaved() and onClose()
- Cancel button calls onClose()
- Use useState for form fields, standard form onSubmit

Wire AnalyteTable into ManagePage's "Analytes" tab.
  </action>
  <verify>`npm run build` succeeds</verify>
  <done>Analyte table displays data, edit modal saves changes, delete removes analytes, all persisted via IPC</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- ManagePage renders with platform/species selection
- AnalyteTable loads and displays analytes
- Edit modal opens, saves changes
- Delete confirms and removes analyte
</verification>

<success_criteria>
Operators can view, edit, and delete analytes in a table UI filtered by platform/species. Changes persist immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-panel-data-management/03.2-02-SUMMARY.md`
</output>
