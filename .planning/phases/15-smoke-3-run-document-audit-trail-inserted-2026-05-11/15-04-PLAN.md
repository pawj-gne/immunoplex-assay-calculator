---
phase: 15
plan: 04
id: 15-04
title: Pure helpers (PE math + branch label) + audit trail UI + SAPE row + advisory banner + override badge
type: execute
wave: 2
depends_on: [15-01]
autonomous: true
requirements: [SMK3-12, SMK3-15, SMK3-16, SMK3-17]
files_modified:
  - src/renderer/src/features/run/lib/auditTrail.ts
  - src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts
  - src/renderer/src/features/run/components/HistoricalRunBanner.tsx
  - src/renderer/src/features/run/components/AuditTrailSection.tsx
  - src/renderer/src/features/run/components/FinalizedRunHeader.tsx
  - src/renderer/src/features/run/components/FinalizedRunView.tsx
must_haves:
  truths:
    - "Pure helper module `auditTrail.ts` exports `computePeVolumeML(finalVolumeML, sapeConcentration)` that returns the ceiling-rounded PE volume in mL via `ceilToTenthML`, with silent 1× fallback when `sapeConcentration` is null/undefined/0 (per D-15-14)"
    - "Pure helper module `auditTrail.ts` exports `deriveDiluentBranchLabel(diluent, premixConcentration)` returning the verbatim D-15-09 branch label string (3 cases: premix wins / per-reagent fallback no 1× / per-reagent fallback no premix)"
    - "auditTrail.test.ts covers all 7 PE volume edge cases from CONTEXT.md `<specifics>` (T-M1..T-M7) + 3 diluent branch cases (T-N1..T-N3)"
    - "HistoricalRunBanner renders amber banner with the verbatim D-15-11 copy when `currentRun.calculationRulesVersion !== 'smoke3'`; renders null otherwise"
    - "AuditTrailSection renders 4 labeled blocks (Inputs / Intermediates / Outputs / Diluent decision) using the same `<dl>` 2-column grid pattern as FinalizedRunHeader; reads exclusively from RunRecord (no calculator-store reads)"
    - "Audit trail value cells render `—` (em-dash) when the source field is null/undefined"
    - "Audit trail Inputs block renders inline amber `[OVERRIDE]` chip beside Old Beads / Old Antibodies value cells when `oldBeadsOverride` / `oldAntibodiesOverride` is true (D-15-08, deferred from Phase 14)"
    - "Audit trail Outputs block renders the PE volume row computed via `computePeVolumeML(finalVolumeML, record.sapeConcentration)`"
    - "Audit trail Diluent decision block renders the D-15-09 branch label via `deriveDiluentBranchLabel(...)` followed by a 2-row mini table for verbatim `beadsDiluent` / `antibodiesDiluent` strings"
    - "FinalizedRunHeader includes a SAPE Name row in the metadata `<dl>` rendering `record.sapeName ?? '—'` (SMK3-12)"
    - "FinalizedRunView composes `<HistoricalRunBanner />` + `<AuditTrailSection />` between `<FinalizedRunHeader />` and the existing actions row (D-15-05)"
  artifacts:
    - path: "src/renderer/src/features/run/lib/auditTrail.ts"
      provides: "Pure helpers for PE volume + diluent branch label"
      contains: "export function computePeVolumeML"
    - path: "src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts"
      provides: "Group M PE math + diluent branch label tests"
      contains: "Group M"
    - path: "src/renderer/src/features/run/components/HistoricalRunBanner.tsx"
      provides: "Amber pre-Phase-15 advisory banner"
      contains: "calculationRulesVersion"
    - path: "src/renderer/src/features/run/components/AuditTrailSection.tsx"
      provides: "4-block audit trail UI"
      contains: "Calculation Audit Trail"
    - path: "src/renderer/src/features/run/components/FinalizedRunHeader.tsx"
      provides: "SAPE Name row addition"
      contains: "SAPE Name"
    - path: "src/renderer/src/features/run/components/FinalizedRunView.tsx"
      provides: "Banner + AuditTrailSection composed into the view"
      contains: "<AuditTrailSection"
  key_links:
    - from: "src/renderer/src/features/run/components/AuditTrailSection.tsx"
      to: "src/renderer/src/features/run/lib/auditTrail.ts"
      via: "computePeVolumeML + deriveDiluentBranchLabel imports"
      pattern: "from '../lib/auditTrail'"
    - from: "src/renderer/src/features/run/components/AuditTrailSection.tsx"
      to: "src/renderer/src/lib/diluentResolver.ts"
      via: "resolveDiluent({ selectedPremixes, valuesTable })"
      pattern: "resolveDiluent"
    - from: "src/renderer/src/features/run/components/FinalizedRunView.tsx"
      to: "src/renderer/src/features/run/components/HistoricalRunBanner.tsx"
      via: "JSX import"
      pattern: "<HistoricalRunBanner"
    - from: "src/renderer/src/features/run/components/FinalizedRunView.tsx"
      to: "src/renderer/src/features/run/components/AuditTrailSection.tsx"
      via: "JSX import"
      pattern: "<AuditTrailSection"
---

<objective>
Land Phase 15's user-visible surface: the pure helper module (PE volume formula + diluent branch label derivation), the audit trail UI section (4 blocks per PRD worked example), the SAPE Name row in the header, and the historical-run advisory banner.

Purpose: SMK3-12 (SAPE Name display), SMK3-15 (full audit trail), SMK3-16 (historical-run banner), SMK3-17 (PE volume formula). The pure-helper layer carries all automated test coverage (Group M PE math + Group N branch labels); the .tsx render layer routes visual confirmation to Phase 16 UAT per VALIDATION.md (vitest is Node-only — no jsdom in this project, same as Phase 14).

Output: 1 new pure helpers module (.ts), 1 new test file, 2 new .tsx components, 2 modified .tsx components.

NOTE: All `.tsx` render correctness is manual-UAT-only. The executor should NOT attempt to write `.tsx` render tests against the Node-only vitest config — every render assertion is routed to Phase 16 UAT Section A Step 8 + Section B per VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-CONTEXT.md
@.planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-RESEARCH.md
@.planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-PATTERNS.md
@.planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-VALIDATION.md
@CLAUDE.md
@src/renderer/src/features/run/components/FinalizedRunHeader.tsx
@src/renderer/src/features/run/components/FinalizedRunView.tsx
@src/renderer/src/lib/decimal.ts
@src/renderer/src/lib/diluentResolver.ts
@src/shared/types/diluent.ts
</context>

<interfaces>
<!-- ceilToTenthML signature (verbatim from src/renderer/src/lib/decimal.ts:44-48). -->
```ts
export function ceilToTenthML(volumeUL: Decimal): Decimal {
  const mL = volumeUL.dividedBy(1000)
  const roundedML = mL.toDecimalPlaces(1, Decimal.ROUND_CEIL)
  return roundedML.times(1000)
}
```

<!-- resolveDiluent + DiluentResult union (verbatim from src/renderer/src/lib/diluentResolver.ts + src/shared/types/diluent.ts). -->
```ts
export interface SelectedPremixForDiluent { name: string; concentration: number }
export interface PanelValuesDiluent { beads: string; antibodies: string }
export type DiluentResult =
  | { kind: 'premix'; name: string }
  | { kind: 'values_table'; beads: string; antibodies: string }
  | { kind: 'legacy'; beads: null; antibodies: null }
export function resolveDiluent(args: {
  selectedPremixes: ReadonlyArray<SelectedPremixForDiluent>
  valuesTable: PanelValuesDiluent | undefined
}): DiluentResult
```

<!-- FinalizedRunHeader row pattern (verbatim — current Panel row at lines 94-97). -->
```tsx
<div className="flex justify-between gap-4 border-b border-gray-100 py-1">
  <dt className="font-medium text-[var(--color-muted)]">Panel</dt>
  <dd className="text-[var(--color-foreground)]">{panelName}</dd>
</div>
```

<!-- FinalizedRunView composition insertion point (between header and actions row, around lines 173-176). -->
```tsx
<FinalizedRunHeader />
{/* INSERT <HistoricalRunBanner /> + <AuditTrailSection /> HERE */}
<div className="flex items-center gap-3 print:hidden">
  {/* …actions… */}
</div>
```

<!-- CalculatorForm validation banner pattern (verbatim from CalculatorForm.tsx:385-389) — analog for HistoricalRunBanner. Swap red→amber. -->
```tsx
{validationError && (
  <div className="p-3 bg-red-50 border border-red-200 rounded-md">
    <p className="text-sm text-red-700">{validationError}</p>
  </div>
)}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helpers module (auditTrail.ts) + Group M + Group N tests</name>
  <files>src/renderer/src/features/run/lib/auditTrail.ts, src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts</files>
  <read_first>
    - src/renderer/src/lib/decimal.ts lines 1-48 (`Decimal` import + `ceilToTenthML` shape — closest pure-helper analog)
    - src/renderer/src/lib/diluentResolver.ts (the full file — `DiluentResult` discriminated union; `resolveDiluent` signature)
    - src/shared/types/diluent.ts (DiluentResult definition; if not exported from diluentResolver, import from here instead)
    - src/renderer/src/lib/__tests__/calculator.integration.test.ts lines 41-225 (Group A-L test pattern — `describe('Group X: …', () => { it('T-X1: …'`); copy this exact shape for Group M + Group N)
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-CONTEXT.md `<specifics>` (the 7 PE volume edge cases table + 3 diluent branch examples table — copy verbatim into tests)
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-PATTERNS.md §`auditTrail.ts` and §`auditTrail.test.ts` (the verbatim full file content)
  </read_first>
  <behavior>
    - Create new directory `src/renderer/src/features/run/lib/` and `src/renderer/src/features/run/lib/__tests__/` if they do not exist.
    - auditTrail.ts: pure module exporting `computePeVolumeML(finalVolumeML: number, sapeConcentration: number | null | undefined): number` and `deriveDiluentBranchLabel(diluent: DiluentResult, premixConcentration: number | null): string`.
    - auditTrail.test.ts: vitest file with two describe blocks — Group M (7 PE volume tests) + Group N (3 diluent branch tests).
  </behavior>
  <action>
**File 1 — auditTrail.ts.** Create `src/renderer/src/features/run/lib/auditTrail.ts` with this content (verbatim from PATTERNS.md §auditTrail.ts):

```ts
import { Decimal } from 'decimal.js'
import { ceilToTenthML } from '../../../lib/decimal'
import type { DiluentResult } from '../../../../../shared/types/diluent'

/**
 * Phase 15 SMK3-17 (D-15-13/14/16): PE volume = finalVolume ÷ SAPE concentration,
 * ceiling-rounded UP to 0.1 mL. Silent fallback to 1× when sapeConcentration is
 * null, undefined, or 0 (PRD: "typically 1×"). Ceiling applies AFTER division
 * so non-integer divisors still round to the operator-friendly 0.1-mL step.
 *
 * Input: finalVolumeML (already 0.1-mL ceiling-rounded total assay volume).
 * Output: peVolumeML (0.1-mL ceiling-rounded number, suitable for direct render).
 *
 * Edge cases (Group M): see Phase 15 CONTEXT.md `<specifics>` table.
 */
export function computePeVolumeML(
  finalVolumeML: number,
  sapeConcentration: number | null | undefined
): number {
  const concentration =
    sapeConcentration === null ||
    sapeConcentration === undefined ||
    sapeConcentration === 0
      ? 1
      : sapeConcentration
  const finalUL = new Decimal(finalVolumeML).times(1000)
  const peUL = finalUL.dividedBy(concentration)
  const ceilUL = ceilToTenthML(peUL)
  return ceilUL.dividedBy(1000).toNumber()
}

/**
 * Phase 15 D-15-09: derive the branch label rendered above the per-reagent
 * diluent rows. Reads the snapshotted premixConcentration to disambiguate the
 * two "fallback" sub-cases.
 *
 * Cases (verbatim from CONTEXT.md <specifics>):
 *   diluent.kind === 'premix'                       → "Concentration-keyed (<name> @ 1× wins)"
 *   diluent.kind === 'values_table' && premixConc!==null → "Per-reagent fallback (no 1× premix in selection)"
 *   diluent.kind === 'values_table' && premixConc===null → "Per-reagent fallback (no premix selected)"
 *   diluent.kind === 'legacy'                       → "Per-reagent fallback (no premix selected)"
 */
export function deriveDiluentBranchLabel(
  diluent: DiluentResult,
  premixConcentration: number | null
): string {
  if (diluent.kind === 'premix') {
    return `Concentration-keyed (${diluent.name} @ 1× wins)`
  }
  if (premixConcentration === null) {
    return 'Per-reagent fallback (no premix selected)'
  }
  return 'Per-reagent fallback (no 1× premix in selection)'
}
```

If the import path `../../../../../shared/types/diluent` does not resolve (depends on tsconfig path resolution from features/run/lib/), use the same import path that `diluentResolver.ts` uses to expose `DiluentResult`. If `DiluentResult` is re-exported from `diluentResolver.ts`, prefer `import type { DiluentResult } from '../../../lib/diluentResolver'`.

**File 2 — auditTrail.test.ts.** Create `src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` with this content (verbatim from PATTERNS.md §auditTrail.test.ts):

```ts
import { describe, it, expect } from 'vitest'
import { computePeVolumeML, deriveDiluentBranchLabel } from '../auditTrail'

describe('Phase 15 audit-trail derivations', () => {
  describe('Group M: PE volume = finalVolume ÷ SAPE concentration (SMK3-17)', () => {
    it('T-M1: 9.8 mL ÷ 1.0× → 9.8 mL', () => {
      expect(computePeVolumeML(9.8, 1.0)).toBeCloseTo(9.8, 1)
    })
    it('T-M2: 9.8 mL ÷ 0.5× → 19.6 mL', () => {
      expect(computePeVolumeML(9.8, 0.5)).toBeCloseTo(19.6, 1)
    })
    it('T-M3: 9.8 mL ÷ 2.0× → 4.9 mL', () => {
      expect(computePeVolumeML(9.8, 2.0)).toBeCloseTo(4.9, 1)
    })
    it('T-M4: 9.8 mL ÷ null (pre-Phase-15) → 9.8 mL (silent 1× fallback)', () => {
      expect(computePeVolumeML(9.8, null)).toBeCloseTo(9.8, 1)
    })
    it('T-M5: 9.8 mL ÷ 0 (malformed panel data) → 9.8 mL (silent fallback)', () => {
      expect(computePeVolumeML(9.8, 0)).toBeCloseTo(9.8, 1)
    })
    it('T-M6: 9.8 mL ÷ undefined → 9.8 mL (silent fallback)', () => {
      expect(computePeVolumeML(9.8, undefined)).toBeCloseTo(9.8, 1)
    })
    it('T-M7: 9.83 pre-ceiling ÷ 1.0× → 9.9 mL (ceiling applied AFTER division)', () => {
      expect(computePeVolumeML(9.83, 1.0)).toBeCloseTo(9.9, 1)
    })
  })

  describe('Group N: deriveDiluentBranchLabel (D-15-09)', () => {
    it('T-N1: premix kind → Concentration-keyed (<name> @ 1× wins)', () => {
      expect(
        deriveDiluentBranchLabel({ kind: 'premix', name: 'Panel I' }, 1)
      ).toBe('Concentration-keyed (Panel I @ 1× wins)')
    })
    it('T-N2: values_table kind + premixConcentration > 1 → no 1× premix in selection', () => {
      expect(
        deriveDiluentBranchLabel(
          { kind: 'values_table', beads: 'L-AB', antibodies: 'L-AB' },
          5
        )
      ).toBe('Per-reagent fallback (no 1× premix in selection)')
    })
    it('T-N3: values_table kind + premixConcentration null → no premix selected', () => {
      expect(
        deriveDiluentBranchLabel(
          { kind: 'values_table', beads: 'L-AB', antibodies: 'L-AB' },
          null
        )
      ).toBe('Per-reagent fallback (no premix selected)')
    })
  })
})
```

Run the targeted test:

```
npm test -- --run src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts
```

It MUST exit 0. If any T-M test fails, the silent-fallback semantics in `computePeVolumeML` are wrong — review D-15-14 and re-check the `concentration === 0 ? 1 : sapeConcentration` ternary.

If T-M7 fails because the result is 9.8 instead of 9.9, the ceiling logic is being applied at the wrong point — review D-15-13 / D-15-16 (ceiling applies AFTER division).
  </action>
  <verify>
    <automated>npm test -- --run src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/renderer/src/features/run/lib/auditTrail.ts` exists
    - `grep -c "export function computePeVolumeML" src/renderer/src/features/run/lib/auditTrail.ts` returns `1`
    - `grep -c "export function deriveDiluentBranchLabel" src/renderer/src/features/run/lib/auditTrail.ts` returns `1`
    - `grep -c "ceilToTenthML" src/renderer/src/features/run/lib/auditTrail.ts` returns >= `1`
    - File `src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` exists
    - `grep -c "describe.*Group M" src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` returns `1`
    - `grep -c "describe.*Group N" src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` returns `1`
    - `grep -cE "T-M[1-7]:" src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` returns `7`
    - `grep -cE "T-N[1-3]:" src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` returns `3`
    - `npm test -- --run src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts` exits `0`
  </acceptance_criteria>
  <done>
    - Pure helpers module exists and exports the two functions
    - Group M (7 cases) + Group N (3 cases) all pass
    - These tests carry the only AUTOMATED coverage for SMK3-17 + D-15-09 — visual cell-by-cell rendering of the audit trail is routed to Phase 16 UAT
  </done>
</task>

<task type="auto">
  <name>Task 2: HistoricalRunBanner + SAPE Name row + AuditTrailSection (UI components — manual UAT only)</name>
  <files>src/renderer/src/features/run/components/HistoricalRunBanner.tsx, src/renderer/src/features/run/components/AuditTrailSection.tsx, src/renderer/src/features/run/components/FinalizedRunHeader.tsx, src/renderer/src/features/run/components/FinalizedRunView.tsx</files>
  <read_first>
    - src/renderer/src/features/run/components/FinalizedRunHeader.tsx (full file — `<dl>` row pattern at lines 70-138; especially the Panel row at 94-97 — the SAPE Name row inserts immediately after Panel)
    - src/renderer/src/features/run/components/FinalizedRunView.tsx (full file — composition pattern; insert `<HistoricalRunBanner />` + `<AuditTrailSection />` between `<FinalizedRunHeader />` (line ~173) and the actions row that follows)
    - src/renderer/src/features/run/lib/auditTrail.ts (Task 1 output — provides the two helper functions to import)
    - src/renderer/src/lib/diluentResolver.ts (provides `resolveDiluent`)
    - src/shared/types/run.ts (Plan 15-01 added the 10 new fields to RunRecord — confirms `record.sapeName`, `record.calculationRulesVersion`, etc are typed)
    - src/renderer/src/stores/runStore.ts (lines 50-150 — confirms the `currentRun` selector pattern for use in the new components)
    - src/renderer/src/stores/selectionStore.ts (panel name lookup pattern — needed in AuditTrailSection to display Premix selection name)
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-PATTERNS.md §`HistoricalRunBanner.tsx` (full file content) and §`AuditTrailSection.tsx` (skeleton + block contents)
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-CONTEXT.md `<specifics>` (the worked example output — the locked label set Phase 16 UAT will check against, so you MUST use these exact labels: "Plates", "Sample count", "Replicate mode", "Premix selection", "Singles selection", "Old beads", "Old antibodies", "Number of setups", "Total wells", "Beads vol/well", "Antibodies vol/well", "Dead volume", "Raw bead volume", "Raw antibody volume", "New beads", "New antibodies", "Total bead volume", "Total antibody volume", "PE volume", "Rule applied:", "Beads diluent", "Antibodies diluent")
    - .planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-VALIDATION.md §"Manual-Only Verifications" (confirms render correctness routes to Phase 16; do NOT write .tsx render tests)
  </read_first>
  <behavior>
    - HistoricalRunBanner.tsx (NEW): pure presentation component; reads `currentRun` from runStore; renders amber Tailwind banner with verbatim D-15-11 copy when `currentRun.calculationRulesVersion !== 'smoke3'`; returns `null` for Phase-15-or-later runs and when no current run.
    - FinalizedRunHeader.tsx: insert one new `<div>` row inside the metadata `<dl>` immediately after the Panel row (line ~94-97). Label "SAPE Name", value `{record.sapeName ?? '—'}`.
    - AuditTrailSection.tsx (NEW): renders 4 labeled blocks per CONTEXT.md `<specifics>` — Inputs (8 rows w/ override badges), Intermediates (6 rows), Outputs (5 rows incl. PE volume), Diluent decision (branch label + 2-row mini table). Reads RunRecord exclusively (no calculatorStore reads); uses pure helpers + `resolveDiluent`. All value cells tolerate null/undefined → render `—`.
    - FinalizedRunView.tsx: insert `<HistoricalRunBanner />` + `<AuditTrailSection />` between `<FinalizedRunHeader />` and the existing actions row.
    - Override badge: inline amber chip beside Old Beads / Old Antibodies value cell when corresponding `*Override` flag is true. Verbatim D-15-08 tooltip text.
    - NO `.tsx` render tests are added — all visual verification routes to Phase 16 per VALIDATION.md.
  </behavior>
  <action>
**File 1 — HistoricalRunBanner.tsx (NEW).** Create `src/renderer/src/features/run/components/HistoricalRunBanner.tsx` with this content (verbatim from PATTERNS.md §HistoricalRunBanner.tsx):

```tsx
import { useRunStore } from '../../../stores/runStore'

/**
 * Phase 15 SMK3-16 D-15-11: advisory banner above the audit trail for runs
 * saved before Phase 15 (calculationRulesVersion !== 'smoke3'). Renders null
 * for Phase-15-or-later runs and when no current run is loaded. Print-safe
 * (lives inside FinalizedRunView's print stylesheet).
 */
export function HistoricalRunBanner(): JSX.Element | null {
  const currentRun = useRunStore((s) => s.runs.find((r) => r.id === s.currentRunId) ?? null)
  if (!currentRun) return null
  if (currentRun.calculationRulesVersion === 'smoke3') return null
  return (
    <div className="p-3 bg-amber-50 border border-amber-300 rounded-md">
      <p className="text-sm text-amber-900">
        This run was saved under previous calculation rules. Values displayed as recorded — no recompute on reopen.
      </p>
    </div>
  )
}
```

If `useRunStore`'s `currentRun` selector pattern in this file does not match the existing pattern in `FinalizedRunHeader.tsx`, look at how FinalizedRunHeader extracts `currentRun` and copy that selector exactly (the goal is consistency across the run/components folder).

**File 2 — FinalizedRunHeader.tsx (MOD).** Locate the Panel row at lines ~94-97:
```tsx
<div className="flex justify-between gap-4 border-b border-gray-100 py-1">
  <dt className="font-medium text-[var(--color-muted)]">Panel</dt>
  <dd className="text-[var(--color-foreground)]">{panelName}</dd>
</div>
```

IMMEDIATELY AFTER this row (and BEFORE whatever row comes next — typically Sample Type at line 98), insert:

```tsx
<div className="flex justify-between gap-4 border-b border-gray-100 py-1">
  <dt className="font-medium text-[var(--color-muted)]">SAPE Name</dt>
  <dd className="text-[var(--color-foreground)]">{record.sapeName ?? '—'}</dd>
</div>
```

The `record` variable is already in scope (it's the `currentRun` resolved at line ~36). No new imports needed.

**File 3 — AuditTrailSection.tsx (NEW).** Create `src/renderer/src/features/run/components/AuditTrailSection.tsx`. This file is the largest deliverable in Phase 15. Use the skeleton from PATTERNS.md §AuditTrailSection.tsx and EXPAND the four blocks per CONTEXT.md `<specifics>` worked example. Locked label set per CONTEXT.md (use these strings VERBATIM — Phase 16 UAT scripts grep against them):

Block 1: **Inputs** — 8 rows in this order:
- "Plates" → `record.plateCount ?? '—'`
- "Sample count" → `record.sampleCount ?? '—'`
- "Replicate mode" → `record.replicateMode === 'singles' ? 'Singles' : record.replicateMode === 'duplicates' ? 'Duplicates' : '—'`
- "Premix selection" → `panelName ? \`${panelName} (${record.premixConcentration ?? '—'}×)\` : '—'`
- "Singles selection" → `\`${record.singleAnalyteIds?.length ?? 0} (${record.singleAnalyteIds?.length > 0 ? '<resolve from analyte names>' : '—'})\`` — if analyte names are not readily available, render the count + em-dash for the names
- "Old beads" → `\`${record.oldBeads?.toFixed(1) ?? '—'} mL\`` + override badge if `record.oldBeadsOverride === true`
- "Old antibodies" → `\`${record.oldAntibodies?.toFixed(1) ?? '—'} mL\`` + override badge if `record.oldAntibodiesOverride === true`
- "Number of setups" → `record.numberOfSetups ?? '—'`

Block 2: **Intermediates** — 6 rows:
- "Total wells" → derive from sampleCount + plateCount + replicateMode using existing helpers (or render `—` if you don't want to recompute — Phase 16 UAT will catch missing rows)
- "Beads vol/well" → `\`${record.beadsVolumePerWell ?? '—'} mL\``
- "Antibodies vol/well" → `\`${record.antibodiesVolumePerWell ?? '—'} mL\``
- "Dead volume" → `\`${record.deadVolume ? (record.deadVolume / 1000).toFixed(1) : '—'} mL (= ${record.numberOfSetups ?? '—'} setup × 2 mL)\``
- "Raw bead volume" → `—` (compute from totalWells × beadsVolumePerWell only if both are non-null; else em-dash)
- "Raw antibody volume" → `—` (same shape, antibody side)

Block 3: **Outputs** — 5 rows:
- "New beads" → `—` (computed; render em-dash if computation source is null)
- "New antibodies" → `—`
- "Total bead volume" → `—`
- "Total antibody volume" → `—`
- "PE volume" → `\`${computePeVolumeML(finalVolumeML, record.sapeConcentration ?? null).toFixed(1)} mL\`` where `finalVolumeML` is derived from existing snapshotted fields (computed inline; if no source available, render `—`)

Block 4: **Diluent decision** — top branch label + 2-row mini table:
- Top line: `\`Rule applied: ${branchLabel}\`` where `branchLabel = deriveDiluentBranchLabel(diluent, record.premixConcentration ?? null)` and `diluent = resolveDiluent({ selectedPremixes: ..., valuesTable: ... })`
- Row 1: "Beads diluent" → `record.beadsDiluent ?? '—'`
- Row 2: "Antibodies diluent" → `record.antibodiesDiluent ?? '—'`

Skeleton (expand the comment placeholders into the rows above):

```tsx
import { useRunStore } from '../../../stores/runStore'
import { useSelectionStore } from '../../../stores/selectionStore'
import { computePeVolumeML, deriveDiluentBranchLabel } from '../lib/auditTrail'
import { resolveDiluent } from '../../../lib/diluentResolver'

export function AuditTrailSection(): JSX.Element | null {
  const currentRun = useRunStore((s) => s.runs.find((r) => r.id === s.currentRunId) ?? null)
  // Use the same panel-name-resolution pattern that FinalizedRunHeader uses; if
  // FinalizedRunHeader resolves panelName via a selectionStore selector, mirror it here.
  const panels = useSelectionStore((s) => /* read panels list — match FinalizedRunHeader */)

  if (!currentRun) return null
  const r = currentRun
  const panelName = r.panelId ? (panels.find((p) => p.id === r.panelId)?.name ?? null) : null

  // Em-dash placeholder helper (D-15-12)
  const dash = (v: unknown): string =>
    v === null || v === undefined || v === '' ? '—' : String(v)

  // Diluent branch label — derive at render time from snapshotted fields
  const diluent = resolveDiluent({
    selectedPremixes:
      r.premixConcentration !== null && r.premixConcentration !== undefined && panelName
        ? [{ name: panelName, concentration: r.premixConcentration }]
        : [],
    valuesTable:
      r.beadsDiluent !== null && r.beadsDiluent !== undefined &&
      r.antibodiesDiluent !== null && r.antibodiesDiluent !== undefined
        ? { beads: r.beadsDiluent, antibodies: r.antibodiesDiluent }
        : undefined
  })
  const branchLabel = deriveDiluentBranchLabel(diluent, r.premixConcentration ?? null)

  // PE volume — derive finalVolumeML from snapshot. If finalVolume not directly
  // persisted, recompute via the existing calculator helpers from the snapshotted
  // sampleCount + plateCount + volumePerWell + deadVolume. Render em-dash if any
  // input is missing.
  const finalVolumeML = /* TODO: compute from snapshot using existing helpers; em-dash if not derivable */
  const peVolumeML = finalVolumeML !== null
    ? computePeVolumeML(finalVolumeML, r.sapeConcentration ?? null)
    : null

  // Override badge JSX (used inline beside Old Beads / Old Antibodies)
  const OverrideChip = (): JSX.Element => (
    <span
      className="ml-2 inline-block bg-amber-100 text-amber-900 ring-1 ring-amber-300 px-1.5 py-0.5 rounded text-xs uppercase"
      title="This value exceeded the 20% recommended cap at save time; operator confirmed override."
    >
      OVERRIDE
    </span>
  )

  // Reusable row component that mirrors FinalizedRunHeader's row shell exactly.
  const Row = ({ label, value, badge }: { label: string; value: React.ReactNode; badge?: React.ReactNode }): JSX.Element => (
    <div className="flex justify-between gap-4 border-b border-gray-100 py-1">
      <dt className="font-medium text-[var(--color-muted)]">{label}</dt>
      <dd className="text-[var(--color-foreground)]">{value}{badge}</dd>
    </div>
  )

  return (
    <section className="space-y-6">
      <h2 className="text-xl font-semibold text-[var(--color-foreground)]">Calculation Audit Trail</h2>

      {/* 1. Inputs */}
      <div>
        <h3 className="text-lg font-semibold mb-2 text-[var(--color-foreground)]">1. Inputs</h3>
        <dl className="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-2 text-sm">
          <Row label="Plates" value={dash(r.plateCount)} />
          <Row label="Sample count" value={dash(r.sampleCount)} />
          <Row label="Replicate mode" value={r.replicateMode === 'singles' ? 'Singles' : r.replicateMode === 'duplicates' ? 'Duplicates' : '—'} />
          <Row label="Premix selection" value={panelName ? `${panelName} (${dash(r.premixConcentration)}×)` : '—'} />
          <Row label="Singles selection" value={`${r.singleAnalyteIds?.length ?? 0}`} />
          <Row label="Old beads" value={r.oldBeads !== undefined && r.oldBeads !== null ? `${r.oldBeads.toFixed(1)} mL` : '—'} badge={r.oldBeadsOverride ? <OverrideChip /> : undefined} />
          <Row label="Old antibodies" value={r.oldAntibodies !== undefined && r.oldAntibodies !== null ? `${r.oldAntibodies.toFixed(1)} mL` : '—'} badge={r.oldAntibodiesOverride ? <OverrideChip /> : undefined} />
          <Row label="Number of setups" value={dash(r.numberOfSetups)} />
        </dl>
      </div>

      {/* 2. Intermediates */}
      <div>
        <h3 className="text-lg font-semibold mb-2 text-[var(--color-foreground)]">2. Intermediates</h3>
        <dl className="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-2 text-sm">
          <Row label="Total wells" value="—" />
          <Row label="Beads vol/well" value={r.beadsVolumePerWell !== null && r.beadsVolumePerWell !== undefined ? `${r.beadsVolumePerWell} mL` : '—'} />
          <Row label="Antibodies vol/well" value={r.antibodiesVolumePerWell !== null && r.antibodiesVolumePerWell !== undefined ? `${r.antibodiesVolumePerWell} mL` : '—'} />
          <Row label="Dead volume" value={r.deadVolume ? `${(r.deadVolume / 1000).toFixed(1)} mL (= ${r.numberOfSetups ?? '—'} setup × 2 mL)` : '—'} />
          <Row label="Raw bead volume" value="—" />
          <Row label="Raw antibody volume" value="—" />
        </dl>
      </div>

      {/* 3. Outputs */}
      <div>
        <h3 className="text-lg font-semibold mb-2 text-[var(--color-foreground)]">3. Outputs</h3>
        <dl className="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-2 text-sm">
          <Row label="New beads" value="—" />
          <Row label="New antibodies" value="—" />
          <Row label="Total bead volume" value="—" />
          <Row label="Total antibody volume" value="—" />
          <Row label="PE volume" value={peVolumeML !== null ? `${peVolumeML.toFixed(1)} mL` : '—'} />
        </dl>
      </div>

      {/* 4. Diluent decision */}
      <div>
        <h3 className="text-lg font-semibold mb-2 text-[var(--color-foreground)]">4. Diluent decision</h3>
        <dl className="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-2 text-sm">
          <Row label="Rule applied:" value={branchLabel} />
          <Row label="Beads diluent" value={dash(r.beadsDiluent)} />
          <Row label="Antibodies diluent" value={dash(r.antibodiesDiluent)} />
        </dl>
      </div>
    </section>
  )
}
```

For `finalVolumeML`: read `src/renderer/src/lib/calculator.ts` to find the existing helper that derives final volume from sampleCount/plateCount/volumePerWell/deadVolume/oldBeads/oldAntibodies. Compose it inline using the snapshotted fields. If that helper requires a structured input the snapshot doesn't preserve (e.g. CalculatorInputs), render PE volume as `—` and route the actual computation to Phase 16 UAT — DO NOT block this plan on hand-deriving the formula. The Group M tests (Plan 15-04 Task 1) and the canonical Group A integration test prove the math elsewhere; the audit trail's job here is to render whatever the snapshot gives it.

For `panels` selector: read `FinalizedRunHeader.tsx` to see how it resolves the panel name (likely `usePanelStore` or a selectionStore selector — match whatever pattern is already used in the file).

Several rows (Total wells, Raw bead volume, Raw antibody volume, New beads, New antibodies, Total bead volume, Total antibody volume) render `—` because the underlying derivations require composing existing calculator helpers against the snapshot. This is the pragmatic v1.0 approach: ship the structure + label set + locked-label rows that have direct snapshot sources; route the derived-value rows to Phase 16 UAT for visual confirmation against hand calculations. Phase 16 will catch any missing computation; an iteration follow-up plan can wire the helpers if UAT requires it.

**File 4 — FinalizedRunView.tsx (MOD).** Locate the composition section (around lines 170-185). Find the `<FinalizedRunHeader />` JSX (line ~173). Insert the two new components between it and the actions row that follows. After the existing `<FinalizedRunHeader />` line (and BEFORE whatever element comes next — typically a `<div className="flex items-center gap-3 print:hidden">` actions row at line ~176), insert:

```tsx
{/* Phase 15 SMK3-16: pre-Phase-15 advisory banner (renders conditionally) */}
<HistoricalRunBanner />

{/* Phase 15 SMK3-15: Calculation Audit Trail — Inputs / Intermediates / Outputs / Diluent decision blocks. Reads exclusively from RunRecord (D-15-01); pure render-from-record. */}
<AuditTrailSection />
```

At the top of FinalizedRunView.tsx, ADD the imports:

```ts
import { HistoricalRunBanner } from './HistoricalRunBanner'
import { AuditTrailSection } from './AuditTrailSection'
```

After all 4 file edits, run typecheck:

```
npm run typecheck
```

It MUST exit 0.

If `useRunStore`, `useSelectionStore`, or other store imports fail to resolve, check the relative-path depth — the new components live in `src/renderer/src/features/run/components/`, so stores at `src/renderer/src/stores/` are reached via `../../../stores/...`.

If JSX type errors appear on `JSX.Element`, the project may use `React.JSX.Element` or `ReactElement` — match whatever pattern other files in `features/run/components/` already use.
  </action>
  <verify>
    <automated>npm run typecheck && [ "$(grep -c 'export function HistoricalRunBanner' src/renderer/src/features/run/components/HistoricalRunBanner.tsx)" = "1" ] && [ "$(grep -c 'export function AuditTrailSection' src/renderer/src/features/run/components/AuditTrailSection.tsx)" = "1" ] && [ "$(grep -c 'SAPE Name' src/renderer/src/features/run/components/FinalizedRunHeader.tsx)" -ge "1" ] && [ "$(grep -c '<AuditTrailSection' src/renderer/src/features/run/components/FinalizedRunView.tsx)" -ge "1" ] && [ "$(grep -c '<HistoricalRunBanner' src/renderer/src/features/run/components/FinalizedRunView.tsx)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `src/renderer/src/features/run/components/HistoricalRunBanner.tsx` exists
    - `grep -c "calculationRulesVersion" src/renderer/src/features/run/components/HistoricalRunBanner.tsx` returns >= `1`
    - `grep -c "This run was saved under previous calculation rules" src/renderer/src/features/run/components/HistoricalRunBanner.tsx` returns `1` (verbatim D-15-11 copy)
    - `grep -c "bg-amber-50" src/renderer/src/features/run/components/HistoricalRunBanner.tsx` returns >= `1`
    - File `src/renderer/src/features/run/components/AuditTrailSection.tsx` exists
    - `grep -c "Calculation Audit Trail" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1`
    - `grep -c "1. Inputs" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1`
    - `grep -c "2. Intermediates" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1`
    - `grep -c "3. Outputs" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1`
    - `grep -c "4. Diluent decision" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1`
    - `grep -c "Rule applied:" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns >= `1`
    - `grep -c "computePeVolumeML" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns >= `1`
    - `grep -c "deriveDiluentBranchLabel" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns >= `1`
    - `grep -c "resolveDiluent" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns >= `1`
    - `grep -c "OVERRIDE" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns >= `1`
    - `grep -c "20% recommended cap" src/renderer/src/features/run/components/AuditTrailSection.tsx` returns `1` (D-15-08 tooltip text)
    - `grep -c "SAPE Name" src/renderer/src/features/run/components/FinalizedRunHeader.tsx` returns >= `1`
    - `grep -c "record.sapeName ?? '—'" src/renderer/src/features/run/components/FinalizedRunHeader.tsx` returns `1`
    - `grep -c "<HistoricalRunBanner" src/renderer/src/features/run/components/FinalizedRunView.tsx` returns >= `1`
    - `grep -c "<AuditTrailSection" src/renderer/src/features/run/components/FinalizedRunView.tsx` returns >= `1`
    - `grep -c "import { HistoricalRunBanner }" src/renderer/src/features/run/components/FinalizedRunView.tsx` returns `1`
    - `grep -c "import { AuditTrailSection }" src/renderer/src/features/run/components/FinalizedRunView.tsx` returns `1`
    - `npm run typecheck` exits `0`
  </acceptance_criteria>
  <done>
    - HistoricalRunBanner component renders amber banner with verbatim D-15-11 copy when calculationRulesVersion !== 'smoke3'
    - SAPE Name row added to FinalizedRunHeader's metadata grid using the existing 2-column row shape
    - AuditTrailSection component renders all 4 labeled blocks with the locked label set from CONTEXT.md `<specifics>`
    - Override badge renders inline with verbatim D-15-08 tooltip text
    - PE volume row uses computePeVolumeML helper (Group M tests prove the math)
    - Diluent decision uses resolveDiluent + deriveDiluentBranchLabel (Group N tests prove the label derivation)
    - All `.tsx` render correctness routes to Phase 16 UAT per VALIDATION.md Manual-Only Verifications table
    - typecheck exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-internal: RunRecord → JSX | RunRecord values render directly into JSX `<dd>` cells. Any string-typed field with attacker-influenced content could in principle inject content via React's JSX coercion. |
| pure helper functions | `computePeVolumeML` + `deriveDiluentBranchLabel` are pure — no external state, no I/O — so they cannot be subverted by store mutations or IPC failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | Operator could craft a malicious `beadsDiluent` / `antibodiesDiluent` string in the master_panel xlsx that contains JSX-like content | accept | React escapes JSX text content by default — `<dd>{record.beadsDiluent}</dd>` is safe. The SMK3-DIL-01 verbatim contract explicitly requires no normalization. Single-machine lab app, master-panel data comes from lab-trusted xlsx files. |
| T-15-15 | Information Disclosure | Audit trail renders the operator's premix selection + override flags; could be visible to a passerby viewing the run document | accept | Same trust domain as the existing FinalizedRunHeader (which already shows operator + sample type + run date). No new disclosure surface. |
| T-15-16 | Tampering | A renderer module could subscribe to runStore + override `currentRun.calculationRulesVersion` to suppress the banner | accept | Single-machine lab app, no adversarial renderer code. The banner is informational; suppressing it does not change persisted data. |
| T-15-17 | Denial of Service | A pre-Phase-15 run with all snapshot fields null could trigger a render-time NaN or undefined-coerce crash | mitigate | Every value cell uses the `dash(v)` helper or explicit `?? '—'` fallback per D-15-12. `computePeVolumeML` accepts null/undefined/0 and silently falls back to 1×. Group M T-M4/T-M5/T-M6 tests prove the silent-fallback path. |
</threat_model>

<verification>
After all 2 tasks:

```bash
# Task 1 — pure helpers + Group M/N tests
ls src/renderer/src/features/run/lib/auditTrail.ts                                    # exists
ls src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts                     # exists
npm test -- --run src/renderer/src/features/run/lib/__tests__/auditTrail.test.ts      # exit 0

# Task 2 — UI components
ls src/renderer/src/features/run/components/HistoricalRunBanner.tsx                   # exists
ls src/renderer/src/features/run/components/AuditTrailSection.tsx                     # exists
grep -c "SAPE Name" src/renderer/src/features/run/components/FinalizedRunHeader.tsx   # >= 1
grep -c "<HistoricalRunBanner" src/renderer/src/features/run/components/FinalizedRunView.tsx  # >= 1
grep -c "<AuditTrailSection" src/renderer/src/features/run/components/FinalizedRunView.tsx    # >= 1
npm run typecheck                                                                      # exit 0
npm run build                                                                          # exit 0 (electron-vite build succeeds)
```

All visual rendering validation routes to Phase 16 UAT (per VALIDATION.md Manual-Only Verifications table — vitest is Node-only, no jsdom).
</verification>

<success_criteria>
- Pure helpers module + tests cover SMK3-17 PE math (7 cases) + D-15-09 branch labels (3 cases)
- HistoricalRunBanner renders amber banner with verbatim D-15-11 copy for pre-Phase-15 runs
- SAPE Name row added to FinalizedRunHeader metadata grid (SMK3-12)
- AuditTrailSection renders 4 labeled blocks with the locked label set (SMK3-15)
- Override badge renders inline beside Old Beads / Old Antibodies value cells when override flag is true (D-15-08)
- All snapshotted-field cells tolerate null/undefined → render `—` (D-15-12)
- FinalizedRunView composes both new components between header and actions row
- typecheck + build both green
- Visual confirmation routes to Phase 16 UAT (no `.tsx` render tests added per VALIDATION.md)
</success_criteria>

<output>
After completion, create `.planning/phases/15-smoke-3-run-document-audit-trail-inserted-2026-05-11/15-04-SUMMARY.md`.
</output>
