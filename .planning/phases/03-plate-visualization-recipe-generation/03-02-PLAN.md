---
phase: 03-plate-visualization-recipe-generation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/renderer/src/features/plate/components/PrepSheet.tsx
  - src/renderer/src/features/plate/components/ReagentChecklist.tsx
  - src/renderer/src/features/plate/components/BeadRegionList.tsx
  - src/renderer/src/features/plate/components/VolumesSummary.tsx
  - src/renderer/src/features/plate/components/PrepInstructions.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Prep sheet displays all calculated volumes from calculator state"
    - "Reagent checklist shows checkbox items for each reagent to prepare"
    - "Bead region section renders with platform context (placeholder for future data)"
    - "Step-by-step instructions guide operator through preparation"
  artifacts:
    - path: "src/renderer/src/features/plate/components/PrepSheet.tsx"
      provides: "Printable preparation sheet composing all sections"
      exports: ["PrepSheet"]
      min_lines: 40
    - path: "src/renderer/src/features/plate/components/ReagentChecklist.tsx"
      provides: "Checkbox list for reagent preparation tracking"
      exports: ["ReagentChecklist"]
      min_lines: 30
    - path: "src/renderer/src/features/plate/components/BeadRegionList.tsx"
      provides: "Bead region list for instrument setup"
      exports: ["BeadRegionList"]
      min_lines: 25
    - path: "src/renderer/src/features/plate/components/VolumesSummary.tsx"
      provides: "Summary of all calculated volumes"
      exports: ["VolumesSummary"]
      min_lines: 30
    - path: "src/renderer/src/features/plate/components/PrepInstructions.tsx"
      provides: "Step-by-step reagent preparation instructions"
      exports: ["PrepInstructions"]
      min_lines: 40
  key_links:
    - from: "src/renderer/src/features/plate/components/PrepSheet.tsx"
      to: "src/renderer/src/features/plate/components/VolumesSummary.tsx"
      via: "VolumesSummary import"
      pattern: "VolumesSummary"
    - from: "src/renderer/src/features/plate/components/PrepSheet.tsx"
      to: "src/renderer/src/features/plate/components/ReagentChecklist.tsx"
      via: "ReagentChecklist import"
      pattern: "ReagentChecklist"
    - from: "src/renderer/src/features/plate/components/VolumesSummary.tsx"
      to: "src/renderer/src/features/calculator/hooks/useCalculator.ts"
      via: "useCalculator import"
      pattern: "useCalculator"
    - from: "src/renderer/src/features/plate/components/BeadRegionList.tsx"
      to: "src/renderer/src/stores/platformStore.ts"
      via: "usePlatformStore import"
      pattern: "usePlatformStore"
---

<objective>
Create prep sheet components for printing reagent preparation recipes.

Purpose: Operators need a printable document showing all calculated volumes, step-by-step preparation instructions, reagent checklists with checkboxes, and bead region list for plate reader instrument setup. This enables them to work away from the computer during physical preparation.

Output: PrepSheet component that composes VolumesSummary, ReagentChecklist, BeadRegionList, and PrepInstructions. Each component reads from calculator state and formats for both screen preview and printing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-plate-visualization-recipe-generation/03-RESEARCH.md
@.planning/phases/03-plate-visualization-recipe-generation/03-01-SUMMARY.md

# Calculator state and types
@src/renderer/src/features/calculator/hooks/useCalculator.ts
@src/renderer/src/stores/calculatorStore.ts
@src/shared/types/calculator.ts

# Platform for bead regions
@src/renderer/src/stores/platformStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VolumesSummary and ReagentChecklist components</name>
  <files>
    src/renderer/src/features/plate/components/VolumesSummary.tsx
    src/renderer/src/features/plate/components/ReagentChecklist.tsx
  </files>
  <action>
    1. Create `src/renderer/src/features/plate/components/VolumesSummary.tsx`:
       - Use useCalculator hook to get outputs and singlesWithVolumes
       - Display in a clean table format:
         - Total wells (unknown + standard)
         - Unknown wells
         - Standard wells
         - Raw volume (uL)
         - Final volume (mL, rounded up)
       - If singles present, show each single analyte with its addition volume
       - Use print-friendly styling (print:text-sm, adequate spacing)
       - Title: "Volume Calculations Summary"

    2. Create `src/renderer/src/features/plate/components/ReagentChecklist.tsx`:
       - Define standard reagent list:
         - Capture Bead Solution (final volume from calculator)
         - Biotinylated Detection Antibody (final volume)
         - SA-PE (Streptavidin-PE) (final volume)
         - Assay Buffer (if custom request type, for dilution)
       - Add any single analytes from calculator state
       - Each item as a label with checkbox + name + volume
       - Checkbox styling: w-5 h-5 print:w-4 print:h-4
       - Use local state to track checked items (useState with Record<string, boolean>)
       - Note: Checkboxes won't persist to print (expected - operator checks on paper)
  </action>
  <verify>
    - `npm run build` succeeds
    - VolumesSummary.tsx exports VolumesSummary
    - ReagentChecklist.tsx exports ReagentChecklist
    - VolumesSummary imports useCalculator
    - ReagentChecklist includes checkbox inputs
  </verify>
  <done>
    VolumesSummary displays all calculated volumes in table format. ReagentChecklist shows checkbox items for standard reagents plus any single analytes. Both components compile and export correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BeadRegionList, PrepInstructions, and PrepSheet components</name>
  <files>
    src/renderer/src/features/plate/components/BeadRegionList.tsx
    src/renderer/src/features/plate/components/PrepInstructions.tsx
    src/renderer/src/features/plate/components/PrepSheet.tsx
  </files>
  <action>
    1. Create `src/renderer/src/features/plate/components/BeadRegionList.tsx`:
       - For now, use placeholder data since bead regions aren't in the current Platform type
       - Display as a numbered list or table with columns: Analyte | Bead Region
       - Title: "Bead Regions for Instrument Setup"
       - Add a note: "Configure plate reader with these bead regions"
       - Use usePlatformStore to get selected platform name
       - Show platform name in header: "{Platform} Bead Regions"
       - Placeholder data: Show "Bead region data not yet configured for this platform"
       - Future: Will read from Platform.beadRegions when available

    2. Create `src/renderer/src/features/plate/components/PrepInstructions.tsx`:
       - Step-by-step numbered instructions for reagent preparation:
         1. "Vortex bead solution for 1 minute to resuspend beads"
         2. "Add {X} mL Assay Buffer to reagent reservoir" (if applicable)
         3. "Add {Y} mL Capture Bead Solution to reservoir"
         4. "For each single analyte: add {Z} uL [analyte name] to master mix"
         5. "Vortex master mix gently to combine"
         6. "Load plate according to plate layout"
       - Use useCalculator to get volumes for instructions
       - Include conditional steps based on requestType (premix vs custom)
       - Style as ordered list with clear numbering

    3. Create `src/renderer/src/features/plate/components/PrepSheet.tsx`:
       - Compose all components in print-friendly layout:
         ```tsx
         <div ref={contentRef} className="p-6 print:p-4 bg-white">
           <header className="mb-6 print:mb-4">
             <h1>Immunoplex Assay Prep Sheet</h1>
             <p>Platform: {selectedPlatform.name}</p>
             <p>Date: {new Date().toLocaleDateString()}</p>
           </header>
           <section className="mb-6 print:break-after-avoid">
             <VolumesSummary />
           </section>
           <section className="mb-6 print:break-after-avoid">
             <ReagentChecklist />
           </section>
           <section className="mb-6">
             <PrepInstructions />
           </section>
           <section>
             <BeadRegionList />
           </section>
         </div>
         ```
       - Accept contentRef prop (RefObject<HTMLDivElement>) for react-to-print
       - Use print:break-after-avoid to keep sections together where sensible
  </action>
  <verify>
    - `npm run build` succeeds
    - BeadRegionList.tsx exports BeadRegionList
    - PrepInstructions.tsx exports PrepInstructions
    - PrepSheet.tsx exports PrepSheet
    - PrepSheet imports and uses all child components
    - BeadRegionList imports usePlatformStore
  </verify>
  <done>
    BeadRegionList shows placeholder for bead regions with platform name. PrepInstructions provides numbered preparation steps using calculator volumes. PrepSheet composes all sections with print-friendly layout and accepts contentRef for printing. All components compile and export correctly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 5 component files exist and export named components
3. VolumesSummary reads from useCalculator hook
4. ReagentChecklist includes checkbox inputs for reagents
5. PrepInstructions shows numbered steps with volume placeholders
6. PrepSheet composes all child components with contentRef prop
7. BeadRegionList imports usePlatformStore for platform context
</verification>

<success_criteria>
- VolumesSummary shows total wells, unknown/standard wells, raw and final volumes
- ReagentChecklist shows checkboxes for standard reagents and singles
- BeadRegionList shows platform name with placeholder for regions
- PrepInstructions shows numbered preparation steps with actual volumes
- PrepSheet composes all sections in print-ready layout
- All components use calculator state (no hardcoded values except placeholders)
</success_criteria>

<output>
After completion, create `.planning/phases/03-plate-visualization-recipe-generation/03-02-SUMMARY.md`
</output>
