# Synthesized Decisions (ADR intel)

No ADR-type documents are present in this ingest set.

The single ingested doc (`smoke-3.md`) was classified as a PRD; its requirements are written to `requirements.md`, and the embedded calculation rules are written to `constraints.md` (as protocol-type constraints, since they assert how the math must behave rather than what the operator must be able to do).

For existing locked decisions in the project, see:

- `/Users/pawj/Lab_Dev/immunoplex-assay-calculator/.planning/PROJECT.md` §Key Decisions
- `/Users/pawj/Lab_Dev/immunoplex-assay-calculator/.planning/STATE.md` §Decisions
- `/Users/pawj/Lab_Dev/immunoplex-assay-calculator/.planning/PANEL-UPLOAD-V2-SPEC.md` §Per-tab layout (locked) — treated as LOCKED for the purpose of this synthesis per orchestrator instruction (referenced as v2 contract from PROJECT.md, even though formal `/gsd-discuss-phase` lock has not yet sealed it).
